# 仕様 — NeNe Folio

> Status: normative（規範）/ 2026-09-09 初版（施主の口述 2026-09-08 を FR に起こしたもの）
> 「決め打ちしない」項目は明示する。実装して窮屈なら変えてよい。変えたら本書と ADR を更新する。

## 0. 一言

枠の無い窓。左にカテゴリごとの索引（ドロワー）、右に Markdown のビューと編集。ファイルは `data/` の中の md と json だけ。

## 1. 機能要件

| ID | 要件 | 備考 |
| --- | --- | --- |
| FR-001 | 窓は枠・タイトルバーを持たない | `WS_POPUP`。DWM の影とスナップは残す |
| FR-002 | 実行ファイルと同じ場所の `data/` を製品ディレクトリとする。`data/<カテゴリ>/` がカテゴリ、その中の `*.md` がノート | カテゴリ = ディレクトリ 1 段。入れ子は初版では扱わない |
| FR-003 | 左のドロワーに、カテゴリごとにノート名を索引として表示する | ノート名はファイル名（拡張子なし） |
| FR-004 | カテゴリ行はアコーディオンで、クリックで中の展開・折り畳みをトグルできる | 展開状態は保存する（FR-008） |
| FR-005 | 索引のノートをクリックすると右のペインに md を表示する（ビュー） | 見出し・強調・コード・リスト・リンク・引用のサブセット。RichEdit に RTF で流す |
| FR-006 | 編集モードで md を編集し、保存すると同じファイルに書き戻す | 同じ RichEdit をプレーンテキストで使う。IME が使える。保存は編集モードを抜けるとき（「閲覧」札・終了）と Ctrl+S、それに別ノートへの切り替えとフォーカスが索引へ移るとき（編集モードは保つ・FR-018）。**Ctrl+S は入力面（Ex・パレット・検索欄・置換の欄・絞り込み欄）の中でも同じ経路で保存し、欄は開いたまま・語も選択もフォーカスも変えない**（Issue #67・ADR 0016 の補正 2）。同じなら書かない。改行は元の本文に合わせ、BOM は書かない（ADR 0006 / ADR 0013） |
| FR-007 | カテゴリは色と表示順を持ち、`data/categories.json` に永続化する | 版を持つ（ARC-009） |
| FR-008 | 各カテゴリの `index.json` がそのカテゴリのノートの表示順を持つ | json に無い md は末尾に付け、json にあって無い md は無視して次の保存で消す |
| FR-009 | 索引はドラッグで表示順を変えられる（カテゴリ行同士・ノート行はカテゴリ内でも別カテゴリへでも） | 別カテゴリへの移動は md のファイル移動が先で、両方の `index.json` が追随する。移動先に同名があれば拒む（ADR 0008・2026-09-10 改定） |
| FR-010 | カテゴリ行の右クリックで色を変えられる | `ChooseColor`。ノート行のメニューは初版では無し |
| FR-011 | 現在表示中のノートの中を検索できる。GUI「このノート内を検索」・Ctrl+F・INDEX と閲覧本文の `/`（前方）・`?`（後方）が同じ検索欄を開き、`n` / `N` が直前の方向／逆方向へ繰り返し、F3 / Shift+F3 が向きを名指しして次／前の一致へ進む | 対象は RichEdit が今表示している平文（閲覧＝描画後の文字・編集＝未保存本文）。一致は選択範囲 1 つで示し、端で巡回する。ASCII の英字だけ大小を無視する。判断は core の `note_search`、語と方向は application、選択は RichEdit（2026-09-17 改定・ADR 0023 / Issue #58）。**全ノートの本文で索引を絞り込む検索は FR-032 の別機能**で、語も状態も別に持つ |
| FR-012 | ドロワーがあふれたらホイールと ↑↓ / PgUp / PgDn でスクロールできる。**スクロールバーは表示しない** | 上端・下端に短いフェードであふれを示す |
| FR-013 | 表示は Per-Monitor v2 の DPI で崩れない | マニフェスト |
| FR-014 | 版はビルド時に 1 か所（`CMakeLists.txt`）から埋め込む | DEVELOPMENT_WORKFLOW 第 9 節 |
| FR-015 | json の読み込み・保存の失敗と md の保存の失敗を利用者に見える形で示す | 既定値へ黙って落とさない（ARC-009） |
| FR-016 | 設定画面・「操作」メニュー・パレット・頭の歯車・`:set theme=system` / `light` / `dark` からライト・ダーク・OS に従うを選べ、値は `data/settings.json` に残る。配色は Ubuntu を参考にしたオレンジ・紫・落ち着いた地の色 | 既定は `system`（OS のアプリのモード）で、`system` のあいだは OS の切替へその場で追従する。色の正本は `rtf_palette`（core）と `folio_palette`（ui）の 2 か所だけ。切り替えても本文・Undo・未保存の印・モード・スクロール位置を失わない。Issue #75 / ADR 0031（2026-09-22 改定） |
| FR-017 | md を書き戻す直前の本文を `data/.history/<カテゴリ>/<ノート>/1.md`〜`5.md` に連番で残し、6 つ目は捨てる。履歴を残せなければ保存しない | 誤った自動保存の保険（2026-09-10 施主）。時刻は使わない。`.` で始まるディレクトリはカテゴリとして走査しない。戻す操作は別途（ADR 0012） |
| FR-018 | フォーカスは索引と本文の 2 区画。索引で `j` / `k` が次／前のノート（折り畳んだ・空のカテゴリ行にも止まり、そのあいだ右ペインは変えない）、`gg` / `G` が最初／最後、`h` / `l` が折り畳み／展開、`Enter` で本文へ。本文で `Esc` は索引へ。切り替えのたびに編集中なら保存し、モード（閲覧／編集）は保つ | 施主の希望（2026-09-10）。鍵で動かしたときだけ選択を見える位置へ寄せる。Escape で窓は閉じない（Alt+F4 のみ）。区画は Win32 のフォーカスそのもの（ADR 0013 / ADR 0015） |
| FR-019 | INDEX にフォーカスがあるとき `:` でコマンド入力へ入り、`:w` 保存・`:q` 終了・`:wq` 保存して終了などの基本 Vim コマンドを使える | 未保存の `:q` は終了を拒否。`:q!` は現在の未保存変更だけを破棄。全 Vim 互換ではなく対応表を持つ。計画 Issue #37（2026-09-12） |
| FR-020 | `Ctrl+P`・F1・`:h`・GUI「ヘルプ」が同じコマンドパレットを開き、コマンドと区画別キーバインドを説明する | 操作識別子・有効条件・実行経路は一つ。入力面を開くだけでは本文を保存・再読込しない。**INDEX の `?` は 2026-09-17 に後方検索（FR-011）へ移した**ので、ヘルプの入口は上の 4 つ。Issue #37（2026-09-13 追補・ADR 0023 で改定） |
| FR-021 | `:set number` / `:set nonumber` / `:set nu!`、操作パレットと「操作」メニューの「行番号の表示を切り替える」から Markdown 原文の論理行番号を表示・非表示にでき、値は `data/settings.json` に残る | **編集モードで文書があるときだけ**出す（閲覧の RTF では表示行が原文の行に対応しないので出さない。無題の編集中は出す）。折り返しの継続行に番号を重複させない。本文には書き込まない。番号は主窓が本文の左の空きに描き、3〜4 桁（9999 行）までは本文が動かない。設定画面の 3 段目「行番号」（表示する／表示しない）も同じ意図へ結ぶ（Issue #149・ADR 0031 補正 4）。Issue #39 / ADR 0025・ADR 0026 |
| FR-022 | 設定アイコンと閉じるアイコンを持ち、キーボードでも同じ操作を呼べる | 閉じるは既存の保存して終了する操作へ結ぶ（Issue #37）。設定はその左隣の歯車で、「操作」メニュー・パレットの「設定」と同じ面を開く（Issue #75）。どちらも GDI の線で描く。未実装の入口だけを表示しない |
| FR-023 | 現在開いている Markdown の**編集中の本文だけ**に正規表現置換を実行できる | 「操作」メニューと操作パレットの「置換」が下端に置換の欄（パターンと置換文字列の 2 つの入力・「1 件」「すべて」・状態の 1 行）を開き、打つたびに**対象名と一致件数**を出す。Enter が 1 件、Ctrl+Enter がすべて、Tab が欄の往復、Esc が閉じて元の区画へ戻す。Ex の `:%s/パターン/置換/[g]` は欄を開かず直接当てる（`g` が無ければ**各論理行の最初の一致だけ**）。適用は `EM_REPLACESEL` 1 回の**Undo 1 単位**で、md は書かない（保存と履歴は既存経路）。エンジンは OS 同梱の ICU で、大小無視は正規表現の `(?i)`（Unicode の full case folding なので検索の「ASCII だけ」とは意味が違う）。置換文字列の文法は Vim 寄りの自前（`&` と `\0`・`\1`〜`\9`・`\r` と `\n`・`\\`・`\&`・`\/`）で、それ以外の `\x` と末尾の単独 `\` は断る。閲覧中は「編集モードではありません。」の 1 行を出して本文は変えない。**全ノート一括置換・`:s` の行範囲・確認付き置換は対象外**。Issue #41 / ADR 0028（2026-09-22 実装） |
| FR-024 | 設定画面・`:set language=ja` / `en` / `zh-Hans` から表示言語を日本語・English・简体中文から選べ、値は `data/settings.json` に残る | 中国語は簡体字（2026-09-12 hide 確定）。操作名・説明・失敗表示・ヘルプを翻訳し、md・ファイル名・台帳と設定のキー・Ex の別名と `:set` の語・鍵の名前は翻訳しない。文言の正本は core の `ui_text` の表 1 か所で、列の網羅は CNF-011 が守る。書体は言語ごと（ja / en は `Yu Gothic UI`・zh-Hans は `Microsoft YaHei UI`）で、主窓の自前描画は Consolas のまま。切り替えても本文・Undo・未保存の印・モード・読んでいる論理行を失わない。**简体中文は母語話者の確認を得ていない**。Issue #76 / ADR 0032（2026-09-22 実装） |
| FR-025 | Google Fonts の日本語 Noto 系ゴシック・簡体字用ゴシックを同梱する | Noto Sans JP / Noto Sans SC の固定版・ハッシュ・ライセンスを `fonts/` に同梱。ネット接続も OS へのインストールも不要。欧文（English の UI）は Noto Sans JP のラテン文字で描く（ADR 0036 補正 5）。Issue #42 / ADR 0036（2026-09-23 実装） |
| FR-026 | 右ペインのパンくずをPowerline風の色面と矢印でつなぐ | 番号・カテゴリ・ノートの順。カテゴリ保存色を番号の面に使い、字は読める黒白を選ぶ。狭い時はノート名→カテゴリ名の順に省略し、操作札と番号を確保。Issue #45 / ADR 0017（2026-09-13 hide指定） |
| FR-027 | 日本語の保存・操作・ヘルプを画面から使い、閲覧本文からコマンドと編集へ進める | 共通登録表とdispatcher。F1・`:h`、閲覧本文の`:`、`i` / `:startinsert`、説明の折畳みと候補スクロール。Issue #49 / ADR0018 |
| FR-028 | 編集本文でCtrl+h/j/k/lが通常の矢印と同じ左／下／上／右へ移動する | 本文・Undoを維持。Shift/Alt併用・IME変換中・他の区画では追加割当しない。HHKBのAlt配置に合わせた選択。Issue #51 / ADR0019 |
| FR-029 | 新しい無題に本文を書き、名前と既存カテゴリを指定して初回保存できる | GUI・Ctrl+N・`:enew`、保存・Ctrl+S・`:w 名前`が共通経路。取消・失敗は本文とUndoを保持。同名は上書きせず、md公開後の台帳失敗は再試行する。Issue #53 / ADR0020 |
| FR-030 | 元ファイルを先に保存せず、現在の本文を別名で保存して開く | GUI・Ctrl+Shift+S・:saveas 名前。閲覧はMarkdown原文、編集は未保存本文を使い、モードとUndoを保持。元の履歴は変更しない。Issue #55 / ADR0021 |
| FR-031 | 現在のノートのmdと履歴を同じカテゴリの新しい名前へ移す | GUI「名前を変更」・F2・`:rename 名前`。編集中は先に保存し、本文・モード・Undoは変えない。同名は変更なし、大小文字だけの別名と既存名は拒否。`data/.rename.json`を先に公開し、履歴→md→index.jsonの順に置換なしで移して記録を消す。未完了なら次の操作が先に再開し、`:q!`でも意図は残って次回起動が続ける。対象はローカルNTFSで、断るのはシンボリックリンクとjunction（name surrogateのタグ）だけ。同期フォルダのプレースホルダなど他の再解析ポイントは通常のファイルとして扱う。Issue #56 / ADR0022（Issue #61で判定をタグへ変更） |
| FR-032 | ドロワーの頭に常設した欄（Ctrl+Shift+F）から、全ノートの**名前と本文**で索引を絞り込める | **本文は起動時ではなく、空でない語が初めて来たときに 1 回だけ読む**（遅延読み込み）。以後は保存成功・新規公開・改名・移動で該当の写しだけ差し替える。読めないノートは写しを持たず一致しない。一致したノートだけを索引に残し、一致を持つカテゴリは折り畳んでいても展開して並べる。ASCII の英字だけ大小を無視する部分一致。**絞り込み中は並び替えとカテゴリの折畳／展開（ドラッグ・`h` / `l`・クリック）を断る**（色・閲覧・編集・保存・新規・改名は可）。**カテゴリ行のクリックと `h` / `l` は失敗を出さずに何もしない**（ドラッグと同じ無音の拒否。閾値を超えて動かしたドラッグは選択にも落ちない）。**対象は保存済みの本文**で、編集中でまだ保存していない内容は一致しない（次の保存で写しが追随する）。現在の文書は一致しなくても閉じない。Esc と Enter は索引へ戻して絞り込みを保ち、空にすると解ける。**ノート内検索（FR-011）とは対象も語も別**で、同期しない。Issue #40 / ADR 0024（2026-09-17 実装） |

## 2. 決め打ちしない項目

| 項目 | 初期値 | 変えてよい条件 |
| --- | --- | --- |
| 窓の初期サイズとドロワー幅 | 960×640・ドロワー 240 | 実機で見て決める |
| 窓の最小サイズ | 560×360（96 DPI・DPI で拡大） | ドロワー + 番号 + 札 2 つが収まる幅。狭い画面で困ったら ADR（2026-09-10・Issue #22・ADR 0011） |
| 右ペインの頭のはみ出し | 札 → 番号 → ノート名 → カテゴリ名の順に幅を確保し、足りなければノート名、次にカテゴリ名の末尾を省略記号にする | ADR 0011。真ん中の省略が欲しくなったら ADR |
| 展開状態の保存先 | `categories.json` の `expanded` | 毎回全展開の方が良ければ外す |
| 別カテゴリへのドラッグ移動 | やる（2026-09-10 に施主が初版を使って判断・Issue #15） | ファイル移動を伴う。順序と同名の扱いは ADR 0008 |
| ノート行の右クリック | 無し | 同上 |
| md サブセットの範囲 | 見出し・強調・コード・リスト・リンク・引用 | 足りない記法が実測で出たら ADR |
| 検索の一致方式 | 部分一致（大文字小文字を区別しない） | 速度を測ってから |
| テーマの切り替え | 決めた（Issue #75 で実装済み）。設定の版 2 に `"theme": "system" \| "light" \| "dark"` を持つ | ADR 0031 が ADR 0005 の「起動時に 1 回読む」を置き換えた |
| 表示言語 | 決めた（Issue #76 で実装済み）。設定の版 3 に `"language": "ja" \| "en" \| "zh-Hans"` を持つ | 初期は `ja`。OS の表示言語への追従は対象外（ADR 0032 の決定 9） |

### 2-1. `data/settings.json`（版 3）

効果を実装したキーだけが入る。**キーの順序も形のうち**で、違えば既定値へ黙って落ちずに拒む（ARC-009）。

```json
{
  "version": 3,
  "number": false,
  "theme": "system",
  "language": "ja"
}
```

- `number`（FR-021 / 版 1 から）・`theme`（FR-016 / 版 2 で追加。`"system"` / `"light"` / `"dark"`）・
  `language`（FR-024 / 版 3 で追加。`"ja"` / `"en"` / `"zh-Hans"`。綴りは BCP 47 のまま）。
- **版 1 の形 `{"version": 1, "number": <bool>}` と版 2 の形
  `{"version": 2, "number": <bool>, "theme": "…"}` も読めて**、足りないキーに既定値
  （`theme` は `"system"`・`language` は `"ja"`）を埋める（移行）。
  書くのは常に版 3 なので、旧版のファイルは次に書いた時点で版 3 になる。
- ファイルが無ければ既定値で始め、**起動時には作らない**。未知の版・壊れた値は既定値で起動して理由を 1 回見せ、
  そのセッションは設定を変えず**上書きもしない**（ADR 0025 の決定 6）。
- 変更は**先に書いてから採用する**ので、表示と保存が食い違わない。

## 3. 非要件（やらないこと）

- CommonMark 完全対応・HTML の描画・画像の埋め込み
- 複数ウィンドウ・タブ・分割表示
- 同期・クラウド・ファイル監視（外部で変わった md は再起動で拾う）
- 常駐・ホットキー・トレイ
- UI ライブラリ（GTK / Qt / WinUI / WebView2）。ADR 0002
- 背景スレッド（C-013）
- Vim 全互換・プラグイン・シェルコマンド実行・全ファイルへの一括置換（2026-09-12 の追加要件の初版範囲）

追加要件の実装順と確認条件は [2026-09-12 の計画](docs/plans/2026-09-12-command-workspace.md)。
FR-019〜031 と FR-011 / FR-016 の追加部分の実装・検証状態は [現在タスク](docs/todo/current.md) で区別する。仕様への記載だけでは実装済みを意味しない。

## 4. 設計上の固定点（憲章との対応）

- 文書のNONE / NAMED / UNTITLEDと保存後の台帳未同期はapplicationが所有する。無題に架空の索引行を作らず、未保存本文とUndoはRichEditだけが持つ（ADR0020）。
- 同じ`data/`を使うプロセスは1つで、`data/.nenefolio.lock`の共有なしハンドルが所有を決める。書けない`data/`は閲覧のために起動を許し、書込は各操作の失敗として理由を出す。未完了の改名は`data/.rename.json`が唯一の正本で、application は意図を1つだけ保持する（ADR0022）。

- ドロワーのレイアウト（行の y 座標・ヒットテスト・ドロップ先・スクロール上限）は `src/core` の純関数で、UI は描くだけ（ARC-011 / ADR 0002）
- カテゴリと索引の台帳・md → RTF 変換・全文検索の索引は `src/core` に置き、`windows.h` も `stdio.h` も含めない。ここだけ厚くテストする（ARC-003）
- 文書一覧・カテゴリ設定・選択中の文書・表示モード・スクロール量・検索語の所有者は `src/application` に 1 つ（ARC-004）
- ファイルの走査と読み書きは `src/adapters/win32` の永続化ポートだけが行う（ARC-007 / ARC-009）
- 編集中の本文だけは RichEdit（UI）が所有し、保存の意図で application へ渡す（ARC-005 の隔離区画）

## 5. 対応環境

Windows 10 version 2004 以降（x64）、DWM 有効。RichEdit は `Msftedit.dll`（OS 同梱）、正規表現は `icu.dll`（OS 同梱・Windows 10 1903 以降）。追加ランタイム無し（`/MT`）。OS 同梱 DLL の版は OS が決めるので固定できない（測定時は ICU 72.1 / Unicode 15.1）。
