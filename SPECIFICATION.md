# 仕様 — NeNe Folio

> Status: normative（規範）/ 2026-09-09 初版（施主の口述 2026-09-08 を FR に起こしたもの）
> 「決め打ちしない」項目は明示する。実装して窮屈なら変えてよい。変えたら本書と ADR を更新する。

## 0. 一言

枠の無い窓。左にカテゴリごとの索引（ドロワー）、右に Markdown のビューと編集。ファイルは `data/` の中の md と json だけ。

## 1. 機能要件

| ID | 要件 | 備考 |
| --- | --- | --- |
| FR-001 | 窓は枠・タイトルバーを持たない | `WS_POPUP`。DWM の影とスナップは残す |
| FR-002 | 実行ファイルと同じ場所の `data/` を製品ディレクトリとする。`data/<カテゴリ>/` がカテゴリ、その中の `*.md` がノート | カテゴリ = ディレクトリ 1 段。入れ子は初版では扱わない |
| FR-003 | 左のドロワーに、カテゴリごとにノート名を索引として表示する | ノート名はファイル名（拡張子なし） |
| FR-004 | カテゴリ行はアコーディオンで、クリックで中の展開・折り畳みをトグルできる | 展開状態は保存する（FR-008） |
| FR-005 | 索引のノートをクリックすると右のペインに md を表示する（ビュー） | 見出し・強調・コード・リスト・リンク・引用のサブセット。RichEdit に RTF で流す |
| FR-006 | 編集モードで md を編集し、保存すると同じファイルに書き戻す | 同じ RichEdit をプレーンテキストで使う。IME が使える。保存は編集モードを抜けるとき（「閲覧」札・終了）と Ctrl+S、それに別ノートへの切り替えとフォーカスが索引へ移るとき（編集モードは保つ・FR-018）。同じなら書かない。改行は元の本文に合わせ、BOM は書かない（ADR 0006 / ADR 0013） |
| FR-007 | カテゴリは色と表示順を持ち、`data/categories.json` に永続化する | 版を持つ（ARC-009） |
| FR-008 | 各カテゴリの `index.json` がそのカテゴリのノートの表示順を持つ | json に無い md は末尾に付け、json にあって無い md は無視して次の保存で消す |
| FR-009 | 索引はドラッグで表示順を変えられる（カテゴリ行同士・ノート行はカテゴリ内でも別カテゴリへでも） | 別カテゴリへの移動は md のファイル移動が先で、両方の `index.json` が追随する。移動先に同名があれば拒む（ADR 0008・2026-09-10 改定） |
| FR-010 | カテゴリ行の右クリックで色を変えられる | `ChooseColor`。ノート行のメニューは初版では無し |
| FR-011 | ドロワーの上部に検索窓があり、全ノートの本文から高速に検索できる。INDEX にフォーカスがあるとき `/` で検索窓へ移る | 起動時に全 md をメモリへ載せ、保存成功時に差分更新。一致したノートだけを索引に残す。2026-09-12 改定・計画 Issue #40 |
| FR-012 | ドロワーがあふれたらホイールと ↑↓ / PgUp / PgDn でスクロールできる。**スクロールバーは表示しない** | 上端・下端に短いフェードであふれを示す |
| FR-013 | 表示は Per-Monitor v2 の DPI で崩れない | マニフェスト |
| FR-014 | 版はビルド時に 1 か所（`CMakeLists.txt`）から埋め込む | DEVELOPMENT_WORKFLOW 第 9 節 |
| FR-015 | json の読み込み・保存の失敗と md の保存の失敗を利用者に見える形で示す | 既定値へ黙って落とさない（ARC-009） |
| FR-016 | 設定でライト・ダーク・OS に従うを選べる。配色は Ubuntu を参考にしたオレンジ・紫・落ち着いた地の色とする | 初期値は OS に従う。色の正本は `rtf_palette` と `folio_palette`。手動選択と新配色は計画 Issue #38。現実装は ADR 0005 の起動時選択（2026-09-12 改定） |
| FR-017 | md を書き戻す直前の本文を `data/.history/<カテゴリ>/<ノート>/1.md`〜`5.md` に連番で残し、6 つ目は捨てる。履歴を残せなければ保存しない | 誤った自動保存の保険（2026-09-10 施主）。時刻は使わない。`.` で始まるディレクトリはカテゴリとして走査しない。戻す操作は別途（ADR 0012） |
| FR-018 | フォーカスは索引と本文の 2 区画。索引で `j` / `k` が次／前のノート（折り畳んだ・空のカテゴリ行にも止まり、そのあいだ右ペインは変えない）、`gg` / `G` が最初／最後、`h` / `l` が折り畳み／展開、`Enter` で本文へ。本文で `Esc` は索引へ。切り替えのたびに編集中なら保存し、モード（閲覧／編集）は保つ | 施主の希望（2026-09-10）。鍵で動かしたときだけ選択を見える位置へ寄せる。Escape で窓は閉じない（Alt+F4 のみ）。区画は Win32 のフォーカスそのもの（ADR 0013 / ADR 0015） |
| FR-019 | INDEX にフォーカスがあるとき `:` でコマンド入力へ入り、`:w` 保存・`:q` 終了・`:wq` 保存して終了などの基本 Vim コマンドを使える | 未保存の `:q` は終了を拒否。`:q!` は現在の未保存変更だけを破棄。全 Vim 互換ではなく対応表を持つ。計画 Issue #37（2026-09-12） |
| FR-020 | `Ctrl+P` でコマンドパレットを開き、コマンド入力と同じ操作を実行できる。INDEX の `?` は `:help` と同じヘルプを開き、コマンドと区画別キーバインドを説明する | 操作識別子・有効条件・実行経路は一つ。入力面を開くだけでは本文を保存・再読込しない。本文・入力欄の `?` は文字入力。Issue #37（2026-09-13 追補） |
| FR-021 | `:set number` / `:set nonumber`、パレット、設定から Markdown 原文の論理行番号を表示・非表示にできる | 初版は編集モードの行番号。折り返しの継続行に番号を重複させない。本文には書き込まない。計画 Issue #39 |
| FR-022 | 設定アイコンと閉じるアイコンを持ち、キーボードでも同じ操作を呼べる | 設定は Issue #38、閉じるは Issue #37。閉じるアイコンは既存の保存して終了する操作へ結ぶ。未実装の入口だけを表示しない |
| FR-023 | 現在開いている Markdown に正規表現置換を実行できる | 対象名・一致件数を表示し、適用は編集本文へ。保存と履歴は既存経路。全ノート一括置換は対象外。計画 Issue #41 |
| FR-024 | 設定画面で表示言語を日本語・English・简体中文から選べる | 中国語は簡体字（2026-09-12 hide 確定）。操作名・説明・失敗表示を翻訳し、md・ファイル名・Ex 構文は翻訳しない。版付き設定を永続化。計画 Issue #38 |
| FR-025 | Google Fonts の日本語 Noto 系ゴシック・簡体字用ゴシックと、Arial / Helvetica に近い欧文サンセリフを同梱する | Noto Sans JP / Noto Sans SC / Arimo を候補とし、固定版・ハッシュ・ライセンスを同梱。ネット接続も OS へのインストールも不要。計画 Issue #42 |
| FR-026 | 右ペインのパンくずをPowerline風の色面と矢印でつなぐ | 番号・カテゴリ・ノートの順。カテゴリ保存色を番号の面に使い、字は読める黒白を選ぶ。狭い時はノート名→カテゴリ名の順に省略し、操作札と番号を確保。Issue #45 / ADR 0017（2026-09-13 hide指定） |
| FR-027 | 日本語の保存・操作・ヘルプを画面から使い、閲覧本文からコマンドと編集へ進める | 共通登録表とdispatcher。F1・`:h`、閲覧本文の`:`、`i` / `:startinsert`、説明の折畳みと候補スクロール。Issue #49 / ADR0018 |
| FR-028 | 編集本文でCtrl+h/j/k/lが通常の矢印と同じ左／下／上／右へ移動する | 本文・Undoを維持。Shift/Alt併用・IME変換中・他の区画では追加割当しない。HHKBのAlt配置に合わせた選択。Issue #51 / ADR0019 |

## 2. 決め打ちしない項目

| 項目 | 初期値 | 変えてよい条件 |
| --- | --- | --- |
| 窓の初期サイズとドロワー幅 | 960×640・ドロワー 240 | 実機で見て決める |
| 窓の最小サイズ | 560×360（96 DPI・DPI で拡大） | ドロワー + 番号 + 札 2 つが収まる幅。狭い画面で困ったら ADR（2026-09-10・Issue #22・ADR 0011） |
| 右ペインの頭のはみ出し | 札 → 番号 → ノート名 → カテゴリ名の順に幅を確保し、足りなければノート名、次にカテゴリ名の末尾を省略記号にする | ADR 0011。真ん中の省略が欲しくなったら ADR |
| 展開状態の保存先 | `categories.json` の `expanded` | 毎回全展開の方が良ければ外す |
| 別カテゴリへのドラッグ移動 | やる（2026-09-10 に施主が初版を使って判断・Issue #15） | ファイル移動を伴う。順序と同名の扱いは ADR 0008 |
| ノート行の右クリック | 無し | 同上 |
| md サブセットの範囲 | 見出し・強調・コード・リスト・リンク・引用 | 足りない記法が実測で出たら ADR |
| 検索の一致方式 | 部分一致（大文字小文字を区別しない） | 速度を測ってから |
| テーマの切り替え | OS に従うを初期値とし、設定で light / dark / system を選ぶ（計画 Issue #38） | 2026-09-12 の明示要望。設定・適用の契約を新 ADR に置き、ADR 0005 の該当決定を置き換える |

## 3. 非要件（やらないこと）

- CommonMark 完全対応・HTML の描画・画像の埋め込み
- 複数ウィンドウ・タブ・分割表示
- 同期・クラウド・ファイル監視（外部で変わった md は再起動で拾う）
- 常駐・ホットキー・トレイ
- UI ライブラリ（GTK / Qt / WinUI / WebView2）。ADR 0002
- 背景スレッド（C-013）
- Vim 全互換・プラグイン・シェルコマンド実行・全ファイルへの一括置換（2026-09-12 の追加要件の初版範囲）

追加要件の実装順と確認条件は [2026-09-12 の計画](docs/plans/2026-09-12-command-workspace.md)。
FR-019〜026 と FR-011 / FR-016 の追加部分の実装・検証状態は [現在タスク](docs/todo/current.md) で区別する。仕様への記載だけでは実装済みを意味しない。

## 4. 設計上の固定点（憲章との対応）

- ドロワーのレイアウト（行の y 座標・ヒットテスト・ドロップ先・スクロール上限）は `src/core` の純関数で、UI は描くだけ（ARC-011 / ADR 0002）
- カテゴリと索引の台帳・md → RTF 変換・全文検索の索引は `src/core` に置き、`windows.h` も `stdio.h` も含めない。ここだけ厚くテストする（ARC-003）
- 文書一覧・カテゴリ設定・選択中の文書・表示モード・スクロール量・検索語の所有者は `src/application` に 1 つ（ARC-004）
- ファイルの走査と読み書きは `src/adapters/win32` の永続化ポートだけが行う（ARC-007 / ARC-009）
- 編集中の本文だけは RichEdit（UI）が所有し、保存の意図で application へ渡す（ARC-005 の隔離区画）

## 5. 対応環境

Windows 10 version 2004 以降（x64）、DWM 有効。RichEdit は `Msftedit.dll`（OS 同梱）。追加ランタイム無し（`/MT`）。
