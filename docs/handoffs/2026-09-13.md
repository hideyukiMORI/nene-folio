# 引き継ぎ — 2026-09-13

## 18:18 JST — hideの停止指示を受けた再開点

**記録を保存したら作業を止める。hideが明示的に再開を依頼するまで、以前の「止まらず進めて」を根拠に続行しない。**
本日の成果/検証の一覧は[日報の最新節](../reports/2026-09-13.md)。ここが最後の再開点で、下の時刻付き節は履歴。

### どこから再開するか

- ルート `C:/Users/info/WORKS/NeNeFolio` はcleanなmain `35d86af3f45c8c4993786e3ede17a96f2dace467`。#53/PR54まで統合済み。
- 別名保存: `out/worktrees/55-save-as` / `feat/55-save-as` / `770880268a872a982203f2e717c910db9d949f06`。clean、push済み。
  [PR57](https://github.com/hideyukiMORI/nene-folio/pull/57) はReady/OPEN。全体ゲートは成功したがCI開始/成功が未確認なので未統合。
- 改名: **`out/worktrees/56-rename` / `feat/56-rename-note`**。ソースの区切りは `2baa0d13c13c19c74dafcbb2b2021946a044f57a`。
  この引き継ぎ/日報を含む文書保存コミットがその後に続く。#56のPRはまだ作成していない。機能途中なのでReady/Issue完了にはしない。
- #56はmain35d86afから分岐し、**別名保存#55のソースをまだ含まない**。最新版の文書は#56作業枝にあり、ルートmainの文書は統合前の状態。
  #55の統合・main同期後に#56へ最新mainを取り込んでからapplication/UIを接続する。両枝のcodec/キーの状態を取り違えない。

### 次の操作

1. GitHubのPR57とCIを読み直す。17:51以降GraphQL内部エラー/HTTP500/502があり、Ready変更がエラー後に反映された。
   18:16 JST時点のstatusCheckRollupと `gh run list --branch feat/55-save-as` は空。成功を推定しない。
   API再試行前は現在状態を確認して重複PRを作らない。必要なら保存済みPR本文でeditedイベントを発生させる。
   CI成功後にsquash mergeし、ルートmainをff-only同期する。必須チェックを迂回しない。
2. #56へ同期したmainを取り込み、[ADR0022](../adr/0022-note-rename-and-recovery.md)と7規範文書を読んで続ける。
   #55のADR0021は現在55-save-asにある。下記中核を作り直さず、既存store/createと同じ経路へつなぐ。
3. adapter寿命のdataロック、NTFS/ID/reparse検証、記録公開前と公開後PENDINGの閉じた結果、履歴→md→台帳→journal削除、起動前復旧を実装する。
   記録後は失敗を通常の拒否へ丸めず意図を保持。履歴IDなしは旧/新の両側不在だけ受理。各再開段階を実ファイルfixtureで測る。
4. applicationのPENDING保持と同じ同期入口、GUI/F2/:renameへ接続。未保存本文は既存の保存成功後に改名。
   pendingの:qは問い合わせのまま拒否、保存などが再試行。フォームの名前は公開後に固定し、閉じても意図を捨てない。
   本文/Undo/モードを再投入せず守り、取消/衝突/履歴失敗/各クラッシュ境界/IMEを検証する。
5. 改名が完成した後、現在ノートの/・?・n・N/GUI検索へ進む。?ヘルプを先に消さず、後方検索と同じ単位で移す。
   全ノート検索#40はCtrl+Shift+Fで独立。行番号#39、置換#41、設定/テーマ/言語#38、同梱フォント#42、カテゴリ作成/実機/配布は後続。

### #56で保存した中核と検証

- `src/core/note_ledger.{c,h}`: 同じ位置での改名をremoved/insertedで合成。note_ledger_readを既存parseの正典にしてnested codecへ公開。
- `note_rename.*` / target / outcome: カテゴリ/旧名/新名/完成後台帳を不透明型で所有。生成/読取/書込/比較/完了時の所有権移転。
- `rename_journal.*` / outcome: `{version:1, rename:{category,from,to,ledger:{version:1,notes}}, fileId, historyId}` の順序付きcodec。
  fileIdはvolume64の16桁+file128の32桁の小文字hex。historyIdだけ空を許す。未知版/不正名/埋込NUL/台帳不一致を拒否。
- CMake、`tests/unit/rename_tests.c`、既存allocation_testsのrename_scenarioを結線。製品の既存操作からは未接続。
- `2baa0d1`で `pwsh -NoProfile -File ./eng/check.ps1` exit 0、前後clean、CTest2/2、1780/1910分岐=93.1937%、14 proofs。
  ソース検証後の変更は文書だけ。**文書保存後のHEADでは全体ゲートを再実行していない**。次のReady直前に最終SHAで正規ゲートを実行する。
- #55は7708802で全体ゲートexit0・前後clean、1638/1742分岐=94.0299%、14 proofs。部品フォーム4回/失敗0。
- ログ/probe原本はルート `out/design/2026-09-13/`。`save-as-final-gate.log`、`save-as-window-probe.c/.log`、
  `rename-core-final-gate.log`、`rename-core-coverage.log`。#53は`new-note-16a4353-final-gate.log`とfirst-save-各probe。

### 独立レビューの判断と残る限界

設計/調査/実装/検証は現在サナの単体が既定。必要な独立レビューだけ読み取り専用で使い、Sol/Lunaへの固定委任を再開しない。
/root/review_rename_designは設計だけをレビューして完了。reparse point拒否、履歴なし両側不在、FS範囲、公開前/公開後の区別をADRへ反映済み。
#56ソースの独立レビュー、Win32改名の実装/実測は未実施。

初版の改名はローカルNTFS・プロセス異常終了後の再開を対象とする。電源断/OSクラッシュの耐久性は未証明。
既存dataのロックを寿命中保持し同じdataの二重起動を拒否する設計。書けないdataの起動もこの版では拒否する予定で、まだ製品へ実装していない。
リンク/親junctionは参照先を動かさないよう明示拒否。ロック取得が全ファイルの書込成功を保証するわけではなく、個別失敗は保つ。
#55では元ファイル/履歴へ先に書かず、閲覧の原文を複製。#56改名と同じ意味にしない。ADR0012のカテゴリ間移動の旧履歴契約を書き換えない。

Computer Useは直近15:45 JSTにnative pipe unavailable / os error 2。通常起動/自プロセスの部品測定と接続復旧を混同しない。
既存ユーザーアプリを閉じない。16:09のhide確認は#49のexeだけ。最新の物理HHKB/日本語IME候補/最小窓/高DPIの目視は未確認。
一部部品exeのASan interceptor警告は記録済みで、警告なしのASan成功とは扱わない。正規単体ゲートは別に成功している。

Issue: #55/#56。ARC-001/004/007〜011、C-002〜008/012/017、QLT-009/012/013、GIT-001〜004。
台帳スキーマ版1、依存/ゲート変更なし。復旧記録版1はcore codecのみ。Waivers: none。


## 17:49 JSTの再開点（#55）

- #53 / PR #54は最終HEAD16a435320006844c9cd75d9753a428e5b1bcab64の全体チェックがexit 0（前後clean）。CTest2/2、1628/1730分岐=94.1040%、14 proofs成功。CI34748076131も成功しmain35d86af3f45c8c4993786e3ede17a96f2dace467へ統合した。
- #55 / `feat/55-save-as` / `out/worktrees/55-save-as` / ADR0021で別名保存を実装。共通の作成と名前入力を拡張し、元のwrite/archiveを先に呼ばない。閲覧は原文、編集は未保存入力を使う。
- GUI「別名で保存」、Ctrl+Shift+S、:saveasが同じ操作。主窓のHACCELでShiftを区別し、IMEと名前入力に漏らさない。閲覧中も作成後の台帳失敗は保存で再試行、:qで拒否する。
- ビルド/clang-tidy、CTest2/2、確保失敗を含む1638/1742分岐=94.0299%とnegative proofが成功。主窓の独立Windows部品測定はフォーム4回・失敗0で、取消/Undo/Ex/閲覧原文/部分失敗を確認。詳細は[別名保存の確認](../quality/2026-09-13-save-as-checks.md)。
- 最終ゲート/CI/統合の最新結果は#55のPRへ。実機の物理キー・IME候補・目視は未確認。既存のユーザーアプリは操作/終了していない。スキーマ・依存・ゲート変更なし。Waivers: none。
- 次は改名。未保存なら保存成功後にmd・索引・パンくず・履歴を追随し、履歴失敗を成功にしない。永続的な復旧情報と再起動後の再開をADRで決めてから実装。その後、現在ノートの/・?・n・NとGUI検索を続ける。

## 17:30 JSTの再開点（#53）

- #51 / PR #52を最終HEAD d07fcf620f12c5d9c027ddc71468099b42749c23の全体チェック（前後clean）とCI34746043689成功後に統合。mainは7ff99c29955ec1825ac246af012042387268a662。
- #53 / ADR0020 / `feat/53-new-note` / `out/worktrees/53-new-note`で無題と初回保存を実装。GUI「新しいノート」、Ctrl+N、:enew、名前/カテゴリ入力、:w 名前、同名保護、取消時の本文/Undo保持を共通操作へ接続した。
- 読み取り専用の独立レビューで初回保存のカテゴリカーソル、最大名の一時ファイル、台帳未同期の:qの3点を検出し修正。長い名前の実測で見つけたMAX_PATH制限もroot生成一か所で修正した。
- ビルド/clang-tidyとCTest 2/2、確保失敗を含む中核測定が成功。実ファイル・実アダプタの履歴・モーダル入力・主窓のEx/取消/Undoの独立部品測定も失敗0。ASan計装ライブラリを使う部品exeにはWindows interceptor警告が出るため、警告なしのASan確認とは扱わない。
- 詳細・実行物・測定限界は[初回保存の確認](../quality/2026-09-13-first-save-checks.md)。最終フルゲート/CI/統合の最新結果は#53のPR本文。実機の物理キー・IME候補窓・高DPI・目視は未確認。既存ユーザーアプリは操作・終了していない。
- 台帳の永続化スキーマは版1を維持。依存・ゲート変更とwaiverはなし。統合後は別名保存→改名→ノート内検索を継続する。別名保存は元の未保存変更を先に保存しない。改名は履歴を追随させ、途中失敗と再起動復旧をADRで先に決める。?のヘルプ割当は後方検索の実装と同時に移す。

## 16:42 JSTの再開点（#51）

- #49 / PR #50は最終HEAD7288c25でフルゲート成功（前後clean）、CI34744075792成功後、main b3a6b49へ統合。
- hideは再起動した#49のexeに対し16:09 JST「大丈夫そう」と報告。SHA-256は
  `2CD8C3AE9CC5DE8D3588DC5499C0D8965E6FDB48A4756D80DEFC9096B7E5632C`。
  ヘルプ・閲覧からの入力・編集再選択の確認を案内した後の総合報告で、各条件の個別成功やDPI・IME名は未記録。
- 現作業は#51 / `feat/51-ctrl-hjkl` / `out/worktrees/51-caret`。ADR0019を先に追加し、
  編集本文だけのCtrl+h/j/k/lをTranslateAcceleratorWとRichEdit TOMへ結線。キャレット状態はRichEditだけが持つ。
- ビルド（clang-tidy含む）とCTest 2/2が成功。独立した非表示Win32部品の113比較で矢印と同じ移動・本文不変・Undo保持を確認。
  測定の範囲・初期測定手順の修正・実機の未確認は[確認記録](../quality/2026-09-13-caret-checks.md)を読む。
  正式フルゲート・CI・統合の最新状態は#51のPR。製品スキーマと依存は変更なし。Waivers: none。
- hideは「止まらずどんどん進めて」と指示。#51の検証・統合後、新規／初回保存→別名保存・改名→ノート内検索を
  採用済み計画に従い焦点の分かれたIssueとADRで続ける。実装は現在のサナが単体で行う。
- Computer Useの直近確認は15:45 JSTのnative pipe unavailable / os error 2。既存のユーザー操作中アプリを閉じない。
  通常の起動と独立したWin32部品測定は接続の復旧や物理キー・IMEの目視確認の証拠ではない。

## 15:50 JSTの再開点（#49）

- #37 / PR #44は最終HEAD f3a0800のフルゲートとCI成功後main 9a60680へ統合。
- #45 / PR #46はmain取り込み後の7142a6bでフルゲート成功（前後clean）、CI成功後main 4628302へ統合。
  CIでPR本文の必須フィールド不足を検出したため本文を修正し、ゲートを変更せず成功を確認した。
- #47 / PR #48はmain取り込み後のffead20でフルゲート成功（前後clean）、CI成功後main 23b3ad1へ統合。
- #49 / ADR0018を `out/worktrees/49-gui-entry` / `feat/49-japanese-operations` で実装。
  日本語の保存・操作・ヘルプ、F1/:h、折畳みキー説明、全候補への移動、閲覧本文の:とi、編集再選択で本文を保持する経路を追加。
  閲覧も共通操作IDへ集約。本文IMEのcompositionはnote_paneが記録し、変換中のキーを親が奪わない。
- 製品＋単体ビルド（clang-tidy含む）、CTest 2/2、Conformance 0違反、git diff --checkが成功。
  初回CTestのsmoke未ビルドは対象をビルドして解消。最終Ready用フルゲート・CIはPRの最終HEADと結果を参照する。
- Computer Useは15:45 JSTにも @oai/sky.list_apps() がnative pipe unavailable / os error 2。
  [実機手順](../quality/2026-09-13-gui-entry-checks.md)を用意。表示・クリック・IME・Undo・高DPIは実機未確認で、以前のexeのhide確認を流用しない。
- hideのHHKBは左Altを英字直接入力、Alt自体をマウスボタン3との組合せにしている。
  編集時の移動はAlt+h/j/k/l案を撤回しCtrl+h/j/k/lを提案した段階。承認・実装済みと扱わない。
- 新規・別名保存・改名・ノート内検索は採用済み計画の次の単位。初回起動のカテゴリ作成も別課題。

## ここまでの経過

- [前回の引き継ぎ](https://github.com/hideyukiMORI/nene-folio/blob/6dd9dd2028fea6dd315c70bc4be8503ff18e943b/docs/handoffs/2026-09-12.md)と[本日の日報](../reports/2026-09-13.md)を読む。
- #37 / Draft PR #44は `out/worktrees/37-commands`。INDEXの `?`、共通パレット下部のキーバインド、
  [操作表](https://github.com/hideyukiMORI/nene-folio/blob/6dd9dd2028fea6dd315c70bc4be8503ff18e943b/docs/KEY_BINDINGS.md)を追加した。保存・終了の意味、5操作の登録表、本文・入力欄の文字入力は維持する。
- #37の`f3a0800`は14:25 JSTに正規フルゲートがexit 0（実行前後clean）。PR #44はまだDraftで、Ready移行とCIは残る。
- 13:50 JSTの再開作業で、入力面の変換中Ctrl+Pが入力を空にするガード漏れを修正した。
  狭いビルド・CTest 2/2・規約検査に加え、その修正を含むHEADで上記フルゲートも成功した。
  入力面とRichEdit本文のIME試験を[実機確認項目](https://github.com/hideyukiMORI/nene-folio/blob/6dd9dd2028fea6dd315c70bc4be8503ff18e943b/docs/quality/2026-09-12-command-checks.md)で分けた。
  #37の修正コミットは`f3a0800`。#45にも同じ修正を取り込み、独立した実装経路は増やしていない。
- hideはArtifact版4をおおむね了承し、パンくずをPowerline風にする追加要望を出した。#45 / ADR 0017として実装。
  右端の操作領域を確保し、カテゴリ名とノート名の省略順を勝手に逆転させない。
- #45は `out/worktrees/45-powerline` / `feat/45-powerline-breadcrumb`。#37のHEAD `f3a0800`を取り込んだ。
  #37を統合してから#45のPR基準をmainへ変更し、その時点のHEADを確認・検証する。両者を混同しない。
  14:08 JSTの取り込み後、固定toolchainの整形検査・ビルド・CTest 2/2・規約検査が成功。
  #45の最新修正後のフルゲートは未実施。#37の成功を#45の成功として流用しない。
- 14:18〜14:22 JST、#45の`d01a623`のexeをhideが手動確認。入力保持、終了拒否・保存、
  パレット変換中Ctrl+P・Enter、長いパンくずを狭めた表示の4項目に問題の報告はなかった。
  [コマンド](https://github.com/hideyukiMORI/nene-folio/blob/6dd9dd2028fea6dd315c70bc4be8503ff18e943b/docs/quality/2026-09-12-command-checks.md)と[パンくず](https://github.com/hideyukiMORI/nene-folio/blob/6dd9dd2028fea6dd315c70bc4be8503ff18e943b/docs/quality/2026-09-13-powerline-checks.md)に
  exeのSHA-256と報告内容を記録。DPI・窓寸法・IME名は未記録で、他のGUI条件は未確認。
- Computer Useはプラグインのオフ→オンとJavaScript再初期化後もnative pipe unavailable / os error 2。
  14:05 JSTに昨日の実行ログを確認。ChromeのArtifact確認は拡張接続のBrowser Useで成功し、
  Windows操作の`sky.list_apps()`は9/12 23:27・23:28 JSTにも同じエラーだった。
  他セッションへの影響が未確認のためCodex全体の再起動は保留。手動MCP追加を復旧の前提にしない。
  通常のプロセス起動は可能で、上記の手動確認はその経路を使った。Computer Use自体の復旧証拠にはしない。
  #45の最終ゲート・両PRのCIと統合はまだ残る。
- デザインリナCLIの現在のIDは `claude agents --json --all` とルートの `out/design/2026-09-12/design-handoff.md` で照合（必要時 `--model opus`）。
  原本はルート `out/design/2026-09-12/`。同じ架空ノートのArtifact更新はhideの承認済み。

スキーマ変更なし。Waivers: none。

## 14:50 JST追補 — #47の採用方針を優先する

- [日本語GUIの後続計画](../plans/2026-09-13-gui-first-operations.md)を読む。hideが`?`後方検索、GUI「ヘルプ」とF1、
  日本語のボタン・メニューを主入口、ショートカットを補助にする案を明示採用した。先頭の`?`ヘルプは既存Draftの実装記録。
- 作業木は`out/worktrees/47-gui-plan`、枝`docs/47-gui-first-plan`。main`c04f898`から分離した計画更新で、製品コードは触っていない。
- 全ノートの絞り込みは#40のまま。INDEXの`/`を全ノート検索へ割り当てた旧計画を変え、INDEXと閲覧本文の`/`・`?`は
  表示中ノートの方向検索へ揃える。全ノート検索語とノート内検索語を強制同期しない。`n`は直前の方向、`N`は逆。
- GUIヘルプ・F1・`:h`を先に用意し、旧`?`ヘルプの削除はノート内検索が動く実装単位で同時に行う。
  無題・別名保存・改名と履歴追随は新ADRと焦点を分けた実装Issueが必要。受理済みADRを書き換えない。
- デザインリナのCLIは`d2bc8c9f-b536-49a5-af88-98ae9e2a6ac6`。`/design`読込成功。
  原本はルート`out/design/2026-09-13/gui-parity-proposal.md`、親からの採否は`parent-feedback-01.md`。
  「＋」「…」だけの入口、強制共有する検索語、INDEXの`?`無反応、`n`常に次、改名履歴の失敗を放置する初稿は不採用。
  同じArtifactの次期案を作成中。Chrome目視して採否を返す作業が残る。
- PR #44の最終ゲートとPR #46の手動確認の記述は更新済み。Ready・CI・統合は未完了。
  #45の最終フルゲートも未実施。新設計の検討と、既存2PRの検証結果を混同しない。

## 15:13 JST追補 — 設計相談の採用結果

- 計画はDraft PR #48。上記の「作成中・Chrome目視が残る」は14:50時点の記録で、版8/9の9画と版11のヘルプ3画を目視した。
  [採用した配置と確認範囲](../design/2026-09-13-gui-first-operations.md)が設計の正本。Artifactの次期ページは最終11画。
- 通常・最小の操作行に日本語「新しいノート」「保存」「操作」「ヘルプ」を常設。ヘルプとCtrl+Pのキー説明は既定で折り畳み、
  「キー操作を表示」から同じ面で全文を読む。最小の初期表示は候補5行を維持する。背後のパンくずの重なりも版11で解消確認。
- 原本と採否はルート`out/design/2026-09-13/gui-parity-proposal.md`、`parent-feedback-01.md`〜`03.md`、
  `parent-feedback-final.md`。ClaudeCodeの`d2bc8c9f-b536-49a5-af88-98ae9e2a6ac6`で/designを使い、相談した。
- 次はGUIの実装Issueを選び、ADR0016の入口・表示契約を後継ADRで拡張する。日本語GUI・F1・`:h`・閲覧本文の`:`と編集開始から進める。
  ノート内検索が動くまで現行`?`ヘルプを先に消さず、検索と同じ実装単位で置き換える。
- 計画PRは差分・Git規約・文書リンクを確認したDraft。正規フルゲートとReadyは未実施。Windows製品への新仕様の反映はまだ。
