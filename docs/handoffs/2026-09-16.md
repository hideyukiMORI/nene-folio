# 引き継ぎ — 2026-09-16

## 再開点

- ルート `C:/Users/info/WORKS/NeNeFolio` はmain。#55 / PR #57は`f35870d`で統合済み（ADR 0021と2026-09-16の補正）。
- **#56 / PR #59（名前変更）**: `out/worktrees/56-rename` / `feat/56-rename-note`。最終状態・CI・統合の結果はPR #59の本文を正とする。
  HEAD `163a9e0`でフルゲートexit 0（前後clean）。書き換え前のHEAD `8188e25`はローカルタグ`wip/56-before-rewrite`（木は同一）。
- **#58（現在ノートの検索）**: Issueのみ。ADR 0023草案は設計リナの手元にあり、枝を切るときに`docs/adr/0023-*.md`へ置いて受理する。
  実装は#59統合後に`out/worktrees/58-search`で行う。INDEXの`?`ヘルプの削除はこの単位で同時に行う（ADR 0016・操作表・共通ヘルプ・テスト）。

## 次の操作

1. PR #59: 読み取り専用レビューの所見が無ければReady → CI → squash merge → ルートmainをff-only同期。所見があれば同じPRで直し、最終SHAで再ゲート。
2. #58: ADR 0023を枝に置き、core `note_search`（純関数）→ application（語と方向）→ UI（検索欄・`/` `?` `n` `N`・Ctrl+F・GUI）→ `?`ヘルプの移行の順。
3. その後は全ノート検索#40（Ctrl+Shift+F）、行番号#39、置換#41、設定/テーマ/言語#38、同梱フォント#42。カテゴリ作成・実機確認・配布は後続。
4. hideへの確認事項: 実機の`data/`が同期フォルダ（OneDrive / Dropbox）の中か。中ならADR 0022決定4のreparse point拒否で改名が常に拒否される。

## 役割と運用

- 設計・判断・ADR・レビュー依頼・統合は設計リナ（Fable）。実装・調査・テスト・読み取り専用レビューはOpusの実装リナを背景で立ち上げて委任し、報告で詰める。
- CIは`ready_for_review` / `synchronize` / `edited` / `reopened`でだけ走る。GitHubがイベントを落としたら`gh pr ready --undo`→`gh pr ready`で再発火する。
- mainを作業枝へ取り込むmergeの件名は`chore(sync): … (#N)`（GIT-003）。既定の`Merge branch`件名は`validate-git`で落ちる。
- 錠の導入で、同じ`data/`を指す2つ目のNeNe Folioは起動しない。hideが起動中のexeと同じ`data/`を新しいexeで開くと起動を断られる。

## 未確認・未実装

- 実機: 物理F2・HHKB・日本語IME候補窓・「操作」メニューのマウス操作・高DPI・両テーマ・最小寸法の入力面・復旧待ちパンくずの目視。
- 未実装: カテゴリを変える改名、壊れた`.rename.json`を画面から直す経路、`.rename.json`版2、履歴の閲覧/復元UI。
- 未証明: 電源断・OSクラッシュ時の名前空間更新の耐久性。ネットワークドライブ・ReFS・同期フォルダ上の`data/`は対象外。

Issue: #55 / #56 / #58。台帳スキーマ版1維持、`data/.rename.json`版1と`data/.nenefolio.lock`を新設。依存・ゲート変更なし。Waivers: none。
