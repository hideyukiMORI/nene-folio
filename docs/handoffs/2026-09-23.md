# 引き継ぎ — 2026-09-23

## 再開点 2 — #91 統合後（この節が最新）

この期間の成果と検証は[日報](../reports/2026-09-23.md)の「再開後 2」。

### どこから再開するか

- ルート `C:/Users/info/WORKS/NeNeFolio` は clean な main **`2a13318`**。
  **#85（PR #87 → `c7497fb`）・#86（PR #90 → `9c1a35e`）・#76（PR #89 → `4dcd3c7`）・
  #88（PR #92 → `262b53f`）・#93（PR #94 → `58122eb`）・#91（PR #95 → `2a13318`）**まで統合済みで、
  Issue #85 / #86 / #76 / #88 / #93 / #91 は閉じた。
- **親の #38 は #76 の統合で閉じた。** 設定・テーマ・言語の 4 単位（#73 / #74 / #75 / #76）はすべて終わっている。
- **次の単位は #82**（名前入力面と失敗の箱をテーマに追随させる・ADR 0031 の決定 10 の積み残し）。
  そのあと **#70**（`regex_match` × `regex_match_limit` のメモリと出力上限の釣り合い）→ **#42**（Google Fonts の同梱）。
- **実機の目視は hide が 2026-09-23 に 1 度行った**（main `2448987`。「おおむねいい」・見つかった 3 点が #85 / #86 / #88）。
  [統合チェックリスト](../quality/2026-09-22-visual-checklist.md)は **85 項目（39-1〜93-1）**で、
  **結果が入っているのは 40-3 / 40-5 の 2 行だけ**である。
  実機で見るときは main **`2a13318`** を `--target nenefolio` でビルドし直す（チェックリスト先頭の SHA は `f4e7bcf` のまま）。

### 作業木

| 作業木 | 枝 | 状態 |
| --- | --- | --- |
| `out/worktrees/85-ex-caret` | `fix/85-ex-caret` | **統合済み**（`c7497fb`） |
| `out/worktrees/86-filter-visible` | `fix/86-filter-visible` | **統合済み**（`9c1a35e`） |
| `out/worktrees/76-language` | `feat/76-language` | **統合済み**（`4dcd3c7`） |
| `out/worktrees/88-icons` | `feat/88-vector-icons` | **統合済み**（`262b53f`） |
| `out/worktrees/93-filter-close` | `fix/93-filter-close-icon` | **統合済み**（`58122eb`） |
| `out/worktrees/91-ncdestroy` | `fix/91-ncdestroy` | **統合済み**（`2a13318`） |
| `out/worktrees/96-report` | `docs/96-report-2026-09-23b` | この記録の作業木 |

**これより前の作業木（`37-commands` から `83-report` まで）もすべて統合済みの枝である**（`git worktree list` で 28 本）。
残っていても害はない（削除は任意）。`git stash` は使わない。

### この期間に決まったこと（契約の追補）

- **主窓の子を足すときは作った順ではなく z 順で考える**（ADR 0024 の 2026-09-23 の補正）。
  あとから作った子は兄弟の**背面**に入る。`WS_CLIPSIBLINGS` は自分より**前**にいる兄弟しか除かないので、
  前に出したい子には `SetWindowPos(HWND_TOP)` を当てる。守っているのは「ドロワー側の `WS_CLIPSIBLINGS`」と
  「欄が前にいること」の**両方**である。
- **画素を読む probe は画面の上に窓を出す**（`WS_EX_TOOLWINDOW` ＋ `HWND_TOPMOST` ＋ `SWP_NOACTIVATE`）。
  画面外に置くと `BeginPaint` の可視領域が空になり、全条件で同じ値が出る。
- **`EM_SETSEL(-1, -1)` は「末尾へ飛ぶ」ではない**（#85）。選択を解くだけで、解いた先は `WM_SETTEXT` が置いた 0 である。
  末尾へ置くなら `GetWindowTextLengthW` の長さを渡す。
- **文言の表は言語ごとの列を持ち、列の網羅は CNF-011 が守る**（ADR 0032）。値を足すときは 3 列とも書く。
  `UI_TEXT_EMPTY` だけが 3 列とも空でよい。**日本語の字面を変えるのは別の判断**である。
- **設定は先に書いてから採用する**（テーマと言語で `adopt_settings_change` の 1 本）。版は効果のあるキーごとに上げる
  （版 3 = `number` + `theme` + `language`）。
- **単独で描く字形は 1 つも残さない**（ADR 0033）。閉じる・設定・折畳・選択の印は 24 の viewBox の点の表で、
  描く経路は `icon_paint_fill` の 1 本だけである（#93 で絞り込み欄の × も寄せた）。
- **`self->handle` を消すのは `window_finalized`（`WM_NCDESTROY`）の 1 か所**（ADR 0014 の補正）。
  既定処理には引数の HWND を渡す。

### 次の操作

1. **#82** に着手する。ADR 0031 の決定 10 が「名前入力面と失敗の箱は追随させない（別 Issue）」と書いた所を実装する。
   `failure_box_show` は #76 で 13 か所（ドロワー 5・主窓 8）が言語を渡す形になっている。
   **Issue ごとに新しい実装 agent を立てる。**
2. hide の実機目視（85 項目）。見つかったことは新しい Issue へ。結果が出たら各確認記録の「限界」へ書き戻す。
3. hide の目視で配色の値を動かすなら `src/core/rtf_palette.c` と `src/ui/win32/folio_palette.c` の 2 ファイルだけ、
   × の濃さを変えるなら `icon_paint.c` の `close_points[]`（腕 1.98 単位。1.6 単位で #88 以前と同じ濃さ）だけを触る。

### hide の判断待ち

- **実機の目視の続き（85 項目のうち 83 行）。** とくに:
  - **39〜75 番台**（行番号・絞り込み・置換・Ctrl+S・最小寸法のパレット・テーマと設定画面）。
  - **76-1〜76-14**（3 言語の見え方・豆腐や字形の混在・**简体中文の訳**）。
    🔴 **简体中文は母語話者の確認を得ていない。**
  - **88-1〜88-9**（**96 DPI の × の濃さ**が 1.53 倍になること・24px の歯車が歯車として読めるか・
    **設定画面の橙の印が紫の選択行の上で読めるか**・折畳の板・カーソルの角が 18px 左へ動くこと）。
  - **93-1**（20px の × が頭の 24px の × と並べて違和が無いか）。
- **配色の値**（ダークの地 `#2C001E`・選択 `#772953`・札 `#E95420` ほか）は**候補**のままである。
  画面案は <https://claude.ai/artifact/SHmNscVHWZjBmL3XLsqSXZ>、アイコンの画面案は
  <https://claude.ai/artifact/UiUEYVVnhqUpYNCXxV8Ry3>（**流儀 C を採用済み**）。
- **Settings アプリで OS のライト／ダークを切り替えたときに追従するか**（75-10 / 75-11）。実機でしか測れない。

### 運用（この期間の追加）

- **姉妹の PR は片方を merge したら、もう片方は衝突が無くても sync の push が要る。**
  保護規則が最新の base での check を求めるため。`chore(sync): … (#N)` の merge を取り込んで push し直す。
- **`gh pr edit` は `edited` で先行の run を cancel する。** 本文を直すなら push の前に済ませる。
- **ADR の草案の段階で検査器を走らせる**（#88 は批評で CNF-002 に当たる書き方を 3 つ見つけ、受理の前に直した）。
- それ以外（Issue ごとの agent・未定義の規則 ID と CNF-006・`ready_for_review` で CI が起動しないこと・
  使用制限で止まったときの再開・PR 本文の GIT-004 の字面）は 1 つ前の再開点のままでよい。

### 未確認・未実装

- **未確認（実機）**: 上の 83 行。加えて `GdiplusStartup` の失敗で窓を作らない経路（**偽の失敗を注入できないので測っていない**）、
  production の子を全部持つ窓での `WM_DESTROY`〜`WM_NCDESTROY` 間のメッセージ、高 DPI と IME での絞り込みの欄の枠と ×。
- **未実装**: **#82**（名前入力面と失敗の箱のテーマ追随）・**#70**（一致の入れ物のメモリ）・**#42**（Google Fonts の同梱）。
  ほかに `:s` の行範囲・確認付き置換・全ノート一括置換、カテゴリを変える改名、壊れた `.rename.json` を画面から直す経路、
  履歴の閲覧／復元 UI、検索語・絞り込み語・置換語の永続化、外部で変わった md の検知。
- **未証明**: `gdiplus_flat.h` の 18 本の署名は SDK のヘッダを**読んで写したもの**で、コンパイラは一致を確かめない
  （型や並びを間違えてもコンパイルは通る。根拠は probe が呼んで `Ok` が返ることだけ）。
  `ui/win32` はゲート内の単体を持たない層なので、点の表・設定画面の行・z 順・`WM_NCDESTROY` の順を守る自動テストは
  **ゲートの中に無い**。そのほかは 1 つ前の再開点のまま。

Issue: #85 / #86 / #76 / #88 / #93 / #91（すべて閉じた）・親 #38（閉じた）・#96（この記録）。
スキーマは **`data/settings.json` が版 3**（`{"version":3,"number":…,"theme":…,"language":"ja"|"en"|"zh-Hans"}`。
版 1・版 2 は読めて既定値を埋める・書くのは常に版 3）。台帳版 1・`data/.rename.json` 版 1・`data/.nenefolio.lock` は据え置き。
ゲートの変更は **CNF-011 の新設（active）**と `textCatalog` の追加、`platformLibraries.ui_win32` の `gdiplus`
（**閾値・除外・重大度は不変**）。Waivers: none。

---

## 再開点 — #75 統合後（1 つ前）

本日の成果と検証は[日報](../reports/2026-09-23.md)。

### どこから再開するか

- ルート `C:/Users/info/WORKS/NeNeFolio` は clean な main **`72fb136`**。
  **#73（PR #77 → `53acb20`）・#67（PR #78 → `ba0e538`）・#69（PR #79 → `d238c01`）・#74（PR #80 → `f881f16`）・
  #75（PR #81 → `72fb136`）**まで統合済みで、Issue #73 / #67 / #69 / #74 / #75 は閉じた。
- **親の #38 は開いたまま。** 残るのは**単位 C（#76：言語・版 3）**の 1 つで、それが終わったら #38 を閉じる。
- **次の単位は #76。** 先に **ADR 0032 の草案**を書く（ADR 0029 の決定 1 が単位 C を予告しているが、
  表を 2 次元にする形・版 3 の移行・ヘルプ行の欄分け・言語ごとのフォントは ADR で決めてから実装する）。
  ADR 0030 の補正で済ませるか新しい ADR を立てるかは Issue #76 が「ADR 0030 の補正か新 ADR」と書いてある。
  **新しい規則 ID を名指しするなら、それを定義する単位の同じコミットで planned として置く**（CNF-006 で落ちる）。
- そのあとの順: **#82**（名前入力面と失敗の箱のテーマ追随・ADR 0031 の決定 10 の積み残し）→
  **#70**（`regex_match` × `regex_match_limit` のメモリと出力上限の釣り合い）→ **#42**（Google Fonts の同梱）。
  #82 と #70 は #76 と併走できる小さい単位。
- **実機の目視は 1 項目も行っていない。** [統合チェックリスト](../quality/2026-09-22-visual-checklist.md)は
  **55 項目（39-1〜75-20）**で、結果は全行が空欄である。ビルドの手順と SHA はチェックリストの先頭にある
  （SHA は `f4e7bcf` のまま。実機で見るときは main `72fb136` を `--target nenefolio` でビルドし直す）。

### 作業木

| 作業木 | 枝 | 状態 |
| --- | --- | --- |
| `out/worktrees/73-ports` | `refactor/73-folio-ports` | **統合済み**（`53acb20`） |
| `out/worktrees/67-store` | `feat/67-input-surface-store` | **統合済み**（`ba0e538`） |
| `out/worktrees/69-palette` | `fix/69-palette-min-size` | **統合済み**（`d238c01`） |
| `out/worktrees/74-ui-text` | `feat/74-ui-text` | **統合済み**（`f881f16`） |
| `out/worktrees/75-theme` | `feat/75-theme-settings` | **統合済み**（`72fb136`） |
| `out/worktrees/83-report` | `docs/83-report-2026-09-23` | この記録の作業木 |

これより前の作業木（`37-commands` / `39-number` / `40-filter` / `41-replace` / `45-powerline` / `47-gui-plan` /
`49-gui-entry` / `51-caret` / `53-new-note` / `55-save-as` / `56-rename` / `58-search` / `61-reparse` /
`65-failure-lines` / `71-report`）もすべて統合済みの枝である。**残っていても害はない（削除は任意）。**
`git stash` は使わない。

### この期間に決まったこと（契約の追補）

- **入力面の Ctrl+S は保存して欄を開いたまま残す**（ADR 0016 の補正 2）。対象は検索欄・Ex・パレット・置換の 2 欄・
  ドロワーの絞り込み欄の 5 つで、語・選択・キャレット・フォーカス・入力面の種類は変わらない。
  絞り込み欄も同じ 1 本（`store_from_surface`）へ寄せたので、**同時に開いている入力面を閉じなくなった**。
- **最小寸法では説明を一度に全部見せなくてよい**（ADR 0018 の 2026-09-22 の補正）。候補に下限 3 行を残し、
  説明は PgUp / PgDn で辿る。箱の高さの式は**面ごとの行数を引数に取る 1 本**（`command_box_height(rows, help_rows)`）。
- **文言の正本は core の `ui_text` の表 1 か所**（ADR 0030）。言語は引数、句は置換子、
  **語や数を連結して句を作らない**。置き場所は CNF-010 と C-018 が守る。
  **`enum ui_text` に値を足すときは表へ 1 行**（CNF-009 の `lineTables`）。
- **設定は先に書いてから採用する**（ADR 0031・ADR 0025 の決定 6 の踏襲）。
  `folio_state_set_theme` は新しい設定と新しい閲覧文書を両方先に作り、書けたら差し替える。どちらの失敗でもファイルも状態も変えない。
- **版は効果のあるキーごとに上げる**（ADR 0029 の決定 2）。版 2 = `number` + `theme`、版 3 = + `language`。
  移行は `folio_settings.c` の中の static な純関数で、版ごとの解析関数に割る。
- **`EM_EXSETSEL` は `EM_SCROLLCARET` 無しでも選択へ寄る**ので、空の選択は戻さない（ADR 0031 の補正 1）。
- **設定の失敗は箱ではなく欄の中の 1 行**（ADR 0031 の補正 8）。`:set number` の失敗も同じ 1 行になった。

### 次の操作

1. #76 に着手する。ADR 0032（または ADR 0030 の補正）の草案 → 現行コードに照らした読み取り専用の批評 → 受理 → 層ごとに実装。
   **Issue ごとに新しい実装 agent を立てる。**
2. hide の実機目視（[統合チェックリスト](../quality/2026-09-22-visual-checklist.md)・55 項目）。
   見つかったことは新しい Issue へ。結果が出たら各単位の確認記録の「限界」へ書き戻す。
3. hide の目視の結果で配色の値を動かすなら、`src/core/rtf_palette.c` と `src/ui/win32/folio_palette.c` の 2 ファイルだけを触る。

### hide の判断待ち

- **実機の目視（55 項目）。** hide の判断で「#41 統合後にまとめて」となっており、いま 75-20 まで積んだ状態で待っている。
  特に見てほしいもの: 75-1〜75-5（Ubuntu 風 2 組の見え方・歯車と小円の大きさと縦位置・窓の縁の色）、
  75-17（S1 の回帰: パレットから開いた設定画面に行が出るか）、75-19（設定画面の上の空き帯が不自然でないか）、
  75-20（読み取り専用の `data/` で失敗が欄の 1 行になるか）。
- **配色の値**（ダークの地 `#2C001E`・選択 `#772953`・札 `#E95420` ほか）は**候補**である。
  画面案は <https://claude.ai/artifact/SHmNscVHWZjBmL3XLsqSXZ>。目視で動かしてよい。
- **Settings アプリで OS のライト／ダークを切り替えたときに追従するか**（75-10 / 75-11）。
  `WM_SETTINGCHANGE` の件数・順序・遅れは**実機でしか測れない**（レジストリを書くだけでは 1 件も来ない）。

### 役割と運用（この期間の追加）

- 各 PR の流れは変わらない: 実測 probe → ADR 草案 → 現行コードに照らした読み取り専用の批評 → 受理してコミット →
  実装（層ごとにコミット）→ 独立レビュー → 止める所見を同じ PR で修正 → 最終 SHA でフルゲート → Ready → CI → squash merge。
- **1 つの実装 agent を複数の Issue にまたいで使わない**（文脈が尽きる）。Issue ごとに新しい agent を立て、
  前の単位の事実は ADR・確認記録・引き継ぎから渡す。
- **ADR が未定義の規則 ID を名指しすると CNF-006 で落ちる。** 新しい ID は定義する単位の同じコミットで
  `docs/QUALITY_GATES.md` / `docs/CODING_RULES.md` に **planned** で置き、強制マトリクスの行も同時に足す。
  `docs/quality/gate-proofs.md` の証明行は **active にするコミット**で足す（`document_checks` は active のときだけ要求する）。
- **CI が `ready_for_review` で起動しないことがある。** `gh pr ready --undo && gh pr ready` を試し、
  それでも起動しなければ空でない push（`synchronize`）で起こす。
- **使用制限で agent が止まっても、作業木の未コミットの編集から再開できる。** 止めるときは同じ agent へ
  「未コミットを事実の範囲でコミット → 狭い検査 → 最終 HEAD で 1 回だけゲート → push → どこまで済んだかをファイル名・関数名で報告」。
- PR 本文は GIT-004 の字面どおりのフィールド行（`目的:` / `使った正典経路:` / `規則 ID:` / 検証 / `Waivers:` / `残るリスク:` と `Closes #N`）。
  main を作業枝へ取り込む merge の件名は `chore(sync): … (#N)`。

### 未確認・未実装

- **未確認（実機）**: 上の 55 項目。加えて #75 の IME（設定画面で組成が始まらないこと）・
  起動中の DPI 変更とテーマ切り替えの組合せ・`DwmSetWindowAttribute` の縁の色の画素。
- **未実装**: **#76（言語・版 3）**、**#82（名前入力面と失敗の箱のテーマ追随）**、**#70（一致の入れ物のメモリ）**、
  **#42（Google Fonts の同梱）**。
  ほかに `:s` の行範囲・確認付き置換・全ノート一括置換、カテゴリを変える改名、壊れた `.rename.json` を画面から直す経路、
  履歴の閲覧／復元 UI、検索語・絞り込み語・置換語の永続化、外部で変わった md の検知。
- **未証明**: 電源断・OS クラッシュ時の名前空間更新の耐久性。ICU の版は OS が決める（測定は ICU 72.1 / Unicode 15.1）。
  ICU 内部の確保失敗は測れない（QLT-009 の「置いていない層」）。
  `recolor_window` の `CreateSolidBrush` の失敗は閉じた結果型で表していない（古いブラシが残る）。
  CNF-010 は字句検査なので、結合や `#define` による回避は見えない。

Issue: #73 / #67 / #69 / #74 / #75（すべて閉じた）・#83（この記録）。親 #38 は #76 で閉じる。
スキーマは **`data/settings.json` が版 2** になった（版 1 は読めて `theme` に `system` を埋める・書くのは常に版 2）。
台帳版 1・`data/.rename.json` 版 1・`data/.nenefolio.lock` は据え置き。
ゲートの変更は **CNF-010 / C-018 の新設（active）**と `lineTables` 4 行・`textCatalog`・`platformLibraries.ui_win32` の `imm32`
（**閾値・除外・重大度は不変**）。Waivers: none。
