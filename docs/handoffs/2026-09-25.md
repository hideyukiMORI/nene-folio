# 引き継ぎ — 2026-09-25

## 再開点

main は **`11c6da3`**（clean。#121 = PR #124 → `a2538ba`・#122 = PR #125 → `0463cbb`・#123 記録（前半）= PR #126 → `44cb483`・
#120 = PR #127 → `f523073`・#119 = PR #128 → `203cc49`・#118 = PR #129 → `49042a3`・#70 = PR #133 → `30377d2`・
#134 = PR #135 → `84d94f4`・#132 = PR #136 → `11c6da3`（sync `chore(sync): main を取り込む (#132)` 1 回）をすべて統合済み）。
この記録（終盤・#137）は Draft PR。#118〜#123・#70・#134・#132 はすべて閉じた。

**開いている Issue は #112 / #42 と、この記録の #137 だけである。**

- 次は **#42**（Google Fonts の同梱）。立て方は変わらず: probe / 現物調査は Sonnet の席で試す、実装は Opus・S 級・1 席 1 仕事。

**hide の判断待ち**: #112（Draft で skipped の必須 check を GitHub が CLEAN と見なす）、
[統合チェックリスト](../quality/2026-09-22-visual-checklist.md) の残り（82-9 と 100% / 150% の項目、简体中文の字面、配色の候補）。
パレットで「破棄」と打つと `:q!`（未保存変更を破棄して終了）が先頭に並ぶ件は **hide が現状で可と判断し、判断待ちから外れた**。
OS のライト／ダーク切替（75-21）・#119 の警告音と印（82-7）・#118 統合後の名前入力面の目視（82-2）は **hide の実機確認ですべて OK になった**。

**実機の後始末**（引き継いだ状態）:

- hide の実機確認 4 件はビルド `49042a3`（PID 39712）で行った。設計席が次に動かすときは main `11c6da3` でビルドし直す。
- `build\data` の hide の試験ノート（仕事/test11.md）等の後始末は前回の引き継ぎから未確認のまま。

**この日に確立した道具**: 設計席が実機を自分で動かす scratchpad の `post.ps1`（`PostMessage` で `WM_CHAR` / `WM_KEYDOWN`）と
`capwin.ps1`（`PrintWindow` で PNG）。`SetForegroundWindow` + `SendKeys` と全画面 `CopyFromScreen` は失敗した手として
二度と使わない。`PrintWindow` で撮った絵は子窓を描き直して写すので、描き直しの欠陥の確認には使えない（画面そのものを撮る）。
**`post.ps1` の「フォーカスが非表示なら主窓へ投げ直す」規則は欠陥を隠すので、フォーカスの検証には `-Raw` の写しを使う。**

## 運用の学び

1. 実機を設計席が動かすときは `post.ps1` / `capwin.ps1`（`PostMessage` と `PrintWindow`）。`SetForegroundWindow` / `SendKeys` は禁止。
   `post.ps1` の「フォーカスが非表示なら主窓へ投げ直す」規則は欠陥を隠すので、フォーカスの検証には `-Raw` の写しを使う。
2. 件名と PR の題は 100 文字以内（CI は枝の各コミットの件名も検査する）。
3. `merge-pr.ps1` は本文の欄が行頭に来ること・`Closes #N` を要求する。
4. Sonnet の席は今日 8 席（現物調査 5・probe 2・記録 1 ＋この記録）で差し戻し 0。Opus の実装席は 8 席
   （#121 #122 #120 #119 #118 #70 #134 #132）で差し戻し 0、最大 65 回・180K。

最新は[日報](../reports/2026-09-25.md)の「終盤」。
