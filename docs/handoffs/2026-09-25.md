# 引き継ぎ — 2026-09-25

## 再開点

main は **`80e940d`**（clean）。この記録（#123）は Draft PR で、まだ main へ統合していない。

**開いている Issue は #70 / #42 / #112 と、この日に増えた #118〜#123 である。**

**着手の順（hide の判断）: #121 #122 → #120 → #119 → #118 → #70 → #42。**

- **#121**（設定画面からライトにすると絞り込みの欄だけダークの地のまま。原因は主窓が `WS_CLIPCHILDREN` で
  親の無効化が子の EDIT に届かないこと）は **Draft PR #124 / HEAD `96531c1`**（道具呼び出し約 29 回・約 86K tokens）。
  **#122**（English で「操作 ▾」が「Act...」に省略。釦の幅が `action_button_rect` の固定表 `{104,52,52,70,160}`）も
  2026-09-25 のうちに Opus の実装席（S 級・1 席 1 仕事）を立てた。**見込みでは Draft PR を出している**
  （`out/agents/122-impl/report.md` を確認してから、統合は root の main で `tools/merge-pr.ps1 -Number N -Subject "…"`）。
- そのあと **#120**（保存できないノートから抜ける道が無い。`:e!` と保存失敗の文言に出口。hide の指摘。箱に選択肢は付けない）。
- そのあと **#119**（失敗の箱に警告音と三角の「!」の印を戻す。音は欲しい・印も欲しい。設計席の判断で三角＝以前の
  `MB_ICONWARNING` と同じ意味）。
- そのあと **#118**（名前入力面の保存先カテゴリのコンボを自前の部品にし、矢印・枠・帯・スクロールを palette で描く。
  hide の判断は A＝自前の部品。B 枠だけ／C 現状 は却下）。
- そのあと **#70**（`struct regex_match` × `regex_match_limit` のメモリと出力上限）→ **#42**（Google Fonts の同梱）。
  立て方は変わらず: probe / 現物調査は Sonnet の席で試す、実装は Opus・S 級・1 席 1 仕事。

**hide の判断待ち**: #112（Draft で skipped の必須 check を GitHub が CLEAN と見なす）、
[統合チェックリスト](../quality/2026-09-22-visual-checklist.md) の残り（82-9 と 150% / 100% の項目、
OS のライト／ダーク切替の項目、简体中文の字面）、配色の候補。

**実機の後始末**（引き継いだ状態）:

- `build\data` に hide の試験ノート 仕事/test11.md（本文「11」）が残っている。読み取り専用は外した。
- 設定は `theme: light` のまま（hide がダークへ戻す）。言語は ja。
- `NeNeFolio.exe`（PID 39712）は起動したまま。

**この日に確立した道具**: 設計席が実機を自分で動かす scratchpad の `post.ps1`（`PostMessage` で `WM_CHAR` / `WM_KEYDOWN`）と
`capwin.ps1`（`PrintWindow` で PNG）。`SetForegroundWindow` + `SendKeys` と全画面 `CopyFromScreen` は失敗した手として
二度と使わない。**`PrintWindow` で撮った絵は子窓を描き直して写すので、描き直しの欠陥の確認には使えない**（画面そのものを撮る）。
3 回目に使うなら `tools/` のスクリプトにする（ADR 0034 決定 2）。今日は 2 回目。

最新は[日報](../reports/2026-09-25.md)。
