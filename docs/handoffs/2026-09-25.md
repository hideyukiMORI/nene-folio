# 引き継ぎ — 2026-09-25

## 再開点

main は **`49042a3`**（clean。#121 = PR #124 → `a2538ba`・#122 = PR #125 → `0463cbb`・#123 記録（前半）= PR #126 → `44cb483`・
#120 = PR #127 → `f523073`・#119 = PR #128 → `203cc49`・#118 = PR #129 → `49042a3` をすべて統合済み）。
この記録（後半・#130）は Draft PR。#118〜#123 はすべて閉じた。

**開いている Issue は #112 / #70 / #42 と、この記録の #130 だけである。**

- 次は **#70**（`struct regex_match` × `regex_match_limit` のメモリと出力上限。**現物調査は Sonnet の席で開始済み**、
  メモは `out/agents/70-probe/memo.md` に来る見込み）→ **#42**（Google Fonts の同梱）。
  立て方は変わらず: probe / 現物調査は Sonnet の席で試す、実装は Opus・S 級・1 席 1 仕事。

**hide の判断待ち**: #112（Draft で skipped の必須 check を GitHub が CLEAN と見なす）、
パレットで「破棄」と打つと `:q!`（未保存変更を破棄して終了）が先頭に並び Enter で終了してしまう並び順、
[統合チェックリスト](../quality/2026-09-22-visual-checklist.md) の残り（82-9 と 100% / 150% の項目、
OS のライト／ダーク切替の実機確認 75-21、简体中文の字面（#119 / #120 で今日足した 3 文を含む）、配色の候補）、
#119 の警告音の実機確認、#118 統合後の名前入力面の目視（82-1 / 82-3 / 82-4。期待は「常時開いた一覧」に直したが結果欄は空欄に戻した）。

**実機の後始末**（引き継いだ状態）:

- `build\data` に hide の試験ノート 仕事/test11.md（本文「11」）が残っている。読み取り専用は外した。
- 設定は `theme: dark` に戻し、言語は `ja` に戻した（実機確認の前に hide が戻した状態）。
- `NeNeFolio.exe`（PID 39712）は起動したまま。

**この日に確立した道具**: 設計席が実機を自分で動かす scratchpad の `post.ps1`（`PostMessage` で `WM_CHAR` / `WM_KEYDOWN`）と
`capwin.ps1`（`PrintWindow` で PNG）。`SetForegroundWindow` + `SendKeys` と全画面 `CopyFromScreen` は失敗した手として
二度と使わない。**`PrintWindow` で撮った絵は子窓を描き直して写すので、描き直しの欠陥の確認には使えない**（画面そのものを撮る）。

最新は[日報](../reports/2026-09-25.md)の「後半」。
