# 実機目視の統合チェックリスト（#39 / #40 / #41）

2026-09-22。#39（原文の行番号）・#40（全ノートの絞り込み）・#41（正規表現置換）で
**値としては確かめたが、描いた絵を 1 度も見ていない**項目を 1 つの表にまとめた。
probe はすべて画面外の窓で値だけを測っている（各単位の確認記録を参照）。

> **この表の「結果」は空欄である。空欄は「まだ見ていない」という意味で、
> 見ていないものを「確認済み」と書かない（ADR 0001 と同じ規律）。**

出どころ:
[#39 の確認記録](2026-09-17-number-checks.md)の末尾 11 項目 /
[#40 の確認記録](2026-09-17-filter-checks.md)の「限界」 /
[#41 の確認記録](2026-09-22-replace-checks.md)の「実機の目視に残すもの」。

## 手順

1. **起動中の `NeNeFolio.exe` を先に閉じる。** 開いたままだとリンクが落ちる。
2. `main` が `f4e7bcf`（#41 統合後）で clean であることを確かめる。
3. ビルドする。**`nenefolio` を名指しする**（`nenefolio_window` / `folio_tests` だけでは
   exe が再リンクされず、古い exe で誤診する。2026-09-11 の引き継ぎ）。

   ```powershell
   . ./eng/toolchain.ps1
   cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Debug
   cmake --build build --target nenefolio
   ```

4. できた exe を起動する。

このチェックリストを書いた時点で、上のコマンドを main `f4e7bcf` で実行して
**exe ができることまでを確かめた**（起動はしていない）。

| 項目 | 値 |
| --- | --- |
| ビルドした SHA | `f4e7bcf`（`feat(editor): 現在のノートの編集本文を正規表現で置換する欄とEx :%s を足す (#41) (#68)`） |
| exe のパス | `C:\Users\info\WORKS\NeNeFolio\build\NeNeFolio.exe` |
| 大きさ | 2,572,288 バイト |
| ビルドの結果 | `cmake --build build --target nenefolio` が exit 0（`Linking C executable NeNeFolio.exe`） |
| 起動 | **していない**（hide が行う） |

## 表

### #39 原文の行番号（FR-021 / ADR 0025・ADR 0026）

| # | 操作 | 期待する見え方 | 結果 |
| --- | --- | --- | --- |
| 39-1 | 編集モードで `:set number` を打ち、**実際のマウスホイール**で本文を転がす | 番号が本文とずれずに追随する（`EN_VSCROLL` の来ない経路） | |
| 39-2 | 同じくホイールと ↑↓ で速くスクロールする | 番号の帯がちらつかない（帯だけを無効化して即時に描き直している） | |
| 39-3 | 編集中に IME で変換を始め、確定する | 変換のあいだは番号が動かず、確定後に正しく追随する | |
| 39-4 | 表示スケールを 120 / 144 / 168% に変える | 帯の幅と番号の位置が本文に合う。DPI を変えた直後の桁の大きさも合う | |
| 39-5 | OS のライト／ダークを切り替える | 番号の色（ライト `#6B727C` / ダーク `#7C838D`）が本文より薄く見える。ClearType でも潰れない | |
| 39-6 | 番号と本文を並べて見る | 等幅 11px の番号と本文 11pt Yu Gothic UI の釣り合いが取れ、上揃えで違和感がない | |
| 39-7 | 1000 行以上のノートを開く | 4 桁が空きに収まって窮屈に見えない（値としては収まっている） | |
| 39-8 | 窓を最小寸法 560×360（96 DPI）にして Ctrl+P → Tab | 一覧とキー説明が破綻しない。**操作パレットの箱に空の 1 行が余っていない** | |
| 39-9 | 本文を上端までスクロールする | 番号が上の操作行（パンくず・札・閉じる）へはみ出さない（帯のクリップ） | |
| 39-10 | 閲覧に切り替える／無題を新規作成して編集する | 閲覧では番号が**出ない**。無題の編集中は**出る** | |
| 39-11 | `data/settings.json` を手で壊して起動する | 起動時に理由の 1 行が 1 回だけ出て、そのセッションは設定を変えず**上書きもしない** | |

### #40 全ノートの絞り込み（FR-032 / ADR 0024）

| # | 操作 | 期待する見え方 | 結果 |
| --- | --- | --- | --- |
| 40-1 | ドロワーの頭の欄を空のまま見る | プレースホルダ「すべてのノートを検索」が薄い色で出て、帯に収まっている | |
| 40-2 | OS のライト／ダークと高 DPI（120 / 144 / 168%）で同じ欄を見る | 欄の寸法・色・字の大きさが帯に合う | |
| 40-3 | **物理キーボード**（HHKB）で Ctrl+Shift+F を押す | どの区画からでも欄へ移り、いまの語が全選択になる | hide 2026-09-23: 語が消える → #86 |
| 40-4 | **IME の候補窓**が出ている状態で Ctrl+Shift+F を押す | 変換中は欄へ移らず、入力が残る（確定してから効く） | |
| 40-5 | 語を打って索引が絞られた状態で、カテゴリ行をクリック／`h` / `l` を押す | **何も起きない**（失敗の箱を出さない無音の拒否） | hide 2026-09-23: 語が消える → #86 |

### #41 正規表現置換（FR-023 / ADR 0028）

| # | 操作 | 期待する見え方 | 結果 |
| --- | --- | --- | --- |
| 41-1 | 編集中に「操作」→「置換」で欄を開く | 状態の 1 行・2 つの欄の左右が揃い、「1 件」「すべて」が右端に並ぶ。押した所で反応する | |
| 41-2 | 両テーマで、壊れたパターン（`(` など）を打つ | 欄の色が地に馴染み、失敗の 1 行 +「位置 N」が切れずに収まる | |
| 41-3 | IME で変換中に Enter を押す | 変換の確定に食われて**置換が走らない**。確定後に 1 回だけ件数が数え直される | |
| 41-4 | 1MB 級のノートで欄に 1 文字ずつ打つ | 打鍵が詰まらない（値では 1 回の走査が 4〜85 ms） | |
| 41-5 | 表示スケールを 120 / 144 / 168% にして欄を開く | 2 つの欄とボタンの寸法・字の大きさが合う | |
| 41-6 | 窓を最小寸法 560×360（96 DPI）にして欄を開く | 置換の欄（Ex より 1 行ぶん高い）が本文を隠しすぎない | |
| 41-7 | Ctrl+P の一覧と「操作」メニューを開く | 「置換」が 1 つだけ出て、`:%s`（`SUBSTITUTE`）は出ない | |
| 41-8 | `:%s/前/後/` と `:%s/無い語/x/` を打つ | 前者は欄を開かずに当たって黙って閉じる。後者は Ex が閉じず「対象名 / 0 件」が残る | |
| 41-9 | 長いノートの途中まで読んでから「すべて」を押す | 読んでいた場所へ戻る（見た目に大きく跳ねない） | |
| 41-10 | 置換したあと Ctrl+Z を押す／Esc で本文へ戻ってから Ctrl+Z を押す | 前者は**欄の取り消し**になる。後者で本文が 1 回で置換前へ戻る | |

### #67 入力面の中の Ctrl+S（ADR 0016 の補正 2）

| # | 操作 | 期待する見え方 | 結果 |
| --- | --- | --- | --- |
| 67-1 | 編集中に Ex（`:`）・パレット・検索欄・置換の欄・ドロワーの絞り込み欄のそれぞれで**物理キーボード**の Ctrl+S | 保存され、**欄は開いたまま**で語・選択・キャレット・フォーカスが変わらない |  |
| 67-2 | 欄で IME 変換中に Ctrl+S | 変換が優先され、保存は走らない。確定後の Ctrl+S では保存される |  |
| 67-3 | 置換の欄で「すべて」を当てた直後に Ctrl+S | 1 度の操作で保存でき、欄も件数もそのまま |  |
| 67-4 | `data/` を読み取り専用にしてから欄で Ctrl+S | 失敗の 1 行（本文の Ctrl+S と同じ見せ方）が出て、閉じたあと**押した欄へフォーカスが戻る**。欄は閉じない |  |
| 67-5 | 無題のノートで欄から Ctrl+S | 名前入力の面が開き、閉じたあと**押した欄へ戻る** |  |
| 67-6 | Ctrl+P → Tab でヘルプを開く | 「入力欄でも  Ctrl+N 新規 …」の行が読める（幅は 406 / 428 px で収まると実測済み。高さは #69 で直した。最小寸法では説明の 1 枚目に出る） |  |

### #69 最小寸法のパレット（ADR 0018 の 2026-09-22 の補正）

| # | 操作 | 期待する見え方 | 結果 |
| --- | --- | --- | --- |
| 69-1 | 窓を最小寸法 560×360（96 DPI）にして Ctrl+P → Tab | 操作の一覧が **3 行**・キー説明が **6 行**出る。「キー操作を閉じる」の札が案内の 1 行目と重ならず、札の右の「1/3」とも重ならない | |
| 69-2 | そのまま PgDn を 3 回、PgUp を 3 回押す | 説明が 1 枚ずつ送れて印が `1/3` → `2/3` → `3/3` と変わり、**末尾で止まる**。PgUp で戻り、先頭で止まる。Esc で閉じて開き直すと `1/3` から | |

### #74 文言の表引き（ADR 0030。字面は変わらないので 1 行だけ）

| # | 操作 | 期待する見え方 | 結果 |
| --- | --- | --- | --- |
| 74-1 | 検索の欄・置換の欄・パレットの案内とキー操作・「操作 ▾」・折畳印・名前入力の面（題・案内・ボタン・見出し）・無題のパンくずを一通り開く | **以前とまったく同じ字面**で、欠けも文字化けも余分な空白も無い | |

### #75 テーマと設定画面（ADR 0031。**描いた絵そのものは一度も見ていない**）

| # | 操作 | 期待する見え方 | 結果 |
| --- | --- | --- | --- |
| 75-1 | 起動してノートを 1 つ開く（OS はダーク） | 地が aubergine（`#2C001E` 系）、選択行が `#772953`、札が orange の面に暗い字。文字が読めて目が痛くない | |
| 75-2 | 頭の右端を見る | 閉じる× の左隣に**歯車**が並ぶ。大きさと縦位置が× と揃い、「閉覧」「編集」の札と重ならない | |
| 75-3 | 歯車をクリック | 設定画面が開き、「テーマ」の見出しと 3 行が出て、**今の選択の行に小円**が付き、その行にカーソルがある | |
| 75-4 | ↑ ↓ で移り、「ライト」で Enter | 見出しの行には止まらない。Enter で**欄は開いたまま**全体がライトになり、小円が「ライト」へ移る | |
| 75-5 | そのまま窓の縁を見る | 枠なし窓の縁の色がライトの `border` へ変わる（**画素での裏取りができていない項目**） | |
| 75-6 | Esc で閉じる | 面が消え、フォーカスが開く前の区画（索引か本文）へ戻る | |
| 75-7 | 編集モードにして数文字打ち、保存せずに設定画面でテーマを変える | 本文が消えず色だけ変わり、**Ctrl+Z 1 回で打つ前に戻る**。未保存のままで、キャレットと選択もその位置 | |
| 75-8 | 長いノートを閲覧で途中までスクロールしてテーマを変える | 色だけ変わり、**読んでいた位置が動かない** | |
| 75-9 | Ctrl+F で一致を 1 つ選んだままテーマを変える | 一致のハイライトが残る（その位置へ寄るのは意図どおり） | |
| 75-10 | 設定を「OS に従う」に戻し、Windows の設定アプリで「アプリのモード」を切り替える | アプリを触らずに配色が追従する（**実機でしか確かめられない項目**） | |
| 75-11 | 設定を「ダーク」に固定してから同じ切り替え | アプリはダークのまま動かない | |
| 75-12 | IME を ON にして設定画面で英字を打つ | **組成の窓が出ない**。文字も入らない（↑ ↓ Enter Esc だけが効く） | |
| 75-13 | 設定画面を開いたまま Ctrl+S / Ctrl+P | Ctrl+S で保存されて面は開いたまま、Ctrl+P で操作の一覧へ切り替わる | |
| 75-14 | 窓を 560×360 にして設定画面を開く | 見出しと 3 行が全部見え、案内の 1 行と重ならない | |
| 75-15 | `data/settings.json` を壊して起動し、設定画面を開く | 開いた瞬間から理由の 1 行が出て、印は「OS に従う」に付く。Enter しても断られ、**ファイルは上書きされない** | |
| 75-16 | `:set theme=dark` と `:set theme=blue` | 前者は欄が閉じてダークになり、後者は未知の操作と同じ 1 行で断られ、入力が消えない | |
| 75-17 | Ctrl+P でパレットを開き、↓ で**一番下の行**まで行ってから打ち込みで「設定」を選んで Enter | 設定画面が開いて**見出しと 3 行が全部見える**（S1 の回帰。直す前は空の箱だった） | |
| 75-18 | ドロワーの頭の「すべてのノートを検索」の欄を見る（ダーク） | 欄の地と文字が周りと同じ配色になる（**白い帯が浮かない**）。ライトへ切り替えても追従する | |
| 75-19 | 設定画面の上の空き帯（欄を持たないのに式が高さを残している） | 案内の 1 行と見出しの間が不自然に空いて見えないか（気になるなら Issue にする） | |
| 75-20 | 読み取り専用の `data/` で設定画面からテーマを変えると `:set number` を試す | どちらも**箱ではなく欄の中の 1 行**で理由が出る（D1） | |

### #85 Ex の欄を開いた直後のキャレット（ADR 0016）

| # | 操作 | 期待する見え方 | 結果 |
| --- | --- | --- | --- |
| 85-1 | 索引で `:` を押し、続けて `w` と打つ | 欄が `:w` になる（`w:` にならない）。キャレットは開いた瞬間から `:` の直後で点滅する。Ctrl+P のパレット・Ctrl+F の検索欄・置換の 2 欄は従来どおり | |

### #86 絞り込みの語と絞り込み中の見え方（ADR 0024 の 2026-09-23 の補正）

| # | 操作 | 期待する見え方 | 結果 |
| --- | --- | --- | --- |
| 86-1 | Ctrl+Shift+F で欄へ移り、一致するノートがある語を打つ | **打った語が欄に残って見え続ける**（索引が絞られても消えない）。カーソルを本文や索引へ移しても残る | |
| 86-2 | そのまま欄を見る | 欄の外周が**札の色（ダーク `#E95420` / ライト `#772953`）の 1px の枠**で囲まれ、**右端に `×`** が出る。枠が帯からはみ出さず、`×` が字と重ならない | |
| 86-3 | ドロワーの頭の帯の右を見る | 数が「**一致 / 総数**」（例 `3 / 128`）になる。語を消すと総数だけに戻る | |
| 86-4 | `×` をクリックする | 語が消えて絞り込みが解け、枠と `×` も消える。索引が元に戻る。Esc / Enter は**従来どおり**（索引へ戻して絞り込みは残る） | |

## 見つかったことの書き戻し先

- 直せる不具合は新しい Issue にする（この表は目視の記録で、修正の台帳ではない）。
- 直さないと決めたものは、該当する単位の確認記録の「限界」へ 1 行足す。
- すでに Issue になっている既知の項目: **#70**（一致の入れ物のメモリ）。
- **#67**（入力面の Ctrl+S）は main `ba0e538` で直した（上の 67-1〜67-6 が目視の項目）。
- **#85**（Ex の欄のキャレットが先頭に残り、打った字が `:` の前に入る。**#37 以来の欠陥**）は
  hide が 2026-09-23 に main `2448987` の実機で見つけた。上の 85-1 が直した後の目視の項目。
- **#86**（絞り込みの語が索引の描き直しで消える。**#40 以来の欠陥**）は hide が 2026-09-23 に
  main `2448987` の実機で見つけた（40-3 / 40-5 の結果欄）。直しと絞り込み中の見え方は 86-1〜86-4 で見る。
- **#69**（最小寸法で「キー操作を表示」を開くと操作の一覧が 0 行になる。39-8 で見えるはずの状態で、
  **#41 以前からの欠陥**）は直した（[確認記録](2026-09-22-palette-checks.md)）。**39-8 は直した後の exe で見る**ことになり、
  「空の 1 行が余っていない」に加えて上の 69-1 / 69-2 を同時に見てほしい。
