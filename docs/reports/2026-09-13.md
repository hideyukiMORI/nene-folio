# 日報 — 2026-09-13

## 別名保存の実装・検証、17:49 JST（#55）

- #53 / PR #54は最終HEAD16a435320006844c9cd75d9753a428e5b1bcab64の全体チェックがexit 0（前後clean）。CTest2/2、1628/1730分岐=94.1040%、14 proofs成功。CI34748076131も成功しmain35d86af3f45c8c4993786e3ede17a96f2dace467へ統合した。
- #55 / `feat/55-save-as` / `out/worktrees/55-save-as` / ADR0021で別名保存を実装。共通の作成と名前入力を拡張し、元のwrite/archiveを先に呼ばない。閲覧は原文、編集は未保存入力を使う。
- GUI「別名で保存」、Ctrl+Shift+S、:saveasが同じ操作。主窓のHACCELでShiftを区別し、IMEと名前入力に漏らさない。閲覧中も作成後の台帳失敗は保存で再試行、:qで拒否する。
- ビルド/clang-tidy、CTest2/2、確保失敗を含む1638/1742分岐=94.0299%とnegative proofが成功。主窓の独立Windows部品測定はフォーム4回・失敗0で、取消/Undo/Ex/閲覧原文/部分失敗を確認。詳細は[別名保存の確認](../quality/2026-09-13-save-as-checks.md)。
- 最終ゲート/CI/統合の最新結果は#55のPRへ。実機の物理キー・IME候補・目視は未確認。既存のユーザーアプリは操作/終了していない。スキーマ・依存・ゲート変更なし。Waivers: none。
- 次は改名。未保存なら保存成功後にmd・索引・パンくず・履歴を追随し、履歴失敗を成功にしない。永続的な復旧情報と再起動後の再開をADRで決めてから実装。その後、現在ノートの/・?・n・NとGUI検索を続ける。

## 無題と初回保存の実装・検証、17:30 JST（#53）

- #51 / PR #52を最終HEAD d07fcf620f12c5d9c027ddc71468099b42749c23の全体チェック（前後clean）とCI34746043689成功後に統合。mainは7ff99c29955ec1825ac246af012042387268a662。
- #53 / ADR0020 / `feat/53-new-note` / `out/worktrees/53-new-note`で無題と初回保存を実装。GUI「新しいノート」、Ctrl+N、:enew、名前/カテゴリ入力、:w 名前、同名保護、取消時の本文/Undo保持を共通操作へ接続した。
- 読み取り専用の独立レビューで初回保存のカテゴリカーソル、最大名の一時ファイル、台帳未同期の:qの3点を検出し修正。長い名前の実測で見つけたMAX_PATH制限もroot生成一か所で修正した。
- ビルド/clang-tidyとCTest 2/2、確保失敗を含む中核測定が成功。実ファイル・実アダプタの履歴・モーダル入力・主窓のEx/取消/Undoの独立部品測定も失敗0。ASan計装ライブラリを使う部品exeにはWindows interceptor警告が出るため、警告なしのASan確認とは扱わない。
- 詳細・実行物・測定限界は[初回保存の確認](../quality/2026-09-13-first-save-checks.md)。最終フルゲート/CI/統合の最新結果は#53のPR本文。実機の物理キー・IME候補窓・高DPI・目視は未確認。既存ユーザーアプリは操作・終了していない。
- 台帳の永続化スキーマは版1を維持。依存・ゲート変更とwaiverはなし。統合後は別名保存→改名→ノート内検索を継続する。別名保存は元の未保存変更を先に保存しない。改名は履歴を追随させ、途中失敗と再起動復旧をADRで先に決める。?のヘルプ割当は後方検索の実装と同時に移す。

## 日本語GUI入口の実装（#49、15:50 JST）

hideの続行指示を受け、#44・#46・#48をそれぞれ最終HEADの正規フルゲートとCI成功後に統合した。
#46のCIは本文の必須フィールド不足を検出し、PR本文を修正して成功。検査を弱める変更はない。
mainは23b3ad1。フルゲートログはルートout/design/2026-09-13/のpowerline-7142a6b-final-gate.logとgui-plan-final-gate.log。

#49を作りADR0018を先に記録し、36pxの日本語操作行、共通登録表からの操作メニュー、F1/:hとヘルプの説明折畳みを実装。
登録済み7操作を小さい窓でも移動可能にし、閲覧本文の:とi、:startinsertを追加した。
編集開始を繰り返しても本文を再設定せず、最初のUTF-16準備失敗ならmodeを変えない。
本文のIME compositionをnote_paneで記録し、未確定入力中のキーをRichEditへ渡す。

固定toolchainの製品・単体ビルドとclang-tidy、CTest 2/2、Conformance 0違反、git diff --checkが成功。
初回の狭いビルドでsmoke対象を含めずCTestが未実行となったため、toolchain_smokeをビルドして2件成功を確認した。
Ready直前のフルゲートとCIは最終SHAで行い、PR本文へ結果を記録する。Win32の目視・クリック・IME・Undoは未確認。
15:45 JSTにComputer Useのスキルと正規APIで再確認したがnative pipe unavailable / os error 2。通常起動は別経路で確認する。

HHKBのAlt配置をhideから聞き、編集中の移動キーはAlt案からCtrl+h/j/k/l案へ変更して提案。これは未実装。
規則: ARC-001 / ARC-004 / ARC-005 / ARC-011、C-002 / C-012 / C-017、QLT-009 / QLT-013、GIT-001〜004。
操作表・FR-027・ADR・日報・引き継ぎを更新。保存スキーマ・依存・ゲート変更なし。Waivers: none。

## ヘルプの入口と説明（#37 / PR #44）

hideが画面案を確認しておおむね了承し、INDEXの `?` とキーバインドの説明を希望した。
`?`を `:help` と同じ `FOLIO_COMMAND_HELP` へ渡し、同じパレットに区画別の説明7行を追加した。
本文・入力欄の `?` は通常の文字のまま。Helpを閉じた後のフォーカスは既存の開閉処理で戻す。
最小窓で説明が下へはみ出さないよう、必要時だけパレットの上端を詰める。説明と候補の領域を分けた。

変更ファイル: `src/ui/win32/folio_window.c`、`SPECIFICATION.md`、`README.md`、計画・デザイン判断・実機確認手順、
[キーバインドとコマンド](https://github.com/hideyukiMORI/nene-folio/blob/6dd9dd2028fea6dd315c70bc4be8503ff18e943b/docs/KEY_BINDINGS.md)。既存ADR 0016の共通パレットの範囲で完結し、受理済みADRは書き換えていない。
規則: ARC-001 / ARC-004 / ARC-011 / C-002 / C-012 / C-017 / QLT-013。

狭い検証: toolchainを読み、`clang-format -i src/ui/win32/folio_window.c`、`python eng/conformance.py`、
`cmake --build build --target nenefolio`、`git diff --check`を実行、すべてexit 0。
文書込み最終HEADのフルゲート結果はPR #44に記録する。

Win32の実機確認は未実施。前セッションのComputer Use native pipe unavailable / os error 2は未解消として扱い、
`?`と`:help`の一致、本文への文字入力、復帰先、最小窓・高DPIの説明の収まりを実機確認手順へ追加した。
スキーマ・依存・ゲート変更なし。Waivers: none。

## パンくずのPowerline案（#45）

hideの追加依頼で [Issue #45](https://github.com/hideyukiMORI/nene-folio/issues/45) を作成。
ClaudeCodeのデザインリナへ、カテゴリ番号→カテゴリ名→ノート名の矢印状の帯と、通常幅／最小幅の案を依頼した。
特殊なフォント記号に依存せず図形で描き、右側の操作領域と現行ADR 0011の省略順を守る方針。
#37のキーバインド追加とは別の枝 `feat/45-powerline-breadcrumb` / `out/worktrees/45-powerline` で実装した。
旧来のスラッシュ描画を置き換え、同じ名前の幅割当を使って色面と多角形の矢印を描く。
カテゴリ色を番号の背景へ使い、相対輝度のコントラスト比で白黒の字を選ぶ。狭い時は余白を詰め、
カテゴリ文字の割当幅が0ならその区画を省く。右ペインの背景・見出しはメモリDCで描いて転送する。

変更: `src/ui/win32/folio_window.c`、`folio_palette.{c,h}`、`breadcrumb_layout.h`、ADR 0017と索引、
デザイン判断・実機確認手順・現状文書。ARC-001 / ARC-004 / ARC-007 / ARC-011 / C-002 / C-012 / C-017 / QLT-013。
既存の保存・操作札・クリック・フォーカス・子窓のクリッピングは維持した。スキーマ・依存・ゲート変更なし。

狭い検証: toolchainと整形後、`python eng/conformance.py`、`cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Debug`、
`cmake --build build --target nenefolio`、`git diff --check`がすべてexit 0。最終HEADのフルゲート結果は#45のPR本文に記録する。
#37の追加HEAD `541897e`は正規フルゲートexit 0、CTest 2/2、1484/1572（94.4020%）、14 proofs。

Chromeで版6の通常dark/light・最小窓・初版Helpを01:00〜01:02 JSTに確認し、目立つ重なりや切れは見つからなかった。
Win32は同日に再接続してもnative pipe unavailable / os error 2。[実機確認項目](https://github.com/hideyukiMORI/nene-folio/blob/6dd9dd2028fea6dd315c70bc4be8503ff18e943b/docs/quality/2026-09-13-powerline-checks.md)は未実施。
Powerlineの実装は#37の上に積み、両方とも実機未確認のDraftとして扱う。Waivers: none。

## 入力面のIMEガードの補修（#37 / PR #44、13:50 JST）

設計サナとSolの実装サナが保存・終了・フォーカス・IMEの経路を監査した。
`command_character`が変換中の判定より前にCtrl+Pを処理し、パレットへの切替で入力欄を空にする経路を確認。
同じ関数の先頭で既存の`command_composing`を調べ、変換中の文字メッセージを既定EDITへ渡すようにした。
実際にこのメッセージ列が届くかはIMEに依存し、今回の指摘は静的な経路の確認。実機での再現確認とは区別する。

変更した製品コードは`src/ui/win32/folio_window.c`の当該関数だけ。ADR 0016の入力保護の範囲に閉じ、
公開API・状態の所有者・スキーマ・依存・ゲートは変えていない。操作表と実機確認項目へ変換中Ctrl+Pを明記した。
規則: ARC-001 / ARC-004 / ARC-011 / C-002 / C-017 / QLT-013。Waivers: none。

Solが固定toolchainを読み込んで整形し、`python eng/conformance.py`（0違反）、
`cmake --build build --target nenefolio folio_tests`（exit 0）、
`ctest --test-dir build --output-on-failure`（2/2成功）、`git diff --check`（exit 0）を実行した。
通常PATHの`clang-format`は見つからず、`eng/toolchain.ps1`をdot-sourceして固定版で再実行した。
今回の修正後のフルゲートは未実施。Readyにする前にその時点の最終HEADで実行する。

Computer Useは初期化後とJavaScriptセッションのリセット・再初期化後とも
`native pipe unavailable / os error 2`で失敗。GUIは起動せず、hideへ接続の復旧を依頼した。
RichEdit本文のIME中Esc/Ctrl+P/Ctrl+Sは、届くキーの列を実機で確認してから判断する。
未確定文字の有無をIMEのopen状態だけで代用する修正はしていない。両PRの実機確認と統合は残る。

## IME修正の取り込みと接続経路の確認（#45 / PR #46、14:08 JST）

#37の`f3a0800`を#45へ取り込んだ。製品コードの追加差分は同じ`command_character`のIMEガードだけで、
パンくずの描画変更と競合しない。日報・引き継ぎの競合は、#45の実装記録と#37の修正記録を両方残して解消した。
ARC-001 / ARC-004 / ARC-011 / C-002 / C-017 / QLT-012 / QLT-013。スキーマ・依存・ゲート変更なし。Waivers: none。

固定toolchainで`clang-format --dry-run --Werror src/ui/win32/folio_window.c`、
`cmake --build build --target nenefolio folio_tests`、`ctest --test-dir build --output-on-failure`を実行し、exit 0、CTest 2/2成功。
`python eng/conformance.py`は0違反、`git diff --cached --check`もexit 0。
ビルドしたexeのSHA-256は[実機確認項目](https://github.com/hideyukiMORI/nene-folio/blob/6dd9dd2028fea6dd315c70bc4be8503ff18e943b/docs/quality/2026-09-13-powerline-checks.md)へ記録した。
最新修正後のフルゲートは未実施。QLT-012に従い実機確認後のReady移行時に実行し、過去HEADの成功を代用しない。

hideの依頼で9/12開始のセッションログを読み、23:53 JST以降の画面案確認はChrome拡張接続のBrowser Useで
Artifactを操作・撮影していたことを確認した。同じログのWindows操作`sky.list_apps()`は23:27・23:28 JSTにも
native pipe unavailable / os error 2。9/13のプラグイン切り替え後も同じ接続エラーだった。
Windows接続が今回初めて壊れたとは判断できず、他セッションへの影響も未確認のためCodex全体の再起動は保留した。
コード・ビルド・自動テストは続行できる。Chrome確認をWindows本体の実機確認に読み替えず、両PRをDraftで維持する。

## hideによる実機確認（#37 / #45、14:18〜14:22 JST）

hideの「立ち上げて」という依頼に従い、#45の`d01a623`のビルドを通常起動した。
Computer Useで画面を操作する経路は使わず、hideが手動で確認した。`build/data/`へ架空ノート3本を新規作成し、
実ノートには触れていない。最初のプロセスはPID 45492、hideが終了した後の再起動はPID 42652。

案内した入力保持と終了拒否・保存の2項目は14:18 JSTに「大丈夫そう」、
パレットIME変換中のCtrl+PとEnter、および長いパンくずを狭めた表示の2項目は14:22 JSTに「オッケー」と報告された。
exeのSHA-256、具体的な操作、証拠の範囲は[コマンド](https://github.com/hideyukiMORI/nene-folio/blob/6dd9dd2028fea6dd315c70bc4be8503ff18e943b/docs/quality/2026-09-12-command-checks.md)と
[パンくず](https://github.com/hideyukiMORI/nene-folio/blob/6dd9dd2028fea6dd315c70bc4be8503ff18e943b/docs/quality/2026-09-13-powerline-checks.md)の確認記録へ追記した（QLT-013）。

今回の主要4項目には問題の報告がないためReady移行の最終ゲートへ進む。
保存失敗、本文IME、Undo、未選択・未知コマンド、別DPI・両テーマなど未確認条件は残るリスクとして維持する。
DPI・窓寸法・IME名は未記録で、サナ自身の目視確認とも区別する。スキーマ変更なし。Waivers: none。

#37の`f3a0800d45e67830fdc576f64cbc571edc9866a1`で`pwsh -NoProfile -File ./eng/check.ps1`を実行し、
14:25 JSTにexit 0を確認した（実行前後clean）。Conformance/Symbols 0違反、CTest 2/2、14 real-tool proofs。
ログはルートの`out/design/2026-09-12/command-f3a0800-final-gate.log`。#45の最新HEADのフルゲートはまだ未実施。

## 日本語GUIと検索・ヘルプの再設計（#47、14:50 JST）

hideは`?`後方検索、画面上の「ヘルプ」とF1、日本語ボタン・メニューを主入口にする案を明示採用した。
閲覧本文からの`:`・`i`、新規md、初回の名前指定保存、別名保存、改名も同日了承済み。
[後続計画](../plans/2026-09-13-gui-first-operations.md)に旧計画との変更点、全ノート検索とノート内検索の分離、
キー・GUI対応、無題の保護、改名時の履歴追随と復旧の設計課題を記録した。
この作業は計画だけで、製品コード・受理済みADR・スキーマ・ゲートは変更していない。

デザイン相談はhideの今回の明示依頼に従いClaudeCodeのデザインリナへ依頼した。
CLIセッション`d2bc8c9f-b536-49a5-af88-98ae9e2a6ac6`で`/design`の読込成功をログで確認。
初稿へ、記号だけの入口を日本語へ変えること、検索対象の独立、INDEXにも`?`を一貫適用すること、
`n`／`N`の方向、全操作へのGUI経路、履歴失敗を成功にしないことを返した。新案のChrome目視はまだ未完了。

PR #44へ`f3a0800`の最終ゲート結果を反映し、PR #46へ現在HEAD`6dd9dd2`の文書追記とhideの手動4項目の範囲を反映した。
両PRはDraftを維持。この計画の新機能を既存exeの実装済みに数えない。
規則: ARC-001 / ARC-004 / ARC-011、QLT-012 / QLT-013、GIT-001〜004。Waivers: none。

## 次期GUIの採用と確認範囲（#47 / PR #48、15:13 JST）

デザインリナと初稿の採否を往復し、同じArtifactの版8/9で通常・メニュー・名前入力・検索・ヘルプ・最小窓を目視した。
36pxの日本語操作行と検索対象の分離を採用。ヘルプの省略、最小窓で候補よりキー説明が大きい問題、
パンくずがヘルプへ重なる問題を返し、版11で通常・最小・キー展開の3画を再確認した。
最小の初期ヘルプでも説明2行と日本語候補5行が見え、キー説明を開いた姿は全文が読める。配置を採用した。

[デザイン判断](../design/2026-09-13-gui-first-operations.md)へ結果を記録した。版11は次期GUI11画、既存案11画の計22画。
実際の保存・キー・説明の開閉クリック・スクロールを検証した結果ではなく、Windows実機の証拠にも流用しない。
原本はルート`out/design/2026-09-13/`、親の最終採否は`parent-feedback-final.md`。

この計画はmain`c04f898`から作成した独立したDraft PR #48へ保存。#37/#45の記録へのリンクは、その時点の固定SHAへ向けた。
`git diff --check`と`pwsh -NoProfile -File eng/validate-git.ps1`がexit 0、変更文書のローカルリンク先の欠落0を確認。
計画PRの正規フルゲートは未実施でDraftを維持する。製品コード・スキーマ・依存・受理済みADR・ゲート変更なし。Waivers: none。

## 編集本文の移動（#51、16:42 JST）

#49 / PR #50は7288c25の正式フルゲート（前後clean）とCI34744075792が成功し、main b3a6b49へ統合。
16:09 JSTにhideから再起動したexeに「大丈夫そう」と報告を受け、PRへ記録した。個別条件やDPI・IME名は未記録。

HHKBの左Altは英字直接入力のため、続行指示に従いCtrl+h/j/k/lの左／下／上／右を#51で実装。
ADR0019でメッセージ翻訳とRichEdit自身のキャレット所有を固定した。HACCELとITextSelectionをnote_paneが所有し、
変換済みキーの文字通知を抑止する。本文・Undo・キャレットの独自コピーは作らない。Shift/Alt併用・IME変換中・他区画は従来処理へ渡す。

製品ビルド（clang-tidy）とCTest 2/2、非表示Win32部品の矢印との113比較が成功。
測定当初のBackspace/Enter失敗はWM_CHARしか送っていなかった測定の通知順を修正して解消した。
詳細と実機未確認の範囲は[確認記録](../quality/2026-09-13-caret-checks.md)。Ready用ゲートとCIはPR最終HEADで別途記録する。
仕様FR-028、ADR、用語、操作表と状態文書を更新。スキーマ・依存・ゲート変更なし。
規則: ARC-001/002/004/005/007/011、C-002/005/006/012/017、QLT-012/013、GIT-001〜004。Waivers: none。

hideから継続指示があり、新規と初回保存、別名保存・改名、ノート内検索を次の焦点として進める。
