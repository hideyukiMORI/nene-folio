# 日報 — 2026-09-16

## 再開・別名保存の統合・名前変更の実装（#55 / #56 / #58）

hideの「対応を進めて」で9/13 18:18 JSTの停止を解いた。設計・判断は設計リナ（Fable）、実装・調査・レビューはOpusの実装リナが背景で行い、
互いの報告で詰めた。

- **PR #57（#55 別名保存）を統合。** mainは`f35870d`。9/13にReadyへ変えた`ready_for_review`イベントをGitHubが取りこぼしCIが走っていなかったため、
  Draft→Readyをやり直してCI 35102202403（HEAD 7708802）成功を確認した。
- 統合前の読み取り専用レビューで2点を検出し、同じPRで修正（`c396b33`）。台帳の修復に失敗しただけで何も作っていないのに名前入力面が成功として閉じ
  名前が消える問題は、結果型に`FOLIO_STATE_LEDGER_UNSYNCED`を足して公開後の`LEDGER_STALE`と分け、入力面は名前を保って理由を出す形にした。
  `:q`だけ台帳の修復を再試行しない非対称は他の保存系と揃えた。ADR 0021に補正節を追加。`c396b33`でフルゲートexit 0（1642/1746分岐=94.0435%）、
  CI 35103815803 / 35103930159成功、squash merge。
- **ADR 0022の決定3を補正**（`fe0c20c`）。「書けないdataは起動拒否」をやめ、他プロセスがロックを持つ共有違反だけ起動を拒み、
  アクセス拒否・読み取り専用ではロック無しで起動を許す。`.rename.json`があってロックを取れないときだけ起動しない。決定4（ローカルNTFS・reparse point拒否）は据え置き。
- **#56の残り（port / adapter / application / UI / 文書 / probe）を実装リナへ委任し、Draft PR #59を作った。** 層ごとに5コミット。
  実アダプタ測定35/35、Windows部品測定12/12、単体はCTest 2/2、分岐1929/2090=92.2967%。
- 設計リナが作ったmainの取り込みmergeの件名がGIT-003の形でなく`validate-git`だけが落ちたため、`chore(sync): …(#56)`（`f2fe559`）で作り直し、
  5コミットを載せ直した（木は書き換え前の`8188e25`と同一）。HEAD `163a9e0`でフルゲートexit 0（前後clean・Git conventions 9 commits）。
  ログは`out/design/2026-09-16/rename-f2fe559-final-gate.log`。`--force-with-lease`で枝を更新。
- **Issue #58（現在ノートの検索）を作成。** ADR 0023の草案（対象はRichEditが今表示している平文、判断はcore、語と方向だけapplication、
  検索欄はADR 0016の入力面、Enterで閉じない、`n`/`N`は欄が閉じていても効く、INDEXの`?`をこの単位で後方検索へ移す）は設計リナの手元。
- 規則: ARC-001/004/007/010/011、C-002/005/007/012/014/017、QLT-009/010/012/013、GIT-001〜004。台帳スキーマ版1は維持。
  新設は`data/.rename.json`（版1）と`data/.nenefolio.lock`。依存・ゲート変更なし。Waivers: none。

実機の物理F2・HHKB・IME候補窓・高DPI・両テーマ・最小寸法の目視は未確認。電源断の耐久性は未証明。
hideの`data/`が同期フォルダ（OneDrive等）の中なら決定4で改名が拒否されるので、置き場の確認を依頼している。

23:55 追記: 独立レビューの所見1（記録公開後の失敗で意図が捨てられる）、所見2（未完了中のF2で
「名前変更の復旧待ち」が名前欄に入る）、所見3（記録が外から消えたときの再開が新規開始に落ちる）を、
同じ PR #59 の中で修正した。最終SHAと数字は PR #59 の本文を正とする。
