# 日報 — 2026-09-17

## 名前変更の統合・現在ノートの検索の実装（#56 / #58 / #61）

設計・判断は設計リナ（Fable）、実装と読み取り専用レビューはOpusの実装リナが背景で行い、報告で詰めた。

- **PR #59（#56 名前変更）を統合。** mainは`69431cb`。独立レビューの止める所見2つと追補1つを同じPRで修正した。
  記録公開後の失敗は`RENAME_PENDING`（再試行で進む）と`RENAME_HALTED`（`data/`を人が直すまで進まない）の2値で意図を保持し、
  名前欄の初期値は台帳の実名、未完了中は最初から固定状態で開く。記録の削除が「見つからない」なら完了扱い、再開なのに記録が無ければHALTED。
  実装リナの判断（STARTで他人の記録が既にあればHALTEDで保持）を設計リナが採用しADR 0022へ記録（`6f0c333`）。
  最終HEAD `6f0c333`でフルゲートexit 0（1934/2094分岐=92.3591%）、CI 35112960797成功、squash merge。実アダプタprobe 41/41、Win32部品probe 19/19。
- **#58 現在ノートの検索**をADR 0023（`afcbc70`で受理）に従い`out/worktrees/58-search`で実装し、Draft PR #60。
  core `note_search`（確保なしの純関数・ASCII英字だけ大小無視・巡回）、applicationは語1つと方向だけ、検索欄はADR 0016の入力面（Enterで閉じず、
  Enter＝直前の方向・Shift+Enter＝逆、Esc で元のフォーカスへ）、`/` `?` Ctrl+F GUIの4入口、`n` `N`は欄が閉じても有効。
  RichEditの`EM_STREAMOUT`はCRLF・`EM_EXSETSEL`はCR 1文字なので、検索用に`EM_GETTEXTEX`（GT_DEFAULT）の別経路を足し、表示位置と選択の1対1をprobeで実測した。
  **INDEXの`?`ヘルプはこの単位で後方検索へ移し**、ヘルプはGUI「ヘルプ」／F1／`:h`／Ctrl+Pに残る。SPECIFICATIONのFR-011を現在ノートの検索に改定し、全ノートの絞り込みはFR-032（#40）へ分離。
- 独立レビューは止める所見なし。Ready前に、前方検索が重なる一致を飛ばして件数表示と食い違う点を「前方は選択中の一致の開始の次から（Vimと同じ）」に統一し、
  採用済み計画#47にあった**F3 / Shift+F3**（次へ／前へ・絶対・記憶する方向は変えない）を追加、語のMALFORMEDを欄内表示へ、本文を取り出せない失敗の区別、
  UTF-16入口の本数（6本）の誤記訂正、テストの空白（折り畳みの境界・ordinal・1文字語・反転anchor）を同じPRで扱った。最終結果はPR #60の本文を正とする。
- hideの質問「同期フォルダのreparse pointは一般にどう処理されるか」に回答。OneDrive Files On-Demand / Dropbox Smart SyncはCloud Files APIで全ファイルを
  プレースホルダ（`IO_REPARSE_TAG_CLOUD`系）にするため、ADR 0022決定4の一律拒否だと同期フォルダ内の改名が常に拒否される。他のエディタはタグを見ず、
  リンクを区別する実装はname surrogateのタグだけを拒否する。補正を**Issue #61**として記録（#60統合後に着手）。
- 規則: ARC-001/004/007/010/011、C-002/003/005/012/014/017、QLT-009/010/012/013、GIT-001〜004。永続化スキーマ・依存・ゲート変更なし（検索語は保存しない）。Waivers: none。

実機の物理キー（`/` `?` `n` `N` F3 Shift+Enter F2）・HHKB・IME候補窓・高DPI・両テーマ・最小寸法の目視、件数表示の目視は未確認。
入力面にいるあいだCtrl+Sが効かないのは既存の振る舞いで、検索欄は開いたまま作業する面なので次単位で検討する。
