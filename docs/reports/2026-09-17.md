# 日報 — 2026-09-17

## 再開後 — #40の独立レビューと修正（PR #63）・#39の実測と設計

hideの「続きの対応をはじめて」で再開した。設計・判断は設計リナ（Fable）、レビュー・修正・実測はOpusの実装リナが背景で行った。

- **PR #63の読み取り専用の独立レビュー。** 止める所見1つ（DPIの変更で`refresh_font`が等幅書体を作り直しても、絞り込みの欄だけが破棄済みのHFONTを持ち続ける）。
  直したい所見は、絞り込み中の`h` / `l`とカテゴリ行のクリックが打つたびにモーダルの失敗箱を出す、`resume_rename`が完了させた改名で一致集合が古いまま残る、
  欄でF1 / Ctrl+P / Ctrl+Nが効かず`KEY_BINDINGS.md`の「どこにいても」と食い違う、の3つ。絞り込み無しの配置の不変・`FILTERED`の網羅・台帳への漏れ・
  `index_filter`の境界と多バイトの偽一致・所有権は問題なしと確かめた。
- **採否をADR 0024の補正節（`43df1a5`）に先に記録してから修正した。** UIは絞り込み中の開閉の意図を送らず静かに無視する（`FOLIO_STATE_FILTERED`の契約はUIを通らない入口のために残す）、
  拒んだドラッグをクリックに落とさない、一致集合の作り直しを`adopt_rename`の1か所へ寄せる、カーソルの移し先を「そのカテゴリ行が止まれる行ならそこ、無ければ最初に見える行」の1規則にする、
  欄へ書体の付け直しと全区画の操作を届ける、一致集合を台帳順の不変条件にして所属を二分探索にする（全一致1000本の配置1枚が2.24ms→0.20ms）、対象は保存済みの本文と明記。
- 修正後の最終結果（フルゲート・CI・統合）はPR #63の本文を正とする。単体に再開改名・解除後のカーソル・解除後の移動・`expanded`の保持・coreの長さと順序の表明を足し、Win32部品probeは33→48項目。
- 実装リナの相談への判断: 性能目的の変更は`refactor`で通す（GIT-003のtypeは増やさない）。`filter_input_anywhere`の分割（C-012）と`settle_cursor`のための前方宣言は採用。
  閾値を超えて動いた操作は、確保失敗でドラッグに入れなかった場合もクリックにしない（補正2の意図どおり）。
- **#39（行番号）の実測**（`out/design/2026-09-17/number-probe/`・39項目成功）。RichEditはスクロールで`WM_PAINT`が来ず子窓ごとスクロールするので、RichEditの中に番号を描く方式は成立しない。
  行の高さは一定でない（ASCII 25px・日本語29px・fallback 33px）。`EN_VSCROLL`はつまみのドラッグで来ない。余白や矩形を変えると最初に見える行は保たれない。
  これを受けて設計リナがADR 0025（`data/settings.json`の版1は`number`だけ・キーを足すたびに版を上げる）とADR 0026（coreの`line_index`が論理行を数え、主窓が本文の左の空き36pxに描く）の草案を書いた。
  草案の確定と実装は#63の統合後の枝で行う。

## 後半 — #58の統合・#61の判定修正・#40の実装とhideの停止指示

hideの「区切りのいいところで止めて日報と引き継ぎ書を書いて」に従い、#40のDraft PR #63ができた時点を区切りにした。
**PR #63はレビュー・Ready・統合のいずれもしていない。** 記録の保存後は新しい実装・調査へ進まない。

- **PR #60（#58 現在ノートの検索）を統合。** mainは`ab01654`。独立レビューは止める所見なしだったが、Ready前に前方検索が重なる一致を飛ばして
  件数表示と食い違う点を「前方は選択中の一致の開始の次から（Vimと同じ）」へ統一し、採用済み計画#47のF3 / Shift+F3（次へ／前へ・絶対・記憶する方向は変えない）、
  語のMALFORMEDの欄内表示、本文を取り出せない失敗の区別（`FOLIO_STATE_PANE_UNAVAILABLE`）、UTF-16入口の本数（6本）の誤記訂正、テストの空白を同じPRで扱った。
  最終HEAD `53c558d`でフルゲートexit 0（分岐92.6162%）。CIが1回落ちたのはPR本文が`## 目的`の見出し形式でGIT-004の`目的:`の字面が無かったためで、本文を直して成功。
  Win32部品probe 30/30。INDEXの`?`ヘルプは後方検索へ移り、ヘルプはGUI「ヘルプ」／F1／`:h`／Ctrl+Pに残る。
- **PR #62（#61 reparse pointの判定）を統合。** mainは`b7038f6`。ADR 0022決定4の補正節を先に置き、`plain_entry`を`not_link`へ改めて
  `GetFileInformationByHandleEx(FileAttributeTagInfo)`のタグが`IsReparseTagNameSurrogate`のときだけ拒む（シンボリックリンク・junction）。
  クラウドのプレースホルダなどは通す。判定1か所の変更なので独立レビューは立てず設計リナが差分を読んだ。フルゲートexit 0、CI成功。
  実アダプタprobe 17/17で、変更前の判定に同じprobeを当てると15/17（緩んだのは非surrogateタグの2項目だけ）。実際のOneDrive上の`data/`は未測定。
- **#40（全ノートの絞り込み・FR-032）をADR 0024（`388ee0f`で受理）に従い`out/worktrees/40-filter`で実装し、Draft PR #63。**
  本文の写しは最初の絞り込みで遅延して読み（起動時には読まない）、以後は保存・新規・改名・移動で該当分だけ差し替える。
  判定はcoreの`index_filter`（UTF-8バイト比較・ASCII英字だけ大小無視・名前か本文に部分一致）、`drawer_layout_create`の入力を`drawer_source`へ束ねて
  絞り込みを同じ配置経路に渡す。絞り込み中は`FOLIO_STATE_FILTERED`で並び替え・移動・開閉を断り、現在の文書は一致しなくても閉じない。
  ドロワーの頭36pxに常設の欄、Ctrl+Shift+F、EscとEnterで索引へ戻り絞り込みは保つ、欄のCtrl+Sは共通SAVE。
  実装リナは途中で利用上限により中断し、同じ文脈で再開して完了した。最終HEAD `6c36ef9`でフルゲートexit 0（CTest 2/2・分岐2281/2460=92.72%・14 proofs）。
  Win32部品probe 33/33。性能は1000ノート×10KBで最初の語316.7ms（読み込み込み）、以後の1打鍵は最悪49.5ms。
  副産物として、`read_note`の確保失敗を「読めないノート」として飲み込んでいた経路を伝播させ、`allocation_tests`が途中で止まっていた穴を塞いだ（90.84%→92.72%）。
- hideの質問「同期フォルダのreparse pointは一般にどう処理されるか」に回答し、その結論を#61として実装した。
- 規則: ARC-001/003/004/007/009/010/011、C-002/003/005/012/013/014/017、QLT-009/010/012/013、GIT-001〜004。永続化スキーマ・依存・ゲート変更なし。Waivers: none。

実機の物理キー・HHKB・IME候補窓・高DPI・両テーマ・最小寸法・件数表示と絞り込み欄の目視はいずれも未確認。
入力面にいるあいだCtrl+Sが効かない既存の振る舞い（検索欄・Ex・パレット）はhideの判断待ち。#40の常設の欄だけはCtrl+Sを通している。

## 名前変更の統合・現在ノートの検索の実装（#56 / #58 / #61）

設計・判断は設計リナ（Fable）、実装と読み取り専用レビューはOpusの実装リナが背景で行い、報告で詰めた。

- **PR #59（#56 名前変更）を統合。** mainは`69431cb`。独立レビューの止める所見2つと追補1つを同じPRで修正した。
  記録公開後の失敗は`RENAME_PENDING`（再試行で進む）と`RENAME_HALTED`（`data/`を人が直すまで進まない）の2値で意図を保持し、
  名前欄の初期値は台帳の実名、未完了中は最初から固定状態で開く。記録の削除が「見つからない」なら完了扱い、再開なのに記録が無ければHALTED。
  実装リナの判断（STARTで他人の記録が既にあればHALTEDで保持）を設計リナが採用しADR 0022へ記録（`6f0c333`）。
  最終HEAD `6f0c333`でフルゲートexit 0（1934/2094分岐=92.3591%）、CI 35112960797成功、squash merge。実アダプタprobe 41/41、Win32部品probe 19/19。
- **#58 現在ノートの検索**をADR 0023（`afcbc70`で受理）に従い`out/worktrees/58-search`で実装し、Draft PR #60。
  core `note_search`（確保なしの純関数・ASCII英字だけ大小無視・巡回）、applicationは語1つと方向だけ、検索欄はADR 0016の入力面（Enterで閉じず、
  Enter＝直前の方向・Shift+Enter＝逆、Esc で元のフォーカスへ）、`/` `?` Ctrl+F GUIの4入口、`n` `N`は欄が閉じても有効。
  RichEditの`EM_STREAMOUT`はCRLF・`EM_EXSETSEL`はCR 1文字なので、検索用に`EM_GETTEXTEX`（GT_DEFAULT）の別経路を足し、表示位置と選択の1対1をprobeで実測した。
  **INDEXの`?`ヘルプはこの単位で後方検索へ移し**、ヘルプはGUI「ヘルプ」／F1／`:h`／Ctrl+Pに残る。SPECIFICATIONのFR-011を現在ノートの検索に改定し、全ノートの絞り込みはFR-032（#40）へ分離。
- 独立レビューは止める所見なし。Ready前に、前方検索が重なる一致を飛ばして件数表示と食い違う点を「前方は選択中の一致の開始の次から（Vimと同じ）」に統一し、
  採用済み計画#47にあった**F3 / Shift+F3**（次へ／前へ・絶対・記憶する方向は変えない）を追加、語のMALFORMEDを欄内表示へ、本文を取り出せない失敗の区別、
  UTF-16入口の本数（6本）の誤記訂正、テストの空白（折り畳みの境界・ordinal・1文字語・反転anchor）を同じPRで扱った。最終結果はPR #60の本文を正とする。
- hideの質問「同期フォルダのreparse pointは一般にどう処理されるか」に回答。OneDrive Files On-Demand / Dropbox Smart SyncはCloud Files APIで全ファイルを
  プレースホルダ（`IO_REPARSE_TAG_CLOUD`系）にするため、ADR 0022決定4の一律拒否だと同期フォルダ内の改名が常に拒否される。他のエディタはタグを見ず、
  リンクを区別する実装はname surrogateのタグだけを拒否する。補正を**Issue #61**として記録（#60統合後に着手）。
- 規則: ARC-001/004/007/010/011、C-002/003/005/012/014/017、QLT-009/010/012/013、GIT-001〜004。永続化スキーマ・依存・ゲート変更なし（検索語は保存しない）。Waivers: none。

実機の物理キー（`/` `?` `n` `N` F3 Shift+Enter F2）・HHKB・IME候補窓・高DPI・両テーマ・最小寸法の目視、件数表示の目視は未確認。
入力面にいるあいだCtrl+Sが効かないのは既存の振る舞いで、検索欄は開いたまま作業する面なので次単位で検討する。
