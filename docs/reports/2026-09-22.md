# 日報 — 2026-09-22

hideの「続きの対応を始めて」で再開し、「今日は落とすので日報と引き継ぎ書を書いて」で止めた。
設計・判断・ADR・レビュー依頼・統合は設計リナ（Fable）、実装・レビュー・実測・批評はOpusの実装リナが背景で行い、報告で詰めた。

## #39（原文の行番号・FR-021）の独立レビューと統合 — PR #64

- **読み取り専用の独立レビュー**（重点12項目）。止める所見1つ: `ENM_SCROLL`を足したので`EM_STREAMIN`の途中に`EN_VSCROLL`が届くと、
  同期描画が流し込み途中の本文から`line_index`を作って「古い」の印を落とす経路がありうる。直したい所見6つ（矩形が動かないときも最初に見える論理行を戻す・
  番号の升目を帯で切っていない・パレットの箱が`SET`の分まで高い・`KEY_BINDINGS.md`の節の位置・改名再開を通さないテストの欠落・桁数と番号の列の落とし所の食い違い）、
  追補8つ。`paint_pane`の更新矩形化・`WM_COMMAND`の振り分け・写しの寿命・帯の幅の非振動・「先に書いてから採用」・メニューIDの対応・規約・coreの純粋性は問題なし。
- **採否をADR 0025 / 0026の「2026-09-22 の補正」に先に記録**（`3d06969`）してから修正した。流し込み中の印（`note_pane.streaming`）で構造的に防ぐ＋probeで実測
  （`EM_STREAMIN`中の`EN_VSCROLL`は**0回**。危険は再現していないが印は残す）、`rearrange_keeping_line`は左端が実際に動いたときだけ戻す、
  `draw_gutter`は`SaveDC` / `IntersectClipRect`で帯にクリップし更新矩形と交わらなければ飛ばす、数字の幅は`refresh_font`で1回測る、桁数は古い表を使う、
  パレットの高さは`folio_command_listed_count()`（12）、`adopt_settings`は`LOADED`で出力を触らないポートを`OUT_OF_MEMORY`に畳む、
  テスト追加（改名再開を通さない・LFは普通の文字・設定の`null`と入れ子）。設計リナの判断で`folio_settings_with_number`は「写してから1つ変える」形に戻した
  （引数を`(void)`で捨てる形にしない）。
- 最終HEAD `273a656`でフルゲートexit 0（CTest 2/2・分岐2447/2632=**92.97%**・14 proofs）。Win32部品probe 62/62。CI 35632070699成功。
  squash merge → main `39099c5`。Issue #39は閉じた。

## #65（失敗の文言の表引き・CNF-009）— 新規Issue・ADR 0027・PR #66

- #39のレビューで`folio_state_failure_line`と`unfinished_failure_line`がC-012の60行に対し59行で飽和していると分かり、#41・#38で値を足す前提として新しいIssue #65を立てた。
- **ADR 0027**（`ef87e7a`）: 文言は指示付き初期化子の表から引く。表が列挙の全値を持つことは人の記憶ではなく`eng/conformance.py`の新しい字句検査**CNF-009**
  （`eng/conformance-rules.json`の`lineTables`。列挙の各値が表に`[値] =`としてちょうど1回現れる）が守る。X-macroでの列挙生成・関数のさらなる分割・
  実行時の範囲検査＋単体は却下。
- 実装: 文言は33値で0件差（変更前のソースから機械的に抽出して突き合わせ）。`tests/conformance/test_conformance.py`に6件（63→69）、`eng/prove-gates.py`にP21（14→15本）。
  `docs/QUALITY_GATES.md`にCNF-009をactiveで記載。分岐網羅は**93.55%**（switchの分岐が減った）。
- 最終HEAD `4dcf164`でフルゲートexit 0、CI成功、squash merge → main `4101196`。Issue #65は閉じた。
- 記録: CNF-009は「`[値] =`が1回あるか」しか見ないので、新しい値に空文字列を入れても通る。単体が文言を突き合わせるのは既存の33値だけ。

## #41（現在のmdだけの正規表現置換・FR-023）— 実測・ADR 0028・実装の途中

- **正規表現エンジンの実測probe**（`out/design/2026-09-22/replace-probe/`・101項目成功）。Windows同梱の`icu.dll`（`uregex`）をclang-cl + `icu.lib`で結べる
  （測定機はICU 72.1 / Unicode 15.1。`icu.dll`は1903以降、仕様の最低環境2004以降に含まれる）。位置はUTF-16のコード単位で`EM_EXSETSEL`と1対1、
  サロゲートペアは`.`に1件。既定の行端の扱いでCRは行端（`UREGEX_UNIX_LINES`は付けてはいけない）。利用者が打つ`\n`はCRに一致しない。
  `(?i)`はfull case folding（全角英字も畳む・既存の検索とは意味が違う）。壊れたパターンは`UErrorCode`と`UParseError.offset`で位置が分かる。
  **ICUの置換文字列の文法は利用者に見せられない**（`\n`が`n`・`$100`がエラー・末尾の`\`が消える）。ゼロ幅一致で無限ループしない。上限は`uregex_setTimeLimit`のsteps
  （時刻を読まない・測定機で1 step≒0.15ms）。1.18MiBでliteralの全一致1.5ms・`(\w+)-(\w+)`46ms。`uregex_*`は非決定名に無く、`eng/symbols.py`はcore/applicationだけ検査する。
- **ADR 0028の草案を書き、現行コードに照らした読み取り専用の批評を挟んでから受理**（`2b58e63`。ADR 0016に入力面の欄が集合になる補正節）。批評で直した点:
  引数4つ（C-012）を超える署名が3本あった→`regex_request` / `regex_matches` / `replace_request` / `replace_apply`に束ねる（ADR 0023で一度起きた失敗）、
  `:%s/a/b/g`は空白で切った1語の別名照合では解けない→別名照合の前に「`%s`で始まり直後が英数字でない」で判定、
  ICUはOS同梱なので`runtimeDependencies`ではなく`platformLibraries`が正本で、CODING_RULES第5節の「kernel32以外をimportしない（R1）」は既にuser32等で崩れている→文言を直す、
  入力面のEDITは1つ前提でTabで2つ目へ移ると欄が閉じる→「自分の欄」を集合に、`apply`にanchorが無い→`note_search_span`を受ける、
  下見を捨てる契機の列挙は10か所以上に散って漏れる→写しに宛先を添えて適用時に照合（`REPLACE_STALE`）、`regex_match.h`はcoreに置かないとcoreが読めない、
  stepsの上限はadapterの定数。`folio_state_create`は4引数で飽和し、#38が`struct folio_ports`に束ねる。
- 決定の要点: エンジンはOS同梱のICUをadaptersに閉じる（`nenefolio_system_link(nenefolio_adapters icu)`）／portは1回の呼び出しで閉じる粗い`scan`／
  置換文字列の文法は自前（`&` `\0`〜`\9` `\r` `\n`=CR `\\` `\&` `\/`、他の`\x`と末尾の`\`は拒む）で組み立てはcoreの純関数／
  applicationは下見（UTF-16の写し・一致の列・宛先）を1つ所有しC-014の名指しの例外／1件・全部は`EM_REPLACESEL`1回でUndo 1単位／
  入口は「操作」メニュー・パレットの「置換」の欄（2欄・「1件」「すべて」）とEx `:%s/…/…/[g]`（`g`なしは各論理行の最初の一致）／`:s`は意図して未対応。
- **実装は実装リナが5層で進め、停止時点の状態は引き継ぎに書いた。**

## 再開後 — #41の完成と独立レビューと統合 — PR #68

hideの再開指示で、停止時点で未着手だったui層・docs層・probeを足して#41を完成させた。

- **ui層**（`d4b66c7`）。`note_pane_replace`（`EM_EXSETSEL`→`EM_REPLACESEL(TRUE)`）を1本足し、
  `COMMAND_SURFACE_REPLACE`を入力面に追加した。欄は下端の帯に状態の1行とパターン・置換文字列の**2つのEDIT**
  （control id 4 / 5）と自前描画の「1 件」「すべて」を置く。入力面の「自分の欄」を`command_owns`の**集合**にして、
  EN_CHANGE・フォーカスの離脱・背景色・加速表がそれを見る（ADR 0016の補正）。Tabは`command_tab`が2欄を往復させ、
  **Enterは`WM_CHAR`の0x0D・Ctrl+Enterは0x0A**で見分ける（`GetKeyState`を読まない・ARC-007。部品probeで実測）。
  Ex `:%s`は欄を開かず、UTF-8のバイト位置の部分列を`utf16_text_create`でUTF-16へ戻してから同じ下見・同じ適用へ渡す。
  **`execute_command()`の仮の分岐を置き換えた。**
- **docs層**（`41f560e`）。FR-023・対応環境の`icu.dll`・CODING_RULES第5節R1・KEY_BINDINGSの置換の節・GLOSSARY 6語・
  計画5節・todoと、ADR 0028の「2026-09-22 の補正」節（実装で決定の列挙と差が出た6件・追加決定7件・実測3件）。
- **probe**。実アダプタprobe（`out/design/2026-09-22/replace-adapter-probe/`。製品の`regex_adapter.c`をそのまま
  コンパイルしてポート越しに測る）**32/32成功**。測定機は**ICU 72.1.0.4 / Unicode 15.1**。
  `\q`が`BAD_PATTERN`（`ERROR_ON_UNKNOWN_ESCAPES`が効いている）、CRの行端、サロゲート、群9個と10個目の切り捨て、
  位置6種、`TIMED_OUT`（`(a+)+b`で536ms）、`TOO_MANY`（110万件で73ms）、1MBのscan（literal 4.3ms / `\w+` 21.3ms /
  `(\w+)-(\w+)` 84.8ms）。**長さ0のパターンはadapterで`BAD_PATTERN`**になる（設計probeは`uregex_open(…, -1, 旗 0)`で
  「文字数+1件」だった。applicationが先に`REPLACE_NO_PATTERN`で断るので製品では走らない）。
  `TOO_COMPLEX`は8MBスタック＋2000stepsでは時間の上限が先に効いて**再現できず、測っていない**。
  Win32部品probe（`out/design/2026-09-22/replace-ui-probe/`）**32/32成功**。`EM_REPLACESEL`1回が`EN_CHANGE`1回でUndo 1単位、
  置換文字列のCRが表示本文でCR 1つ、置換後の選択位置、全置換後に最初に見えていた論理行（78行目）が戻ること、
  2欄のTab / Enter / Ctrl+Enter / Esc、`WM_KILLFOCUS`の相手が集合の中（dismissが閉じない）、
  96 DPI・560×360のパレットの箱。

### 独立レビューの所見と直し方

- **B1（止める）: 直前の下見が失敗していてもEnter / Ctrl+Enter / ボタンが古い下見を当てる。**
  `replace_preview_holds()`は本文と宛先しか比べないので、パターンを空にして`REPLACE_NO_PATTERN`が出ている欄で
  Enterを押すと1つ前の置換が本文に当たった。Issue #41の「失敗で本文が不変」に反する。
  **構造で直した**（`6db7a67` / `605405c`）: applicationは`folio_state_preview_replace`がREADY以外を返す
  すべての経路で**下見を捨てる**（下見は常に「最後の入力の結果」か「無い」）。下見が無い適用は既存の`REPLACE_STALE`で、
  新しい結果の値は足さない。UIの早期returnは**表示の都合**（出ている失敗の1行をSTALEで上書きしない）で、
  安全の正本はapplication側である。`REPLACE_STALE`の文言を「本文か入力が変わったので、この置換は当てられません。
  もう一度入力してください。」に変え、単体の期待を反転した
  （"a failed preview drops the previous one; apply then returns STALE"）。
- **D1**（`2f0c3dd`）: `allocation_tests`の`replace_under_probe`の本文が3単位だったので、一致の列を広げる
  `reserve_matches()`のreallocが1度も走らず、`grown == nullptr`の枝が測定ビルドで未到達だった。本文を200単位にした。
  llvm-covの枝がTrue: 1 / False: 53になり、分岐網羅は93.21%→**93.29%**。
- **D2**（`7a1fe47`）: `note_replace_build()`が`count > capacity`の列を「入っているぶんだけ」当てて**黙って部分適用**していた。
  `NOTE_REPLACE_PARTIAL_MATCHES`を足して組み立ての前に断り、`from_replace`は`REPLACE_TOO_MANY`へ写す。
- **D3**（`b2832da`）: ゼロ幅の一致を空文字列で置き換える「1 件」は本文も選択も進まない。**特別規則は足さない**
  （Vimの`:s/x*//`も同じ）。操作表に1行書き、そのときは「すべて」を使うと案内する。
  あわせて「Enter（1件）が探し始めるのは**いまの選択の開始以降**で、ノート内検索の`/`が選択の**次**から探すのとは
  1文字ぶん違う」も書いた。
- **D4**: 死に枝3か所（`source_for()`の`FOLIO_DOCUMENT_NONE`の三項、`apply_replace` / `apply_substitute`の
  `pane == nullptr`の三項）を落とした。
- **追補**: 置換の直後はフォーカスが欄にあるのでCtrl+Zは**欄の取り消し**になる（本文はEscで戻ってからCtrl+Z）。
  `struct regex_match`は200バイト超で、`regex_match_limit`（100万件）直下では`found`と下見の写しに
  **各約200MBを1打鍵ごとに確保する**。出力上限8MBと釣り合っていない（**この規模は測っていない**）。
- レビュー前の修正（`49efa9a`）で、`inline_outcome()`の`||`の並び（`folio_state_outcome`の第2の表）を
  **全42値のswitch**（`default`は書かない・C-002）に置き換えた。値を足すと`-Werror,-Wswitch`で落ちる。
  1値を消すとビルドが落ち、戻すと通ることを確かめた（コミットには含めない）。関数は51行でC-012の60行以内。

### 統合

最終HEAD `b2832da`でフルゲート**exit 0**（CTest 2/2・分岐**2586/2772 = 93.29%**・15 proofs・conformance 69件・
`Conformance: 0 violation(s)`）。ログは`out/design/2026-09-22/replace-gate.log`。
Draft → Ready → CI成功 → squash merge → main **`f4e7bcf`**。Issue #41は閉じた。

### hideの判断と新しいIssue

- **入力面（検索欄・Ex・パレット・置換欄）のCtrl+Sは「保存して欄は開いたまま」にする** → **Issue #67**
  （#41では直していない。絞り込みの欄だけが前から共通の保存へ渡している）。
- 実機の`data/`は**同期フォルダの中ではなくローカルNTFS**（#56 / ADR 0022の前提に合う）。
- **#39〜#41の実機目視は#41統合後にまとめて行う** → [統合チェックリスト](../quality/2026-09-22-visual-checklist.md)。
- 置換の欄の**画面案（Artifact）は不要**。
- 新しいIssue: **#67**（入力面のCtrl+S）・**#69**（最小寸法560×360でパレットの「キー操作を表示」の行が0行。
  **#41以前からの欠陥**で、#41の2行を足しても変わらない）・**#70**（一致の入れ物のメモリと出力上限の釣り合い）。

### 次

次の単位は**設定・テーマ・言語 #38**。`settings.json`を版2にして移行の純関数を初めて書く前に、
**`struct folio_ports`でportを束ねる**（`folio_state_create`は#41で4引数に飽和した）。
#67 / #69 / #70は小さい単位として併走できる。

## 判断と運用

- `(void)引数;`で引数を捨てる形は採らない（「写してから1つ変える」の形に戻した）。
- ゲートの検査器に手が入る変更（#65）は設計リナが差分を直接読んでReadyにした。
- 設計の前に実測probe → ADR草案 → 現行コードに照らした批評 → 受理、の順は#39に続き#41でも手戻りを減らした（批評で実装不能な決定が2つ見つかった）。
- `/design`という名前のスキルはこの環境に無い。画面の案が要るときはArtifactで出す。

規則: ARC-001〜005 / 007 / 009〜011、C-002 / 003 / 005 / 006 / 007 / 008 / 012 / 013 / 014 / 016、QLT-009 / 010 / 011 / 013、CNF-002 / 006 / 007 / 009、GIT-001〜004。
永続化スキーマの変更なし。ゲートの変更は#65のCNF-009の追加（閾値・除外は不変）。#41で`platformLibraries.adapters_win32`に`icu`。Waivers: none。

実機の目視は**まだ1項目も行っていない**。#39の11項目・#40の物理キー／IME候補窓／絞り込み欄の見た目・#41の10項目を
[統合チェックリスト](../quality/2026-09-22-visual-checklist.md)へまとめた（ビルドはmain `f4e7bcf`の`--target nenefolio`。exeができることまでは確かめ、起動はしていない）。
