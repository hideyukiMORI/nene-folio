# 日報 — 2026-09-23

> 深夜〜明け方（00:08〜05:17）の記録は元 `2026-09-24.md`、午後〜夕方（14:33〜18:21）は元 `2026-09-25.md`。どちらも日付を誤って書かれていた（[正誤表](ERRATA.md)・Issue #147）。夜（18:43〜）の #112 と本訂正は末尾の節。

## #96 の統合と #82 の設計（ADR 0034 の草案で停止）

この日に**統合した production の変更は 1 行も無い**。#96 の docs を統合したあと、
**#82（名前入力面と失敗の箱をテーマに追随させる）の実測 probe → ADR 0034 の草案 → 批評**まで進み、
hide の停止指示で **ADR 0034 を受理せずに止めた**。main は **`4e57483`**（clean）。
設計・判断・ADR・批評は設計リナ（Fable）、probe と実装は Issue ごとに立てた agent（Opus）が背景で行った。

### #96 — 2026-09-22 の記録の統合

| 単位 | 枝の最終 HEAD | PR | CI | squash 後の main |
| --- | --- | --- | --- | --- |
| #96 記録（日報・引き継ぎ・§6・todo） | `22c2387` | [#97](https://github.com/hideyukiMORI/nene-folio/pull/97) | 35740104937 成功 | `4e57483` |

docs と `CLAUDE.md` だけで、製品コード・ゲート・スキーマ・閾値は触っていない。Issue #96 は閉じた。
**開いている Issue は #82 / #70 / #42 と、この記録の #98 だけである。**

### #82 — 実測 probe（`out/design/2026-09-24/prompt-theme-probe/`）

`out/` は git の管理外なので、ここには**要点だけ**を写す（本文は README と 4 本のログにある）。
**production コードは 1 行も触っていない。** レジストリは読んでも書いてもいない。
機械は Windows 11 Pro 10.0.26200・clang-cl 19.1.5・**system DPI 120（125%）**・OS は**ダークテーマ**。

- `prompt_theme_probe.c` は `src/ui/win32/name_prompt.c` の作り方を字面どおり写したもの
  （メモリ上の `DLGTEMPLATE`・EDIT・`COMBOBOX(CBS_DROPDOWNLIST)`・STATIC×3・BUTTON×2・同じ矩形・
  `WM_SETFONT` で Yu Gothic UI −14）。**同じソースから 2 本**作り、manifest 無し（= 現行の `NeNeFolio.exe` と同じ
  **comctl32 v5.82**）と `Common-Controls 6.0` を埋めた版（**v6.0**）を比べた。
  結果は no-v6 が **PASS 90 / FAIL 8**、with-v6 が **PASS 82 / FAIL 16**。
  ログの `FAIL` は「**その色が乗らなかった**」という結果そのもので、probe の欠陥ではない。
- **画素は矩形ごとに 1 回の `BitBlt` で 32bpp の DIB へ取る。**
  `GetPixel` をスクリーン DC へ何万回も撃つと**固まる**（最初の版で実際に固まった）。
  窓を画面に出す必要があることは #86 の probe で分かっていたので最初から `WS_EX_TOOLWINDOW` ＋ `HWND_TOPMOST` ＋
  `SWP_NOACTIVATE` で出した。

#### 塗れるもの・塗れないもの（v5 = 現行の出荷形）

| 面 | 返すメッセージ | 結果 |
| --- | --- | --- |
| ダイアログの地 | `WM_CTLCOLORDLG` | `#2C001E` **100%**・**塗れる** |
| STATIC（見出し・注意・失敗の 1 行） | `WM_CTLCOLORSTATIC` | 地 81% ＋ ClearType の縁 14%・字 `#EFE4EA`・**15.06:1**・**塗れる** |
| EDIT の中 | `WM_CTLCOLOREDIT` | 地 92%・**15.06:1**・**塗れる** |
| コンボの閉じた**字の面** | `WM_CTLCOLOREDIT` | 84%・**塗れる** |
| ドロップダウンの**リスト** | `WM_CTLCOLORLISTBOX` | 83%・**塗れる** |
| EDIT の 1px の縁 | — | `#646464` のまま・**塗れない** |
| コンボの**矢印の釦** | — | `#F0F0F0` のまま・**塗れない** |
| ドロップダウンの**選択の帯** | — | OS のアクセント `#0078D7`・**塗れない** |
| ドロップダウンの**スクロールバー** | — | `#F0F0F0` のまま・**塗れない** |
| 押し釦の面 | `WM_CTLCOLORBTN` は**来るが無視される** | `#F0F0F0` のまま・**塗れない** |
| `BS_OWNERDRAW` の釦（`WM_DRAWITEM`） | — | `#1A0011` 73%・**塗れる** |

ライトでも同じ結果で、STATIC / EDIT の字と地は **17.78:1**。`folio_palette` の 2 組はどちらも 4.5:1 を大きく超える。
**押し釦を暗くする手は `BS_OWNERDRAW` ＋ `WM_DRAWITEM` しか無い。**

- **コンボは有効なとき `WM_CTLCOLOREDIT`、`EnableWindow(FALSE)` のとき `WM_CTLCOLORSTATIC` に変わる。**
  後者は `name_prompt.c` の**改名の経路**そのもので、**両方に答えないと改名のときだけコンボが白く残る**。
- **題の帯は DWM で palette の色にできる**。`DwmSetWindowAttribute` はダイアログの HWND にそのまま効き（`hr=0`）、
  `DWMWA_USE_IMMERSIVE_DARK_MODE` **だけ**では OS の暗い題 `#212020`、
  ＋ `DWMWA_CAPTION_COLOR` / `DWMWA_TEXT_COLOR` で **`#1A0011`（`palette.window`）95% ＋ 字 `#EFE4EA`** になった。
  主窓の `decorate()` と同じ `dwmapi` なので `platformLibraries` は増えない。
- **塗ってもフォーカス（開いた瞬間は名前欄）・IME 文脈（同じ `HIMC`）・Enter → `IDOK`・Esc → `IDCANCEL` →
  `EndDialog`・owner の無効化は 1 つも変わらない**（2-a / 2-b / 2-c）。
- モーダルの間も **`WM_SETTINGCHANGE` は主窓に届き、面は閉じない**（同じスレッドの全窓のメッセージをモーダルループが回す）。
  ブラシを差し替えて `RedrawWindow(RDW_ALLCHILDREN | RDW_UPDATENOW)` を撃てば**開いている面も塗り直せる**。
  一方でモーダル中は owner が `EnableWindow(FALSE)` なので、**テーマ／言語の入口 3 つはどれも使えない**（実測）。

#### comctl32 v6 は**後退**する

| 当てるもの | v5（現行） | v6 |
| --- | --- | --- |
| `SetWindowTheme(DarkMode_CFD)` | **何も変わらない**（1 画素も違わない） | コンボが **`#333333`**（uxtheme 固定。palette にならない） |
| `SetWindowTheme(DarkMode_Explorer)` | **何も変わらない** | 釦が `#333333` |
| コンボの閉じた面に `WM_CTLCOLOREDIT` | **塗れる** | **無視され `#FDFDFD` のまま**（**v5 で塗れていたものが塗れなくなる**） |

`IsAppThemed()` は**両方 1 を返す**ので、この API では v6 の有無を判別できない。
`#333333` は地 `#2C001E` の隣で灰色の島になる。**`uxtheme` を足す価値があるのは v6 を同時に入れる場合だけ**で、
その v6 が塗りを後退させる。

#### 失敗の箱

- **`MessageBoxW` は OS がダークでも白い**（client の画素 `#FFFFFF` 50% / `#F0F0F0` 30%）。`MB_*` に色の引数は無く、
  自分の窓手続きを通らないので `WM_CTLCOLOR*` を返す先も無い（色を変える手は `WH_CBT` フックか `#32770` の subclass しか無く、
  ADR 0010 が既に却下している流儀）。**v6 を入れても同じ画素**である。
- 大きさ: 最長の en（131 UTF-16 単位）に `MessageBoxW` が与えたのは 窓 558×216 / client 540×169（DPI 120）＝
  **96dpi 換算で client 約 432×135**。
- **自前で描くときの幅**（Yu Gothic UI 14・`DT_CALCRECT`・26 行を 1 本ずつ）: 折り返さない最大幅は **805px**
  （`name_prompt` の client 400 の 2 倍で、**1 行では絶対に収まらない**）。
  **幅 480 なら最大 2 行（38px）**・幅 360 なら最大 3 行（57px）。
  **高さは文言ごとに変わるので `DT_CALCRECT` を 1 回引く必要がある**（`MessageBoxW` が OS 側で自動にやっていたこと）。
- 箱に出る値: `enum folio_state_outcome` は **42 値**、`inline_outcome()` が真は **14 値**。
  **面が閉じていれば 40 値すべてが箱**、開いていれば **26 値が箱に残る**。
  文言の長さは **en が最長 131**（`INVALID_NAME`）・**ja が最長 64**（`RENAME_HALTED`）。
- **13 か所の呼び出しの文脈**（ドロワー 5・主窓 8）を 1 件ずつ読んだ: `drawer_window.c` の 5 件（行のクリック・ドラッグの落とし先・
  `ChooseColorW` の後の書き戻し・スクロール・`reveal_cursor`）と `folio_window.c` の 8 件（`command_failure` の箱の側・
  検索の本文が取り出せない・Ctrl+S / Esc の保存・ノートの前後移動・カテゴリの折畳＋保存・入力欄の語を UTF-8 に写せない 3 件）。
  **13 か所とも「知らせて戻る」だけで、答えを待って分岐する所は 1 つも無い。**
  モーダルが要る所は無く、`MessageBoxW` は「必ず気づかせる」ためだけに使われている。
- 費用の見積り（**実装していない見積もり**）: (a) `MessageBoxW` のまま **0 行**／(b) 自前のモーダル **約 160 行**／
  (c) `command_layer` の 1 行へ寄せる **60〜100 行 ＋** 面が閉じているときの状態の行の新設 ＋ ドロワーからの `folio_message_*` 1 本。

#### この probe で測っていないもの

**描いた絵そのものの目視**・**IME で実際に組成を始めたときの候補窓の色**（候補窓は IME 自身が描くので `WM_CTLCOLOR*` の外）・
**`WM_DPICHANGED` と塗りの組合せ**・**OS をライトテーマにしたときの各部品の既定色**（この機械は OS がダークのまま測った）・
`ChooseColorW` の窓（#82 の「やらないこと」）・**comctl32 v6 を入れたときの production 全体への影響**
（RichEdit・`ChooseColorW`・主窓の自前描画は見ていない）。

### ADR 0034 の草案（**受理していない**）

`out/design/2026-09-24/adr-drafts/0034-dialog-theme.md`。状態は**草案**で、リポジトリには 1 行も入っていない。
表題は「名前入力面と失敗の箱は comctl32 v5 のまま自前の塗りでテーマに従い、失敗の箱は `MessageBoxW` をやめて
同じ塗りの自前のモーダルにする」。決定の骨は 6 つ。

1. **comctl32 は v5 のまま。manifest も uxtheme も足さない。** v6 は塗りを後退させ、`DarkMode_*` の色は palette にならない。
   `platformLibraries` は変えない。
2. **塗りの経路は 1 本**: 新規 `src/ui/win32/dialog_theme.{h,c}` が名前入力面と失敗の箱の両方から呼ばれる
   （`WM_CTLCOLOR*` の色 ＋ owner-draw の釦 ＋ DWM の題の帯）。**別々に書けば第 2 の経路になる**（ARC-001）。
3. **名前入力面**: 押し釦 2 本を `BS_OWNERDRAW` に、**EDIT は `WS_BORDER` を外して面が枠を描き**（塗れない `#646464` を消す・
   絞り込み欄と同じ流儀）、**コンボは `CBS_OWNERDRAWFIXED`**。有効・無効の両方の `WM_CTLCOLOR*` に答える。
   **残るのはコンボの矢印の釦とドロップダウンのスクロールバー**（v5 の限界として明記し、目視の対象にする）。
4. **失敗の箱は `MessageBoxW` をやめ、同じ塗りの自前のモーダルにする**（案 (b)）。本文の幅は 480px（96 DPI）で
   `DT_WORDBREAK | DT_CALCRECT` から高さを決める。アイコンは描かず `MessageBeep` も呼ばない。
   (c)（欄の 1 行へ寄せる）は採らない: 面が閉じているときの状態の行が無く、「知らせて止まる」意味が変わる。
5. 開いている面のテーマ追随（**批評で撤回。下記 S4**）。
6. やらないこと: v6 / uxtheme / `SetWindowTheme`、`ChooseColorW`（ADR 0010 のまま）、コンボの矢印とスクロールバー、
   失敗の箱のアイコンと音、ハイコントラスト。

### 批評（現行コードに照らした所見）と設計リナの判断

**止める所見 8・直したい所見 12・追補 8 の合計 28 で、設計リナはすべて受け入れた。**
判断は草案の末尾の節に書いてあるが、**本文にはまだ織り込んでいない**。止める側の要点:

| 所見 | 判断 |
| --- | --- |
| **S1** 型の形 | `dialog_theme.h` は**不完全型 `struct dialog_theme;` ＋ `create` / `destroy` の対**（C-003 / C-007）。ブラシは `DialogBoxIndirectParamW` の直後に捨てる（font と同じ対） |
| **S2** 役割の決め方 | 専用ファイルの `enum dialog_theme_surface`（`DIALOG` / `LABEL` / `FIELD` / `LIST`）を面が HWND 比較で決めてから渡す。無効なコンボの `WM_CTLCOLORSTATIC` は `FIELD` |
| **S3** 署名 | `failure_box_show(HWND, enum folio_state_outcome, const struct folio_state *)` の **3 引数**に変える（13 か所は `folio_state_language(self->state)` → `self->state` の機械的な置換）。箱の中で言語と palette を引く。**決定 4 の「署名は不変」を撤回** |
| **S4** 開いている面の追随 | **やめる**。面のテーマは**開く瞬間に固定**（ADR 0032 決定 4 の face と同じ流儀）。モーダル中は入口が使えないので実害が無い。**決定 5 を撤回** |
| **S5** コンボ | `CBS_OWNERDRAWFIXED` **＋ `CBS_HASSTRINGS`**（`fill_categories` の `CB_ADDSTRING` 直後の解放と `CB_GETLBTEXT` を保つため） |
| **S6** 既定釦 | **受理前に probe を取り直す**。`BS_OWNERDRAW` の釦で Tab → Enter がキャンセルになるか・Space・Esc・`DM_GETDEFID`・`ODS_DEFAULT` の有無 |
| **S7** 枠の色 | EDIT の枠と非既定の釦の枠は `chip_background`（`draw_filter_frame` と同じ）。**3:1 の対比を probe に足す** |
| **S8** owner | ドロワーからの 5 か所の owner は `GetAncestor(GA_ROOT)`。**probe で主窓とドロワーの両方の `IsWindowEnabled` を測る** |

直したい 12 は D1（`ODS_DEFAULT` で足りれば `primary` の引数ごと消す）・D2（釦の文言は `GetWindowTextW`）・
D3（ブラシの寿命）・D4（新しい case は `composing` の門より前）・D5（`HDC` で受ける）・D6（戻り値の約束）・
D7（角丸の札の形が `draw_chip` と 2 か所になることを**明記して受け入れる**）・D8（幅は `min(480, 測った幅)` に下限）・
D9（**`MB_ICONWARNING` と音が消えるのは「変わる振る舞い」**として目視項目に）・D10（`WM_DPICHANGED`）・
D11（`UI_TEXT_PROMPT_OK` を 3 か所に足し `APP_NO_WINDOW` を末尾に残す＝CNF-009 の `lineTables` を動かさない）・
D12（`CMakeLists.txt` のソース一覧）。

**#88 で効いた「草案の段階で検査器を回す」がここでも効いた**（S1 / S2 / D11 は CNF-002 と CNF-009 の当たりである）。
**S6 / S8 は机上では決まらないので、受理の前に probe を取り直す**。

### 実機目視の状態

- [統合チェックリスト](../quality/2026-09-22-visual-checklist.md)は **85 項目**のままで、
  **結果が入っているのは 40-3 / 40-5 の 2 行だけ**である（hide が #86 を見つけた記入）。**残り 83 行は空欄**。
- 手順の先頭にあったビルドの SHA `f4e7bcf` は、**この記録で「main の最新をビルドし直す」に直した**
  （#83 / #96 で 2 度積み残した 1 行）。このコミットの時点の main は **`4e57483`**。

### 停止

hide の停止指示で止めた。**ADR 0034 は受理していない**ので、`docs/adr/` にも `docs/todo/` にも
規則 ID にも 1 行も入っていない。再開点は[引き継ぎ](../handoffs/2026-09-23.md)の先頭節。

### 運用の学び（この期間の追加）

- **ADR は probe → 草案 → 批評 → 受理の順で、批評を本文へ織り込んでから受理する。**
  今回は批評の 28 所見を判断まで済ませたところで止まっているので、**再開時はまず本文を書き直す**。
  草案の末尾の判断の節は「まだ本文ではない」ことを表している。
- **机上で決められない所見は受理の前に probe へ戻す**（S6 の `ODS_DEFAULT` と S8 の owner）。
  設計の文に「たぶんこうなる」を残さない。
- それ以外（Issue ごとの agent・姉妹 PR の sync の push・`gh pr edit` が先行 run を cancel すること・
  使用制限で agent が止まっても作業木から再開できること）は 2026-09-22 の日報のままである。

### 次

1. **S6 / S8 の probe** を `out/design/2026-09-24/prompt-theme-probe/` に足す。
2. **ADR 0034 を判断どおり書き直して受理**する。
3. **#82 を実装**（Issue 専任の実装リナ）→ 独立レビュー → merge。
4. **#70**（一致の入れ物のメモリ）→ **#42**（Google Fonts の同梱）。


## 再開後 — 手 3 の記録・運用 ADR 0034・#101〜#103・#82（ADR 0035）を統合

hide の再開指示で、引き継ぎの「順番」どおり (a) 運用 ADR → (b) #101〜#103 → (c) #82 の再開手順を**同じセッションで全部**進めた。
main は **`4e57483` → `9813454`**。設計・裁定・ADR・指示書・受理は設計リナ（Fable）、実装・差し戻し・probe・独立レビューは
**すべて手 3 の型（1 席 1 仕事・道具出力を小さく・最終報告 30 行以内）で立てた Opus の背景席**が行った。

### 統合の表

| 単位 | PR | 統合の手段 | squash 後の main | 閉じた Issue |
| --- | --- | --- | --- | --- |
| 手 3 の記録（引き継ぎ・正本・`docs/IMPL_SEAT_BRIEF_TEMPLATE.md`） | [#107](https://github.com/hideyukiMORI/nene-folio/pull/107) | 手（`gh pr merge`） | `3ccda40` | #106 |
| 運用 ADR 0034（手 1〜3 と施主指示の型・`DEVELOPMENT_WORKFLOW.md` §10） | [#108](https://github.com/hideyukiMORI/nene-folio/pull/108) | **`tools/merge-pr.ps1` の初回の実走**（#110 の枝の版） | `1ae0b91` | #100 |
| #101 `tools/prepare-real-machine.ps1` | [#109](https://github.com/hideyukiMORI/nene-folio/pull/109) | `merge-pr.ps1` | `ad35359` | #101 |
| #102 `tools/merge-pr.ps1`（差し戻し 2 回込み） | [#110](https://github.com/hideyukiMORI/nene-folio/pull/110) | `merge-pr.ps1`（差し戻し 2 の版） | `ba1ea52` | #102 |
| #103 `tools/compare-screens.ps1` / `capture-window.ps1` | [#111](https://github.com/hideyukiMORI/nene-folio/pull/111) | `merge-pr.ps1`（main の版） | `e9dc421` | #103 |
| #82 名前入力面と失敗の箱のテーマ追随（ADR 0035） | [#113](https://github.com/hideyukiMORI/nene-folio/pull/113) | `merge-pr.ps1` | `9813454` | #82 |

`tools/prepare-real-machine.ps1` は統合後に root の main で 1 回実走した（#101 の検証計画）: `{"sha":"e9dc421…","exe":"…\\build\\NeNeFolio.exe","bytes":2803712,…}`・`ninja: no work to do`（製品コードは #82 まで不変だったため）。
`tools/merge-pr.ps1` の実走の JSON は `out/agents/102-accept/merge-1{08,09,10,11,13}.json`。

### 手 3 の型で立てた席（ADR 0034「強制」の記録）

| # | Issue・仕事 | model | 道具呼び出し | tokens（概数） | 報告ファイル |
| --- | --- | --- | --- | --- | --- |
| 1 | #101 実装 | opus | 14 | 78K | `out/agents/101-impl/report.md` |
| 2 | #102 実装 | opus | 13 | 98K | `out/agents/102-impl/report.md` |
| 3 | #103 実装 | opus | 17 | 107K | `out/agents/103-impl/report.md` |
| 4 | #82 probe（S6 / S7 / S8） | opus | 27 | 137K | **harness が Write を拒み無し**（README の追記節が代わり） |
| 5 | #102 差し戻し 1（Issue の状態の再試行） | opus | 9 | 64K | `out/agents/102-fix1/report.md` |
| 6 | #82 probe 2（Enter の直し方 V4〜V6） | opus | 21 | 99K | README の追記節 |
| 7 | #102 差し戻し 2（skipped を完了と数えない） | opus | 7 | 62K | `out/agents/102-fix2/report.md` |
| 8 | #82 実装 A（`dialog_theme` ＋ 名前入力面） | opus | 53 | 171K | 拒まれ無し（最終報告に申し送り） |
| 9 | #82 差し戻し A2（`create` の結果型） | opus | 9 | 65K | — |
| 10 | #82 実装 B（失敗の箱・owner） | opus | 30 | 127K | 拒まれ無し |
| 11 | #82 独立レビュー（読み取り専用） | opus | 16 | 123K | — |
| 12 | #82 probe 3（production の `dialog_theme.c` を一緒にコンパイル） | opus | 25 | 159K | `out/design/2026-09-24/dialog-theme-probe/README.md` |
| 13 | #82 差し戻し R1（枠と印を `dialog_theme` に閉じる） | opus | 11 | 70K | — |
| 14 | #82 差し戻し R2（箱の退避経路） | opus | 11 | 74K | — |

合計 14 席・約 1.43M tokens・**最大の席でも 53 回の道具呼び出し・171K**（hide の 9/22 の集計は 1 席 150〜400 ターン・文脈 350〜800K）。
**Sonnet / Haiku の席は 0**（この日の仕事は実装・差し戻し・probe・レビューで、手 1 の「下ごしらえ」に当たるものが無かった。
probe を Sonnet に落とせるかは次に試す）。SendMessage の使い回しと `fork` は 0。

### #82 — ADR 0035 と実装

- **probe の追試（受理前）**: S6 = `BS_OWNERDRAW` の釦は `WM_GETDLGCODE` が `DLGC_BUTTON` だけで、**Cancel にフォーカスを置いて Enter → IDOK**。`ODS_DEFAULT` は一度も立たない。
  直し方は V4（subclass）が**既定釦の移動で `BM_SETSTYLE` が釦の型を書き換えて owner-draw が止まる**ので却下、**V5（面の `WM_COMMAND` で `GetFocus()` を見て読み替える）**を採用（(c)〜(j) すべて現行の押し釦と同じ値）。
  S7 = `chip_background` と地の対比 5.47 / 8.80:1（`border` は 1.56 / 1.38 で不可）。S8 = ドロワーは `WS_CHILD` なので owner はどちらでも同じ（`GA_ROOT` は意図の明示）。
- **ADR 0035**（草案の 0034 は運用 ADR が番号を取った）: 決定 1〜7 ＋ 実装後の補正 11 項。`failure_box_show` は 3 引数、塗りは `dialog_theme` の 1 本（6 関数 ＋ `dialog_theme_frame`）、結果型 `dialog_theme_outcome`・`failure_box_outcome`。
- **独立レビュー**: 止める 2（S1 箱を作れないと黙る → `MessageBoxW` へ退避して必ず知らせる／S2 EDIT の枠の色が主窓の palette の**参照**から来る → `dialog_theme_frame`）・直したい 6（D3 `ODS_NOFOCUSRECT`・D5 前方宣言を直し、D1 / D2 / D6 は補正に記録）。
- **probe 3**（production の `dialog_theme.c` ＋ `folio_palette.c` を一緒にコンパイル・DPI 120・OS ダーク）: PASS 93 / FAIL 2。FAIL は釦の字の行だけ R/G が 1 段ずれる ClearType の丸め。対比 18 件すべて 4.5:1 / 3:1 以上。最長 en の箱は 600×50（2 行）。
- 分岐網羅 **93.26%（2710 / 2906）**・実ツール反例 17 本。**描いた絵そのものと実機の面・箱は見ていない**（チェックリスト 82-1〜82-10）。

### 新しい Issue

- **#112** ci: Draft で skipped になった必須 check を GitHub が `CLEAN` と見なす。#111 で sync の push（Draft・skipped）→ `ready` の run が cancelled → head に skipped だけが残って `mergeStateStatus = CLEAN` になり、古い版の `merge-pr.ps1` がそのまま merge しかけた（設計席が背景の task を止めた）。`merge-pr.ps1` は差し戻し 2 で skipped を待つが、GitHub 側の穴は残る。判断は hide（QLT の変更）。

### 運用の学び（この期間の追加）

- **sync の merge コミットの件名も GIT-003**（`chore(sync): main を取り込む (#N)`）。`Merge remote-tracking branch …` は CI の `validate-git` で落ちる（#109 で 1 度落とした）。
- **`ready_for_review` の run は同じ PR への次の event に cancel される**（`concurrency.cancel-in-progress`）。Draft の push は job が skipped で、**skipped の必須 check を GitHub は満たしたと見なす**（#112）。
- **`gh pr merge` 直後の `gh issue view` は `OPEN` を返す**（閉じる処理の遅れ。`merge-pr.ps1` は最長 30 秒待つ）。
- **背景席は `out/agents/<issue>-<仕事>/report.md` を書けないことがある**（harness が「subagent は報告ファイルを書かない」と Write を拒む。14 席のうち 3 席）。拒まれたら最終メッセージに申し送りを入れる。テンプレートにその 1 行を足す。
- **Bash の素の pwsh から `validate-git.ps1` を呼ぶと文字化けで GIT-003 が誤って落ちる**。フルゲートの中の結果を正とする。
- **`TaskStop` は待ち手の task を止めるだけで、その task が待っていた背景の task は止まらない**。止めるべきは実体の方。
- 差し戻しは「変更 1 命題 = 新しい席」で 5 回（#102 × 2・#82 × 3）。1 席 7〜11 回の道具呼び出しで済み、**同じ席に SendMessage で足すより安い**ことが数字で見えた。

### 次

1. **hide の判断**: #112（CI の穴）、実機の目視（チェックリストは **95 項目**で結果は 2 行。82-1〜82-10 は「アイコンと音が消える」を含む）、配色の候補。
2. **#70**（`regex_match` × `regex_match_limit` のメモリと出力上限）→ **#42**（Google Fonts の同梱）。probe / 下ごしらえは Sonnet で試す。

### 停止 2

hide の停止指示で止めた。#114 / PR #115 まで統合済み（main `b907b9b`・clean）、背景席 0・未コミットの編集 0。
再開点は[引き継ぎ](../handoffs/2026-09-23.md)の「停止 2」（再開点は「再開点 2」のまま）。次は #70 → #42、判断待ちは #112・実機の目視・配色。


---

> 以下は同じ 2026-09-23 の午後から夕方（14:33〜18:21）。元は誤って `2026-09-25.md` として書かれていた。

日付の扱い: この記録の日付は **2026-09-23**（hide の判断。リポジトリの最新が 09-24 なのでその順に続ける）。
機械の時計は 2026-09-22 で、スクリーンショットのファイル名（`スクリーンショット 2026-09-22 13xxxx.png`）もその日付である。

## 実機目視 1 巡目（#82）

hide の指示は「その運用（ADR 0034）で品質を下げずにコストを下げる。実機テストを立ち上げ、観点一覧を出して確認したいものを聞く」だった。

設計席が `tools/prepare-real-machine.ps1` を root の main で実走した（モデル代ゼロ）。結果:
sha `80e940d810c6f7ce6922ffc7b803611de6a94b5f`（#116 の docs コミット）、exe `build\NeNeFolio.exe` 2,876,416 バイト、13:06 リンク。
`ninja` は `folio_window.c` / `folio_state.c` 等 13 手を再ビルドした。起動は別の 1 行（`Start-Process`）で、PID は 39712。
data は `build\data`（カテゴリ 仕事 / 個人 / 読書・ノート 5 本）。設定は版 3・行番号 ON・ダーク・日本語。機械は表示 125%（120 DPI）・OS ダーク。

観点一覧（[統合チェックリスト](../quality/2026-09-22-visual-checklist.md) 96 項目を 4 群に分けたもの）を出し、hide が選んだのは
**群 1 = #82 の 10 項目**・**環境は 125%・ダークのまま**・**進め方は「設計席が 1 項目ずつ手順を出し hide が結果を返す」**だった。

途中で hide が「この辺の確認はリナが一人でできるんじゃない？僕が確認してリナに渡すのはコストが高すぎる」と判断し、
以後、環境を変えない項目は設計席が自分で窓を動かして撮る運用に変えた（下の「設計席が実機を動かす道具」）。

Computer Use は hide が Claude デスクトップアプリでオンにしたが、ターミナルの Claude Code セッションには道具が来ない
（デスクトップアプリ内のセッションだけ）ため、不要と判断した。

結果は[統合チェックリスト](../quality/2026-09-22-visual-checklist.md)の 82-1〜82-10 の結果欄に写した（82-9 は 150% を今日はやらないので空欄のまま）。
75 番台（75-21）と 76 番台（76-15）にも hide が実機で見つけた 1 行ずつを足した。表は **96 項目**、結果が入っているのは
**82 番台 9 行と 75-21 / 76-15 の合わせて 13 行**である。

要点:

- 82-1〜82-2・82-4〜82-6・82-10 は期待どおり。
- 82-1〜82-3: **コンボの閉じた面とリストの外周の枠が OS の色のまま** → #118（hide の所見「他の GUI と統一感が取れていない」→ 自前の部品にする）。
- 82-7: 失敗の箱はアイコン無し・音無しで palette どおり開閉できたが、**hide の判断で音と印（△に !）が欲しい** → #119。**保存できないノートから抜ける道が分からない** → #120。
- 82-8: English の 1 行は箱に収まったが、**最長の en（INVALID_NAME・131 単位）は未確認**。`:saveas` 面の中の失敗の 1 行は 2 行に折り返した。
- 75-21: 設定画面からライトにしても**ドロワーの絞り込みの欄だけダークの地のまま** → #121。原因は主窓が
  `WS_CLIPCHILDREN` で親の無効化が子の EDIT に届かないこと（PR #124 で欄を明示的に無効化）。
- 76-15: English で 1200 幅の窓の「操作 ▾」が「Act...」に省略される → #122。

## 新しい Issue

| # | 題 | 種類 | hide の判断 |
| --- | --- | --- | --- |
| #118 | 名前入力面の保存先カテゴリのコンボを自前の部品にし、矢印・枠・帯・スクロールを palette で描く（ADR 0035 の補正） | enhancement | A（自前の部品）。B 枠だけ／C 現状 は却下 |
| #119 | 失敗の箱に警告音と三角の「!」の印を戻す（ADR 0035 の補正） | enhancement | 音は欲しい・印も欲しい。設計席の判断で三角（以前の `MB_ICONWARNING` と同じ意味） |
| #120 | 保存できないノートから抜ける道が無い。`:e!` と保存失敗の文言に出口 | bug | hide の指摘。箱に選択肢は付けない |
| #121 | 設定画面からライトにすると絞り込みの欄だけダークの地のまま | bug | 当たり: `recolor_window` が欄の EDIT を無効化していない（未確認） |
| #122 | English で「操作 ▾」が「Act...」に省略 | bug | 釦の幅が `action_button_rect` の固定表 `{104,52,52,70,160}` |
| #123 | この記録 | documentation | — |

**着手の順（hide の判断）: #121 #122 → #120 → #119 → #118 → #70 → #42。**
#121 と #122 は 2026-09-23 のうちに Opus の実装席（S 級・1 席 1 仕事）を立てた。

## 設計席が実機を動かす道具

- 失敗した手: `SetForegroundWindow` ＋ `SendKeys`。背景プロセスからの前面化を Windows が拒み、
  キーが**別のアプリ（Claude デスクトップの新規チャット欄）に入った**。全画面 `CopyFromScreen` は 8338×2880 の
  全モニタと他のアプリの窓まで写す。**二度と使わない。**
- 使える手: scratchpad の `post.ps1`（`GetGUIThreadInfo` でアプリのフォーカスの子窓を取り `PostMessage` で
  `WM_CHAR` / `WM_KEYDOWN` を投げる。フォーカスが非表示ならルートへ。記号 `{ENTER}` `{ESC}` `{TAB}` `{CTRL+P}`=`WM_CHAR 0x10`
  `{CTRL+S}`=`0x13` `{DOWN}` `{UP}`）と `capwin.ps1`（プロセスの可視トップレベル窓を列挙して `PrintWindow` で 1 枚ずつ PNG。
  失敗の箱と主窓は題が同じ「NeNe Folio」なので題では区別できない）。
  Ex で状態はほぼ全部変えられる（`:set theme=`・`:set language=`・`:saveas`・`:rename`・`:set number`）。
- 分かった癖: 閉じた Ex の欄（非表示の EDIT）がフォーカスを持ち続けることがある／モーダルの面が開いていると
  投げた文字は面の名前欄に入る（不正な名前なので保存はされない）／面を Esc で閉じた後、主窓へ投げたキーが
  処理されなくなった 1 回があり原因は未特定。
- 3 回目に使うなら `tools/` のスクリプトにする（ADR 0034 決定 2）。今日は 2 回目。
- **`PrintWindow` で撮った絵は子窓を描き直して写すので、描き直しの欠陥の確認には使えない。画面そのものを撮る。**

## 運用（背景席の表）

| # | Issue・仕事 | model | 報告ファイル |
| --- | --- | --- | --- |
| 1 | #123 記録（この日報・引き継ぎ・チェックリスト・正本） | sonnet | harness が Write を拒み無し（最終報告の申し送り）。道具呼び出し約 57 回・約 133K tokens |
| 2 | #121 実装 | opus | out/agents/121-impl/report.md。約 29 回・約 86K tokens |
| 3 | #122 実装 | opus | out/agents/122-impl/report.md。約 23 回・約 97K tokens。probe は out/design/2026-09-25/action-width-probe/ |

Sonnet の席はこれが初である。3 席とも 1 席 1 仕事で、差し戻しは 0。

統合（root の main で `tools/merge-pr.ps1`）:

| PR | Issue | squash 後の main |
| --- | --- | --- |
| #124 | #121 | `a2538ba` |
| #125 | #122（sync `chore(sync): main を取り込む (#122)` を 1 回） | `0463cbb` |

**受理で拾ったこと**: (1) #121 の席の指摘で、設計席の「Ex では欄が正しく明るかった」という観察を取り消した（PrintWindow の作り物）。(2) `merge-pr.ps1` は PR 本文の欄「振る舞い・スキーマの変更:」と行頭が欄名で始まる形を求めるが、指示書テンプレートに欄の一覧が無く、3 本とも設計席が本文を直した。テンプレートに 1 行足した（この PR）。(3) #122 の判断: 「実測 ＋ 一様な余白」と「ja は 2px 以内」は両立しないので、旧幅表を日本語の下限に残し、字が広い言語だけ広げる形を受理した。English の 560 幅では既存の縮退で「ヘルプ」が出ない。

## 後始末と判断待ち

実機の後始末:

- 仕事/test11.md の読み取り専用は外した（設計席）。`build\data` に hide の試験ノート 仕事/test11.md（本文「11」）が残っている。
- 設定は `theme: light` のまま（hide がダークへ戻す）。言語は ja。
- `NeNeFolio.exe`（PID 39712）は起動したまま。

判断待ちのまま残るもの: #112（CI の穴）、82-9 と 150% / 100% の項目、OS のライト／ダーク切替の項目、简体中文の字面、配色の候補。

## 次

**#121〜#118 は統合済み。次は #70（一致の入れ物のメモリ。現物調査は Sonnet の席で 2026-09-23 のうちに開始）→ #42（同梱フォント）。**

## 後半 — #120 / #119 / #118 の設計と統合

### 統合の表

すべて root の main で `tools/merge-pr.ps1`（BEHIND のときは `chore(sync): main を取り込む (#N)` を 1 回）。

| 順 | Issue | PR | squash 後の main | 備考 |
| --- | --- | --- | --- | --- |
| 1 | #121 絞り込み欄の色 | #124 | `a2538ba` | 原因は主窓の `WS_CLIPCHILDREN`（親の無効化が子の EDIT に届かない）。`recolor_window` で `filter_input` を明示的に無効化 |
| 2 | #122 頭の釦の幅 | #125 | `0463cbb` | 旧幅表を日本語の下限に残し、字面の実測が広い言語だけ広げる。sync 1 回 |
| 3 | #123 記録（前半） | #126 | `44cb483` | チェックリスト・日報・引き継ぎ・正本・指示書テンプレート（PR 本文の欄の 1 行） |
| 4 | #120 `:e!` | #127 | `f523073` | ADR 0016 補正 4。core / application / ui / tests / GLOSSARY。分岐 93.27%（2718 / 2914） |
| 5 | #119 音と印 | #128 | `203cc49` | ADR 0035 補正 12〜16。sync 1 回。分岐 93.26%（2710 / 2906） |
| 6 | #118 一覧 | #129 | `49042a3` | ADR 0035 補正 17〜21。`name_prompt.c` と GLOSSARY のみ。面は 96 DPI で 400×270 → 400×334。probe PASS 56 / FAIL 0。分岐 93.27% |

**main は `49042a3`（clean）。開いている Issue は #112 / #70 / #42 と、この記録の #130 だけ。** #118〜#123 はすべて閉じた。`out/worktrees/` の統合済みの作業木はすべて消した。

### 設計の判断（ADR の補正）

- **ADR 0016 補正 4（#120）**: 語 `e!`（別名 `edit!`）・表示名「編集を破棄して読み直す」・パレットとメニューにも出る・確認の箱は無し。application の `folio_state_discard_edits` は前提の判定だけで状態を変えない（「未保存の印」は独立した bool ではなく `folio_state_note_changed` の毎回の比較。`state->body` は保存の失敗で動かない）。UI は `show_note` と同じ経路で `state->body` を流し込み直す。**ディスクは読み直さない**（vim と違う）。失敗の文言 `NOTE_STORE_FAILED` に「別名で保存するか、編集を破棄して読み直せます。」、`HISTORY_FAILED` に「編集を破棄して読み直せます。」を 3 言語で足した。箱に選択肢は付けない。Issue #120 の「並替・色も先に保存を試みる」は誤りで、実際に先に保存するのは切替・新規・終了確認（と Ctrl+S / Esc / `:w` 系）だけ。probe で RichEdit 5.0 の `EM_STREAMIN` は Undo を空にすると分かり、`EM_EMPTYUNDOBUFFER` は足していない。
- **ADR 0035 補正 12〜16（#119）**: `MessageBeep(MB_ICONWARNING)` を箱が組み上がった直後に 1 回、退避の `MessageBoxW` も `MB_ICONWARNING`。印は `ICON_PAINT_WARNING`（三角 ＋ 縦棒の穴 ＋ 点の穴、歯車と同じ alternate 塗り）を `failure_box.c` の新設 `WM_PAINT` から `icon_paint_fill` で描く。24px・本文 1 行目の中央・色は `chip_background`（対比 ライト 8.80 / ダーク 5.47）。折り返し幅は 480 → 440。probe PASS 61 / FAIL 0、最長 en は 3 DPI とも 2 行、箱の大きさは #82 と同じ 650×155（DPI 120）。
- **ADR 0035 補正 17〜21（#118）**: 現物調査で「操作 ▾」の一覧は OS の `TrackPopupMenu`（自前描画ではない）と分かり、面に浮く一覧の前例も無いので、**開閉するプルダウンをやめ、常時開いた owner-draw の LISTBOX（4 行・枠は名前欄と同じ）**にする提案 E を採った。probe（`out/design/2026-09-25/category-list-probe/`）PASS 47 / FAIL 0: `dialog_theme_draw_item` は無改修で行を描け、Enter は既定の釦（保存）に落ち、Tab / Esc / Space は V5 のまま、無効化でも白く戻らない。ダブルクリックは保存に読み替える。残る OS の部品はカテゴリ 5 件以上のときのスクロールバーだけ。hide には提示済みで異論なし（返事は無し）。

### 実機の確認（設計席）

設計席が窓ハンドルへキーを投げて `PrintWindow` で撮った。配置と文言の確認に使い、描き直しの欠陥の確認には使わない。

- #122: main `0463cbb` で English「Actions ▾」と 简体中文 の 5 釦が全文（76-15 に記入済み・PR #126）。
- #120: main `f523073` で、読み取り専用の test11 を編集 → Ctrl+S → 新しい文言の箱（2 行）→ パレット「編集を破棄して読み直す（edit! e!）」→ 本文が保存済み（空）に戻り編集モードのまま。**気づき**: パレットで「破棄」と打つと既存の「未保存変更を破棄して終了」（`:q!`）が先に並び、Enter でアプリが終了する（設計席が 1 度そうなった）。欠陥ではないが、終了の命令が一覧の先頭に来る並びは危うい → 判断待ちに載せる。
- #119: #128 統合後の main で同じ手順の箱に三角の「!」が札の橙で 1 行目の中央に出て、文言は 2 行。**音は設計席には聞こえないので hide が確認する**（82-7 の結果欄は「印は設計リナが確認・音は未確認」）。
- #118: #129 統合後の main `49042a3` で「別名で保存」（ダーク）と `F2` の改名の面を撮った。保存先カテゴリは常時開いた 4 行の一覧で、外周は名前欄と同じ橙の枠、選択の帯は palette、4 行目の余白は欄の地。改名では一覧が無効で字と帯が薄くなり白く戻らない。矢印の釦と OS の枠は消えた。**ライトの面は撮れていない**（面を閉じた後に投げたキーを主窓が受け取らなくなる癖）→ hide の目視（82-2）に残す。
- #121: **画面そのものはまだ見ていない**（窓が他の窓の下にあり、前面化は使わない）。hide が窓を前に出して設定画面からライト → ダークと切り替え、絞り込み欄が追随すれば合格（75-21 の結果欄は「修正は統合済み・実機は未確認」のまま）。

### 運用（背景席の表・後半）

| # | Issue・仕事 | model | 道具呼び出し | tokens（概数） | 報告 |
| --- | --- | --- | --- | --- | --- |
| 4 | #120 現物調査 | sonnet | 63 | 134K | out/agents/120-probe/memo.md（98 行・疑問 15） |
| 5 | #120 実装 | opus | 58 | 156K | 報告ファイルは harness が拒否（申し送りで受理）。Draft PR #127 |
| 6 | #119 現物調査 | sonnet | 25 | 105K | out/agents/119-probe/memo.md（67 行・疑問 6） |
| 7 | #118 現物調査 | sonnet | 31 | 116K | out/agents/118-probe/memo.md（67 行・疑問 12） |
| 8 | #118 probe（LISTBOX 案） | sonnet | 55 | 187K | out/design/2026-09-25/category-list-probe/README.md（PASS 47 / FAIL 0） |
| 9 | #119 実装 | opus | 29 | 154K | 報告ファイルは harness が拒否。Draft PR #128 |
| 10 | #118 実装 | opus | 47 | 180K | 報告ファイルは harness が拒否。Draft PR #129 |
| 11 | この記録（後半） | sonnet | （この席） | | |

前半（#123 の日報に記載）: #121 実装 opus 29 回 86K・#122 実装 opus 23 回 97K・#123 記録 sonnet 57 回 133K。
**この日の Sonnet の席は 5 席（現物調査 3・probe 1・記録 2 のうち後半 1）で、いずれも設計席が照合して直せる仕事**。Sonnet の probe（#118）は Opus の probe と同じ足場（`dialog-theme-probe`）を写して PASS 47 の表を返し、実装席がそのまま使えた。差し戻しは 0。Opus の実装席は 4 席とも 1 席 1 仕事で最大 58 回・156K。
**実装席の 4 席のうち 3 席で harness が報告ファイルの Write を拒み**、申し送りで受理した（テンプレートの想定どおり）。

### 運用の学び（後半）

- `merge-pr.ps1` は PR 本文の各欄が**行頭が欄名で始まる 1 行**であることを求める（`## 目的:` は不可・`振る舞い・スキーマの変更:` も必須）。テンプレートに 1 行足した（PR #126）。
- 実機の確認は設計席が `PostMessage` で窓ハンドルへキーを投げ、`PrintWindow` で窓ごとに撮る。`SetForegroundWindow` ＋ `SendKeys` は他のアプリにキーが入るので使わない。`PrintWindow` は子窓を描き直して写すので**描き直しの欠陥の確認には使えない**。
- パレットの部分一致は catalog の順で並ぶ。破壊的な命令（`:q!`）が先頭に来る語がある。
- 統合済みの作業木は消した（`out/worktrees/` は `118-category-list` だけが残る見込み）。

### 判断待ち

#112（CI の穴）、パレットで `:q!` が「破棄」の先頭に来る並び、82-9 と 100% / 150% の項目、OS のライト／ダーク切替の項目、简体中文の字面（今日足した 3 文を含む）、配色の候補、#121 と #119 の音の実機確認、#118 統合後の名前入力面の目視（82-1 / 82-3 / 82-4）。

### 次

**#70（一致の入れ物のメモリ。現物調査は Sonnet の席で 2026-09-23 のうちに開始）→ #42（同梱フォント）。**

## 終盤 — hide の実機確認・#70 / #134・#132

### hide の実機確認（4 件）

main `49042a3` のビルド（DPI 120・ダーク・日本語）で hide が確認した。

- 82-7（#119）: 読み取り専用の test11 を編集 → Ctrl+S。警告音が鳴り、三角の「!」の印が出た。hide「全部オッケー」。
- 75-21（#121）: 歯車 → ライト → 左上の「すべてのノートを検索」の欄が明るい地に追随、ダークに戻すと暗い地に戻る。OK。
- 82-2（#118）: ライトの「別名で保存」の面。保存先カテゴリは 4 行の一覧、枠は名前欄と同じ色、灰色の矢印と白い枠は無い。OK。
- パレットで「破棄」と打つと `:q!` が先頭に来る並び: hide は現状で可と判断（Issue にしない）。

### 新しい所見（hide）→ #132

設定画面でテーマを変え、Ctrl+P でパレットを出し、Esc で設定画面へ戻ったあと、キーもマウスも受け付けなくなることがある、という所見。設計席の証拠: Ex 実行後に非表示の EDIT がフォーカスを持ち続けていた（`GetGUIThreadInfo`）・面を閉じた後に主窓へ投げたキーが処理されなくなった（2 回）。Sonnet の現物調査席を立てた（out/agents/132-probe/memo.md）。

### #70 の実測と 2 本の統合

実測（out/design/2026-09-25/replace-memory-probe/）: 1MB `\w+`（169,120 件）+77.5MB、900KB `.`（907 千件）+403.6MB、1MB `.`（TOO_MANY）+0.65MB。ADR 0028 補正 24〜26 で上限を 1,000,000 → 200,000（1 写し 46.4MB）に下げ、写しを 1 つにするのは 2 本目、疎な群は却下と決めた。

1 本目 #133（`regex_match_limit` を 200,000 に下げる）: main **`30377d2`**。再実測は 180KB `.`（181,292 件）+81.2MB（机上と 1.3% 差）、900KB `.` は TOO_MANY（+0.64MB）、1MB `.` の打ち切りは 30.5ms → 7.6ms。Issue #70 は閉じ、2 本目を #134 として起票した。

2 本目 #134（下見が一致の配列を引き取り、定常の写しを 1 つにする・ADR 0028 補正 25）: PR #135 → main **`84d94f4`**。180KB `.` は +41.1MB（1 写し ＋ 2.5%）。`found` は `_Nullable` になり下見の成功で空に戻る。分岐 93.29%（2713 / 2908）。残る癖: 64 件超の一致は打鍵ごとに走査が 2 周。

席: #70 現物調査 sonnet 25 回 95K・#70 メモリ probe sonnet 58 回 199K・#70 実装 a opus 30 回 99K・#134 実装 opus 36 回 149K。

### #132 の原因と修正

現物調査（sonnet 36 回 128K・out/agents/132-probe/memo.md 182 行）: `apply_theme` / `apply_language` / `apply_number` が Ex から成功したとき `hide_command_surface` だけを呼び `SetFocus` を返さない。隠れた `command_input` がフォーカスを持ち続け、次に面を開くとき `command_return_focus` に再記録されて伝播する。面の重ねは 1 段（設定画面の上でパレット → Esc は設定画面を開く前の窓へ戻る）。マウスが効かない原因のコードは見当たらない（キーが効かないので無反応に見えた可能性がある）。

設計: ADR 0016 補正 5（入力面を隠す 1 か所で可視の窓へフォーカスを返す・戻り先に非表示の窓を記録しない・重ねは変えない）。

実装（opus 65 回 137K・out/agents/132-impl/report.md）: PR #136 → main **`11c6da3`**（sync `chore(sync): main を取り込む (#132)` 1 回）。`surface_focus_region` / `surface_return_target` の 2 関数を足し、`hide_command_surface` が面にフォーカスがあれば可視の戻り先（無ければ区画）へ返す。`open_command_surface` は非表示の窓を戻り先に記録しない。`close_command_surface` も同じ規則。`apply_*` は不変。

probe（out/design/2026-09-25/surface-focus-probe/README.md・別プロセスの exe・PostMessage）: 修正前は Ex の `:set` のあと 5/5 で非表示の Edit にフォーカスが残り次の命令 0/5、修正後は 3 経路 × 5 回 = 15/15 で可視・有効（補足の経路 d も 5/5）。**設定 → テーマ → Ctrl+P → Esc の素の経路は修正前でも再現せず**、hide の症状は直前に設計席が同じプロセスへ Ex の `:set theme=` を投げて固着していたことと整合する。マウスが効かない別原因は未調査（キーが効かないので無反応に見えた可能性がある）。

### 運用（背景席の表・終盤）

| # | Issue・仕事 | model | 道具呼び出し | tokens（概数） | 報告 |
| --- | --- | --- | --- | --- | --- |
| 12 | #70 現物調査 | sonnet | 25 | 95K | out/agents/70-probe/memo.md |
| 13 | #70 メモリ probe | sonnet | 58 | 199K | out/design/2026-09-25/replace-memory-probe/ |
| 14 | #70 実装 a（#133） | opus | 30 | 99K | Draft PR #133 |
| 15 | #134 実装 | opus | 36 | 149K | Draft PR #135 |
| 16 | #132 現物調査 | sonnet | 36 | 128K | out/agents/132-probe/memo.md（182 行） |
| 17 | #132 実装 | opus | 65 | 137K | out/agents/132-impl/report.md |
| 18 | この記録（終盤） | sonnet | （この席） | | |

この日の Sonnet の席は合わせて 8 席（現物調査 5・probe 2・記録 1 ＋この記録）で差し戻し 0。Opus の実装席は 8 席（#121 #122 #120 #119 #118 #70 #134 #132）で差し戻し 0、最大 65 回・180K。

### 運用の学び（終盤）

- 設計席が実機を動かすときは `post.ps1` / `capwin.ps1`（`PostMessage` と `PrintWindow`）を使う。`SetForegroundWindow` / `SendKeys` は禁止。`post.ps1` の「フォーカスが非表示なら主窓へ投げ直す」規則は欠陥を隠すので、フォーカスの検証には `-Raw` の写しを使う。
- 件名と PR の題は 100 文字以内（CI は枝の各コミットの件名も検査する）。
- `merge-pr.ps1` は本文の欄が行頭に来ること・`Closes #N` を要求する。

### 判断待ち

#112（CI の穴）。82-9 と 100% / 150% の項目、简体中文の字面、配色の候補。パレットで `:q!` が先頭に来る並びは hide が現状で可と判断したので判断待ちから外れた。

### 次

**#42（Google Fonts の同梱）。#112 は hide の判断待ち。**

## 夕方 — #42 同梱フォント（ADR 0036・単位 A / B / C・差し戻し C1）

### hide の判断

資産はリポジトリに入れる（取得スクリプトは作らない）・exe に埋め込まず隣の `fonts\` から読む・書体は 2026-09-12 の選定（Noto Sans JP / SC・Arimo、Regular / Bold、静的）のまま。

### 現物調査と probe

現物調査（sonnet 29 回 99K・out/agents/42-probe/memo.md 169 行）: 言語 → face の切替と退避は ADR 0032 で実装済み。`out/fonts/2026-09-12/` に 6 ファイル（27,028,564 バイト）＋ライセンス 2 ＋ manifest があり、SHA-256 は 2026-09-23 に照合して一致。

probe（sonnet 50 回 168K・out/design/2026-09-25/font-bundle-probe/）: 6 ファイルの `FR_PRIVATE` 登録は合計 27ms、GDI / RichEdit / 別スレッドから見える。行高は現行より 1〜2px 高く、RichEdit の行間は最大 6px 広い。JP と SC の字形差は 1.46%。限界: この機械の OS に `NotoSansJP-VF.ttf` があり同名の衝突込みの実測。SC と Arimo の漢字が同じ画素で、同梱の SC が選ばれているかは `GetFontData` で確かめる必要がある（単位 B の受け入れ条件にした）。

### ADR 0036 の骨

受理（枝の先頭コミット `d54f9fc`）。決定 1 資産は `fonts/`、決定 2 CNF-012（active）、決定 3 adapters の `font_bundle`、決定 4 `ui_font` の 2 列（同梱の face・OS の退避 face）と 3 段の退避、決定 5 読めなければ OS の書体で起動し 1 回知らせる、決定 6 CMake の POST_BUILD 複写と `gdi32` を adapters の platformLibraries に、決定 7 単位 A / B / C、決定 8 やらないこと（exe への埋め込み・OS へのインストール・可変フォント・等幅の同梱など）。QLT-011（依存の再現性）は `planned` のまま。

### 単位 A / B / C と差し戻しの表

| Issue | 仕事 | PR | main の SHA | 席 |
| --- | --- | --- | --- | --- |
| #139 | 単位 A（資産と検査） | #141 | `4886f43` | opus 31 回 112K |
| #140 | 単位 B（登録） | #142 | `7517304` | opus 67 回 182K |
| #42（単位 C） | 表の差し替え | Draft #143 | （差し戻し前） | opus 44 回 124K |
| #42（差し戻し C1） | English の face を直す | #143 に 1 コミット `b2c35ba` | `4a150de` | opus 16 回 84K |

単位 A: `fonts/` 10 ファイル・`.gitattributes`・CNF-012・実ツール反例 2 本（17 → 19）・`QUALITY_GATES.md`・`gate-proofs.md`・CMake の POST_BUILD。製品コードは不変。

単位 B: `src/adapters/win32/font_bundle.{h,c}`（`FR_PRIVATE` 登録・解除）。`enum font_bundle_outcome` と判定の純関数は **core** に置いた（ui は adapters を include できない・単体の依存の都合。ADR 0036 補正 1）。`main.c` が窓を作る前に登録し、メッセージループのあとに解除する。`FOLIO_STATE_FONT_BUNDLE_UNAVAILABLE` と 3 言語の文言を足し、箱は `folio_window_create` の最初の描画のあとに出す（補正 2）。`gdi32` を adapters の platformLibraries に追加、ARC-004 の所有者の表に 1 行。分岐網羅 93.30%。identity probe: 登録後は SC / JP / Arimo の 3 face とも `GetFontData` の `name` テーブルが同梱ファイルと一致し、JP は登録前が OS の可変フォント・登録後が同梱の static だった。実 exe で `fonts\` を退避すると失敗の箱が出ることも別プロセスで確認した。

単位 C: 退避の表を別ファイル `src/core/ui_font_fallback.c` に置いた（CNF-009 が表ごとに網羅を数えるため。補正 3）。`ui_font.c` を同梱の face の表に差し替え、3 段の順は純関数 `ui_font_pick`、`ui_face_for` は実在の確認だけを持つ。分岐網羅 93.31%。

### 設計席が絵で見つけた後退と直し

単位 C の Draft PR #143 で撮った絵（`shots/c-ja-0 / c-en-0 / c-zh-0.png`）を設計席が見て、**English で日本語のノート名の字間が空く後退**を見つけた（Arimo に漢字が無いため、日本語のノート名だけ別の書体に束ねられる）。hide の判断（案 1）: **English も Noto Sans JP にし、Arimo は同梱から外す。**

差し戻し C1（opus 16 回 84K）: PR #143 に 1 コミット `b2c35ba`。EN の同梱 face を `Noto Sans JP` にし、Arimo の 2 ファイルと `OFL-Arimo.txt` を `git rm`。manifest は 4 entry（合計 26,063,980 バイト）＋ライセンス 1 本になった。`prove-gates.py` の反例 P24 を `NotoSansJP-Regular.otf` に向け直し、ADR 0036 に補正 5、`GLOSSARY` と `gate-proofs.md` を更新。フルゲート exit 0・分岐網羅 93.31%・実ツール反例 19 本。絵 `c-en-1.png` を設計席が見て、English で日本語のノート名の字間が揃ったことを確認した。**残る癖**: CMake の POST_BUILD の複写は古いファイルを消さないので、古い `build/fonts/` に Arimo が残りうる（設計席が実機をビルドし直す前に `build/fonts` を消す）。

### 運用（この日の席の合計）

この日の Opus の実装席は合わせて 12（#121 #122 #120 #119 #118 #70 #134 #132 #42 単位A #42 単位B #42 単位C #42 差し戻しC1）、Sonnet の席は 12（現物調査 6・probe 4・記録 3。うちこの記録 #144 は進行中）。差し戻しは #42 の 1 回（設計席が絵を見て見つけた）で、他はすべて差し戻し 0。

### 判断待ちと次

hide の要望次第。仕様の未実装で残るのは履歴の閲覧／復元 UI・外部で変わった md の検知・`:s` の行範囲・確認付き置換・全ノート一括置換・カテゴリを変える改名・壊れた `.rename.json` を画面から直す経路・検索語などの永続化。**#112 は hide の判断待ちのまま。**

---

## 夜 — #112（必須 check の判定 job・ADR 0037）と日付の訂正（#147）

hide の再開指示のあと、仕様の未実装の一覧と #112 の候補 3 つを提案し、hide の「推奨で」で候補 (b) を採った。
`check.yml` の job を `full-gate`（Draft では skipped のまま）と判定 job `check`（`needs: [full-gate]`・`if: always()`・
`success` 以外は `exit 1`）に分け、ruleset は触っていない。ゲートそのものの判断なので ADR 0034 の決定 1 に従い設計席が自分で書いた（YAML 約 20 行・製品コード 0 行）。
PR #146 で実測: Draft の push 2 回（run 35843768164・35843898385）とも `full-gate` = skipped・`check` = failure・`BLOCKED`、
同じ head を Ready にすると（run 35844073589）両 job success・`CLEAN`。QLT-012 を active にし、CNF-006 の要求で `gate-proofs.md` に行を足した
（最初のローカルのフルゲートはこの行が無くて落ちた）。`tools/merge-pr.ps1` で main `7edf030` へ統合し、#112 は閉じた。

そのあと hide が「今日は 23 日」と指摘し、文書の暦が 09-22 の夜から 1〜3 日進んでいたと分かった（設計席も #112 の記録を 09-26 と書いていた）。
hide の判断「1 で」（全面訂正）に従い、#147 で日報・引き継ぎを実日ごとに 1 本へ統合し、確認記録のファイル名・ADR の日付・本文 39 ファイルを実日付へ直した。
直せないもの（コミット件名 6 本・`out/design/2026-09-2x/` の実測ディレクトリ名）は[正誤表](ERRATA.md)に残し、
「日付は実時計から取る」を DEVELOPMENT_WORKFLOW に足した。**開いている Issue は #147 のこの記録だけで、次の単位は hide の指示待ち。**

---

## 夜 2 — #149 設定画面の行番号・#152 統合スクリプト・#156 撮影の道具・#154 履歴を戻す（ADR 0038）・#155

hide の「対応を進めて」で、提案の順（設定画面の行番号 → 履歴を戻す UI）を進めた。

- **#149**（PR #150 → `db546da`）: 設定画面に 3 段目「行番号」（表示する／表示しない）。採用は既存の `apply_number` の 1 本で、設定画面から採用したときは面を閉じない（ADR 0031 補正 4）。
  設計席が枝の exe を動かし 149-1〜149-3 を絵で確認（简体中文も）。
- **#152**（PR #153 → `4d633bb`）: ADR 0037 の副作用。Ready 直後に前の Draft の run の `check` failure が残り `merge-pr.ps1` が止まった（#150 で実測）。
  check-run を `check_suite.id` で名前ごとの最新に絞り、pending が残るあいだは failure で止まらないようにした。
- **#151 → 閉じた（再現せず）**・**#156**（PR #157 → `696707b`）: 「パレットが送られない」は席製 `capwin.ps1` の DPI 非対応が 125% の画面で窓の右下 20% を切った撮影の産物だった
  （#151 の席が DPI 対応の probe で証明）。`tools/capture-window.ps1` に `-TargetPid`、キーを投げる `tools/post-keys.ps1` を正典に。初回のゲートは件名 103 字で GIT-003 に落ち、90 字に直した。
- **#154 履歴を戻す（FR-033・ADR 0038）**: 現物調査（Sonnet）→ ADR 0038（戻す＝未保存の編集にする・ファイルは触らない・確認の箱なし・時刻なし）→
  単位 A（PR #159 → `0cdc984`・#158。port `read_history` は宛先を `history_version` に束ねて 3 引数・application 6 本・結果値 2 つ・単体と確保失敗・実アダプタ probe 19/19）→
  単位 B（PR #160 → `120c988`。操作「履歴から戻す」`:history` / `:hist`・パレット型の面・`note_pane_replace` の全文 span で Undo 1 単位）。
  単位 A の席は ui の全値 switch で正しく止まり（ui は編集禁止にしていた）、case 2 行は設計席が足した。設計席の絵: 一覧（4 版・最初の空でない行・CRLF の版）・`:hist` の 1 行「履歴はありません。」・
  戻した後の編集モード・保存後の `1.md` の繰り上がりを確認。**Ctrl+Z 1 回・歯抜け・560×360 は未**。
- **#155**（PR #161）: 低い窓でパレットの案内が絞り込み欄に塗り潰される。原因は欄に `WS_CLIPSIBLINGS` が無いこと（z 順は正しかった）。1 行の直し。設計席が修正後の絵で確認。

背景席（すべて手 3 の型）: Opus 実装 5（#149・#154 A・#154 B・#151 調査・#155）、Sonnet 3（履歴の現物調査・`read_history` probe）。差し戻し 0。設計席が直接書いたのは #152 のスクリプト・#156 の道具・ADR・ui の 2 行。
学び: (1) squash で入った単位 A の元コミットを持つ枝は main と衝突しうる（ADR の補正の節で 1 件・解いて再走）。(2) 撮影の道具は DPI 対応で PID を指定する。(3) 編集モードの本文に `:` は文字として入る（Ex は索引か閲覧から）。

---

## 停止 — hide の指示（夜 2 のあと）

hide に「特に僕の裁定が必要なものは無いね？」と問われ、**無い**と答えた（開いている Issue・PR は 0）。設計席が自分で決めて通したのは ADR 0038 の 4 点
（戻す＝未保存の編集にする・確認の箱なし・版の見出しは版番号と最初の行だけで時刻なし・Ex は `:history` / `:hist`）で、hide が違うと思えば変える。
次の提案の単位「ノートの削除とカテゴリの作成／削除／改名」は仕様に無いので、選ぶなら hide の裁定（削除の行き先・右クリックの有無など）から始める。
hide の指示で区切って停止した。**明示的な再開指示まで続行しない。** main は #162（PR #165 → `5221423`）とこの記録。作業木は 42 本すべて統合済みの枝。背景席 0。
hide の目視に残るもの: 149 番台（English の絵は未）・154-2（歯抜け）・154-4（Ctrl+Z 1 回）・154-6（560×360）。実機は `4a150de` のままなので main をビルドし直す（`build/fonts` の古いファイルは先に消す）。
