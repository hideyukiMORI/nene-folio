# いまのタスク — NeNe Folio

> GitHub Issue が正。ここは要約であり、Markdown のチェックリストをタスク状態として扱わない。
> 更新は実測でだけ行う（「動くもの」は `pwsh -NoProfile -File ./eng/check.ps1` が通ったもの）。

## 段階

| 段階 | 状態 |
| --- | --- |
| Phase 0 言語の実測 | ✅ 2026-09-09（69 ケース・`docs/quality/phase0-results.json`） |
| Phase 1 文書とゲートの足場 | ✅ 2026-09-09（Issue #1・フルゲート緑） |
| Phase 2 negative proof | ✅ 2026-09-09（17 行・`docs/quality/gate-proofs.md`。ruleset と CI の実行証拠は PR #2 で取った） |
| Phase 3 縦切り 1 本 | ✅ 2026-09-09（Issue #3・ADR 0004。core / application / adapters / ui / app と tests/unit。ARC-002 / ARC-003 / ARC-007 / QLT-009 が active） |
| Phase 3 縦切り 2 本目（トグルと保存） | ✅ 2026-09-09（Issue #5。意図 → reducer → 書き込みポート → 原子的な置き換え） |
| Phase 3 縦切り 3 本目（右ペインの閲覧） | ✅ 2026-09-09（Issue #7。md → RTF のサブセット変換・note_text・RichEdit のビュー） |
| Phase 3 縦切り 4 本目（見た目とテーマ） | ✅ 2026-09-09（Issue #9・ADR 0005。案2「堅」・OS に従うライト／ダーク） |
| Phase 3 縦切り 5 本目（編集と保存） | ✅ 2026-09-10（Issue #11・ADR 0006。札 2 つ・平文編集・Ctrl+S と抜けるときの原子的な書き戻し） |
| Phase 3 縦切り 6 本目（ドラッグの並び替え） | ✅ 2026-09-10（Issue #13・ADR 0007。ドロップ先は core の純関数・`index.json` への初めての書き戻し） |
| Phase 3 縦切り 7 本目（別カテゴリへの移動） | ✅ 2026-09-10（Issue #15・ADR 0008。md の rename が先で両方の `index.json` が追随・同名は拒む） |
| Phase 3 縦切り 8 本目（ドロワーのスクロール） | ✅ 2026-09-10（Issue #19・ADR 0009。上限と表示座標は core・量の所有者は application・上下端のフェードは自前の合成） |
| Phase 4 公開 | 🔲 |

## 動くもの

- 起動して、実行ファイルと同じ場所の `data/` を走査し、`categories.json` / `index.json` と照合した索引を枠なし窓の左ドロワーに描く（96 DPI・1 モニタで実機確認。`docs/quality/gate-proofs.md` 第 5 節）
- 台帳が版 1 の形でなければ、理由 1 行を出して終了コード 1（FR-015）
- カテゴリ行のクリックでアコーディオンがトグルし、`categories.json` の `expanded` を一時ファイル経由で原子的に書き戻す（FR-004 / FR-007）。書き戻せなければ表示は変えず理由 1 行
- ノート行のクリックで右ペインの RichEdit に md を表示する（FR-005 の閲覧側。見出し・強調・コード・リスト・リンク・引用のサブセット）
- 見た目はデザイン「案2 堅」。OS のアプリのモードに従ってライト／ダークが切り替わる（FR-016）
- 右ペインの頭の「編集」の札で同じ RichEdit が平文の編集になり、Ctrl+S・「閲覧」の札・別ノートの選択・窓を閉じる操作で同じ md へ原子的に書き戻す（FR-006）。本文が同じなら書かず、改行の形（LF / CRLF）は元のまま・BOM は書かない。書き戻せなければ理由 1 行を出して編集中の本文を残す（FR-015）
- ドロワーの行をドラッグすると挿入線（掴んだ行のカテゴリ色）が出て、離すと表示順が変わり、カテゴリなら `categories.json`、
  ノートなら `data/<カテゴリ>/index.json` へ原子的に書き戻す（FR-008 / FR-009）。押して動かさずに離せば今までどおりの
  クリック。書き戻せなければ理由 1 行を出して表示は変えない
- ノートは**別のカテゴリへも移せる**（ADR 0008）。ポインタのある塊のカテゴリが移動先で、折り畳んでいれば末尾に入る。
  離すと md が移動先のディレクトリへ移り、移動先 → 移動元の順に `index.json` が書き戻る。移動先に同じ名前のノートが
  あれば移さずに理由 1 行。md を移せなければ何も変えない。md は移ったが台帳を書けなかったときは
  「次回の起動で揃います」の 1 行を出し、表示はファイルに従う（FR-008 の照合で自己修復する）
- 編集中でも並び替えと移動ができる。本文は RichEdit が持ったままで、右ペインの頭のカテゴリ名・番号・色だけが付け替わる
- 索引が窓に収まらなくなると、ポインタをドロワーに置いたホイール（1 刻み = ノート行 3 つ）と、主窓が受ける
  ↑↓（1 行）・PgUp / PgDn（1 画面 − 1 行）でスクロールする（FR-012）。**スクロールバーは出さない**。上に隠れた行が
  あれば頭の帯の直下に、下に隠れた行があれば下端に、24 px（96 DPI）のフェードが地の色へ線形に混ざる。上限は core の
  純関数で、量の所有者は application。スクロールした位置でもクリック・トグル・ドラッグの挿入線とドロップ先は正しく、
  ドラッグ中にホイールを回すと同じポインタ位置で挿入線が引き直される。編集中にドロワーをスクロールしても本文と Ctrl+S は無事
- 中核の分岐 1274/1342（94.86%）。確保失敗の経路は測定ビルドの注入で全部走る

## 動かないもの

- 破棄（保存せずに編集を抜ける）・打鍵ごとの自動保存・リンクを開く
- 右クリックの色変更・検索・ドラッグ中の自動スクロールと半透明の追従・横スクロール・スクロール位置の保存
- 選んだノートや移した先を見える位置へ寄せること（利用者の位置を勝手に動かさない・ADR 0009）
- ノートの改名・カテゴリの入れ子・移動の取り消し（Undo）・外部で変わった md の検知
- 起動中の OS テーマ変更への追従
- 起動時の `index.json` の書き戻し（並び替えか移動をするまで作られない。外から md を足しただけのカテゴリは走査順のまま）
- DPI 切り替え・スナップ・縁のリサイズは実装したが実機で未確認

## 次の 1 手

残るのは**右クリックの色変更（FR-010）→ 検索（FR-011）**の 2 本。どちらから取るかは施主の判断に委ねる
（2 つとも縦切り 1 本ぶんで、互いに依存しない）。

- FR-010 色変更: 台帳の `color` を書き戻す経路は `expanded` と同じで、要るのは色を選ばせる UI と `rgb_color` の意図 1 本
- FR-011 検索: ドロワーの頭に検索窓が要る。索引の絞り込みは core の純関数に置く。行が減れば
  スクロール上限も縮むだけで済む（ADR 0009 の想定）

FR-006 の残り（IME のかな漢字変換の未確定文字列が残ったままの Ctrl+S）と、
FR-009 の残り（96 DPI と高 DPI での挿入線・実際のマウスでのドラッグ・大文字小文字だけ違う名前への移動）、
FR-012 の残り（96 DPI と高 DPI でのフェード・実際のマウスのホイール）は施主の実機確認に依頼する。
