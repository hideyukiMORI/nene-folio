# いまのタスク — NeNe Folio

> GitHub Issue が正。ここは要約であり、Markdown のチェックリストをタスク状態として扱わない。
> 更新は実測でだけ行う（「動くもの」は `pwsh -NoProfile -File ./eng/check.ps1` が通ったもの）。

## 段階

| 段階 | 状態 |
| --- | --- |
| Phase 0 言語の実測 | ✅ 2026-09-09（69 ケース・`docs/quality/phase0-results.json`） |
| Phase 1 文書とゲートの足場 | ✅ 2026-09-09（Issue #1・フルゲート緑） |
| Phase 2 negative proof | ✅ 2026-09-09（17 行・`docs/quality/gate-proofs.md`。ruleset と CI の実行証拠は PR #2 で取った） |
| Phase 3 縦切り 1 本 | ✅ 2026-09-09（Issue #3・ADR 0004。core / application / adapters / ui / app と tests/unit。ARC-002 / ARC-003 / ARC-007 / QLT-009 が active） |
| Phase 3 縦切り 2 本目（トグルと保存） | ✅ 2026-09-09（Issue #5。意図 → reducer → 書き込みポート → 原子的な置き換え） |
| Phase 3 縦切り 3 本目（右ペインの閲覧） | ✅ 2026-09-09（Issue #7。md → RTF のサブセット変換・note_text・RichEdit のビュー） |
| Phase 3 縦切り 4 本目（見た目とテーマ） | ✅ 2026-09-09（Issue #9・ADR 0005。案2「堅」・OS に従うライト／ダーク） |
| Phase 3 縦切り 5 本目（編集と保存） | ✅ 2026-09-10（Issue #11・ADR 0006。札 2 つ・平文編集・Ctrl+S と抜けるときの原子的な書き戻し） |
| Phase 3 縦切り 6 本目（ドラッグの並び替え） | ✅ 2026-09-10（Issue #13・ADR 0007。ドロップ先は core の純関数・`index.json` への初めての書き戻し） |
| Phase 3 縦切り 7 本目（別カテゴリへの移動） | ✅ 2026-09-10（Issue #15・ADR 0008。md の rename が先で両方の `index.json` が追随・同名は拒む） |
| Phase 3 縦切り 8 本目（ドロワーのスクロール） | ✅ 2026-09-10（Issue #19・ADR 0009。上限と表示座標は core・量の所有者は application・上下端のフェードは自前の合成） |
| Phase 3 縦切り 9 本目（カテゴリの色変更） | ✅ 2026-09-10（Issue #21・ADR 0010。右クリック → OS の `ChooseColorW` → 意図 1 本。`expanded` と同じ書き戻し経路） |
| Phase 3 縦切り 10 本目（保存の直前の履歴） | ✅ 2026-09-10（Issue #23・ADR 0012。`data/.history` に連番で 5 版・残せなければ保存しない・`.` 始まりのディレクトリは走査しない） |
| Phase 3 縦切り 11 本目（2 区画と vim の鍵） | ✅ 2026-09-11（Issue #24・ADR 0013。区画は Win32 のフォーカスそのもの・辿り方と寄せる量は core / application・切り替えの順は主窓の 1 か所） |
| Phase 3 縦切り 12 本目（カテゴリ行のカーソル） | ✅ 2026-09-11（Issue #32・ADR 0015。折り畳んだ／空のカテゴリ行にも止まり、右ペインは変えず、`h` / `l` で開閉。面は選択・角はカーソル） |
| Phase 4 公開 | 🔲 |

## 動くもの

- 起動して、実行ファイルと同じ場所の `data/` を走査し、`categories.json` / `index.json` と照合した索引を枠なし窓の左ドロワーに描く（96 DPI・1 モニタで実機確認。`docs/quality/gate-proofs.md` 第 5 節）
- 台帳が版 1 の形でなければ、理由 1 行を出して終了コード 1（FR-015）
- カテゴリ行のクリックでアコーディオンがトグルし、`categories.json` の `expanded` を一時ファイル経由で原子的に書き戻す（FR-004 / FR-007）。書き戻せなければ表示は変えず理由 1 行
- ノート行のクリックで右ペインの RichEdit に md を表示する（FR-005 の閲覧側。見出し・強調・コード・リスト・リンク・引用のサブセット）
- 見た目はデザイン「案2 堅」。OS のアプリのモードに従ってライト／ダークが切り替わる（FR-016）
- 右ペインの頭の「編集」の札で同じ RichEdit が平文の編集になり、Ctrl+S・「閲覧」の札・別ノートへの切り替え・本文の `Esc`・窓を閉じる操作で同じ md へ原子的に書き戻す（FR-006）。本文が同じなら書かず、改行の形（LF / CRLF）は元のまま・BOM は書かない。書き戻せなければ理由 1 行を出して編集中の本文を残す（FR-015）。**閲覧へ戻すのは「閲覧」の札だけ**。窓を閉じる操作は編集モードを切り替えず、保存成功後に終了する。ノートを切り替えてもモードは保つ（ADR 0013 / ADR 0016）
- ドロワーの行をドラッグすると挿入線（掴んだ行のカテゴリ色）が出て、離すと表示順が変わり、カテゴリなら `categories.json`、
  ノートなら `data/<カテゴリ>/index.json` へ原子的に書き戻す（FR-008 / FR-009）。押して動かさずに離せば今までどおりの
  クリック。書き戻せなければ理由 1 行を出して表示は変えない
- ノートは**別のカテゴリへも移せる**（ADR 0008）。ポインタのある塊のカテゴリが移動先で、折り畳んでいれば末尾に入る。
  離すと md が移動先のディレクトリへ移り、移動先 → 移動元の順に `index.json` が書き戻る。移動先に同じ名前のノートが
  あれば移さずに理由 1 行。md を移せなければ何も変えない。md は移ったが台帳を書けなかったときは
  「次回の起動で揃います」の 1 行を出し、表示はファイルに従う（FR-008 の照合で自己修復する）
- 編集中でも並び替えと移動ができる。本文は RichEdit が持ったままで、右ペインの頭のカテゴリ名・番号・色だけが付け替わる
- 索引が窓に収まらなくなると、ポインタをドロワーに置いたホイール（1 刻み = ノート行 3 つ）と、主窓が受ける
  ↑↓（1 行）・PgUp / PgDn（1 画面 − 1 行）でスクロールする（FR-012）。**スクロールバーは出さない**。上に隠れた行が
  あれば頭の帯の直下に、下に隠れた行があれば下端に、24 px（96 DPI）のフェードが地の色へ線形に混ざる。上限は core の
  純関数で、量の所有者は application。スクロールした位置でもクリック・トグル・ドラッグの挿入線とドロップ先は正しく、
  ドラッグ中にホイールを回すと同じポインタ位置で挿入線が引き直される。編集中にドロワーをスクロールしても本文と Ctrl+S は無事
- カテゴリ行を**右クリック**すると OS の色の選択（`ChooseColorW`・全開・初期値はその行の色・カスタム色 16 枠は白）が出て、
  OK で閉じるとドロワーの番号・選択行の角の印・挿入線・右ペインの頭のパンくずがその色になり、`categories.json` の
  `color` に `#RRGGBB`（大文字）で原子的に書き戻る（FR-010）。キャンセルと同じ色は書かない。ノート行・頭の帯・
  左ボタンを押している間の右クリックは何もしない。編集中に色を変えても本文と編集モードは保たれる。
  書き戻せなければ理由 1 行を出して表示は変えない。**色の選択の窓は OS の部品なので、ダークでもライトのまま出る**（ADR 0010）
- 枠なし窓の client は**作成の瞬間から窓の矩形全体**（`WM_NCCALCSIZE` は TRUE も FALSE も自分で答え、既定処理に
  渡さない）。窓は 96 DPI で 560×360 より小さくならず、最大化するとモニタの作業領域に収まる。大きさを変えると
  ドロワーの高さ・右ペインの頭の札・パンくずが新しい大きさで描き直る。右ペインの頭が狭いときは
  **ノート名 → カテゴリ名の順に末尾が省略記号**になり、番号・「/」・札 2 つは残る（ADR 0011）
- md を書き戻す**直前**の本文が `data/.history/<カテゴリ>/<ノート>/1.md`（最新）〜`5.md`（最古）に連番で残り、
  6 つ目は捨てられる（FR-017 / ADR 0012）。本文が同じなら履歴も md も書かない。**履歴を残せなければ保存しない**
  （「履歴を書けなかったので保存していません。編集中の本文は残っています。」の 1 行を出し、md も編集中の本文もそのまま）。
  履歴はファイルのバイト列をそのまま写すので改行の形も BOM の無さも保たれ、`.` で始まるディレクトリは
  カテゴリとして走査しないので `.history` はドロワーに出ない。`data/` を丸ごと写せば履歴も一緒に移る
- フォーカスの区画は**索引（主窓）と本文（RichEdit）の 2 つ**で、押した側が取る（FR-018 / ADR 0013 / ADR 0015）。索引にあるとき
  `j` / `k` が次／前の**止まる行**（カテゴリごとに、見えるノート行があればそのノート、無ければ
  **折り畳んだ／ノート 0 本のカテゴリ行**。展開中でノートを持つカテゴリ行は飛ばす・端で止まる）、`gg` / `G` が最初／最後、
  `h` / `l` が**カーソルの行のカテゴリ**の折り畳み／展開（同じなら書かない）、`Enter` で本文へ。↑↓ / PgUp / PgDn は
  これまでどおりスクロール。**カーソルがカテゴリ行にあるあいだ、選択も右ペインも変わらず保存も走らない**。
  `h` で畳むとカーソルはそのカテゴリ行へ移り、`l` で展開すると選択中のノートがその中にあればそこへ、
  無ければ最初のノートへ移る（0 本なら行に留まる）。`Enter` はカテゴリ行では何もしない。
  **鍵で動かしたときだけ**カーソルの行が見える位置へ寄る（クリックでは寄せない）。
  本文にあるとき `Esc` は、編集中なら保存してから索引へ戻す（Ctrl+S はそのまま）。**モードはペインの状態として残り**、
  編集のままノートを切り替えると、いまのノートを保存してから次のノートが**編集モードで**開く（保存に失敗したら
  理由 1 行を出して選択は動かない）。**Escape で窓は閉じない**（Alt+F4 と閉じる操作だけ）。面は選択の行に敷き、
  **右端の角はカーソルの行**（カテゴリ行なら `+` / `−` の左）に置く。角は索引にフォーカスがあるとき塗り・本文にあるとき枠 1px
- 中核の分岐 1427/1510（94.50%）。確保失敗の経路は測定ビルドの注入で全部走る

## 動かないもの

- 終了せずに未保存本文を破棄して編集を抜ける操作・打鍵ごとの自動保存・リンクを開く
- 検索・ドラッグ中の自動スクロールと半透明の追従・横スクロール・スクロール位置の保存
- カスタム色の保存（色の選択で作った色は閉じると消える）・色の変更の Undo・ノート行の右クリックのメニュー（ADR 0010）
- クリックや移動で選んだノートを見える位置へ寄せること（寄せるのは鍵で動かしたときだけ・ADR 0009 / ADR 0013）
- **展開中でノートを持つカテゴリ行には止まらない**（畳むには中のノートに立って `h`・ADR 0015 の「失う・残る」）。
  複数選択・`dd` などの削除・キーバインドの設定（ADR 0013 の「やらないこと」）
- **IME が ON のときの `j` / `k`**（未確定文字になって索引へ届かない。英数へ切り替える。対策しない・ADR 0013）
- ノートの改名・カテゴリの入れ子・移動の取り消し（Undo）・外部で変わった md の検知
- **履歴から前の版へ戻す UI**（別 Issue）。いまは `data/.history` を手で開いて戻す
- **移動時の履歴の追随**（ノートを別カテゴリへ移しても履歴は付いて行かず、元の場所に孤立する。
  移動後の最初の保存で新しい場所に積み始める・ADR 0012 の決定 5）。保持数の設定・時刻付きの履歴・差分表示・履歴の圧縮も無い
- 起動中の OS テーマ変更への追従
- 起動時の `index.json` の書き戻し（並び替えか移動をするまで作られない。外から md を足しただけのカテゴリは走査順のまま）
- スナップ（Win + ←→）と、実際のマウスで縁を掴んだリサイズは実装したが実機で未確認。
  DPI の切り替え（120 → 144 → 168）・最大化と復帰・`SetWindowPos` での大きさの変更は実機で確認した
  （`docs/quality/gate-proofs.md` 第 5-j 節）。**96 DPI はこの機械にモニタが無いので施主に依頼**
- 窓の枠に白い帯が出る問題（Issue #22）は 2 段で直した。作成直後の 8 px は `WM_NCCALCSIZE` の `wParam == FALSE`（ADR 0011）、
  放置後の再発は非アクティブ化の `WM_NCACTIVATE` の既定処理が `WS_THICKFRAME` の縁を描いていたもの（ADR 0014・`WM_NCACTIVATE` → TRUE・
  `WM_NCPAINT` → 0）。証拠は「作成直後から差 0 px」と「非アクティブ化の直後に外周 8 px が地の色のまま」（第 5-j 節と追補）。
  **施主の実機で放置後も帯が出ないことの確認が残る**

## 次の 1 手

2026-09-12 の hide の追加要望により、**共通コマンド・INDEX の `:`・Ctrl+P（Issue #37、ADR 0016）を実装中**。
保存・未保存なら拒否する終了・保存して終了・明示的な破棄・ヘルプと閉じるアイコンを追加した。
core/applicationの単体・確保失敗テストとレビューは通過し、GUI実機確認を残してDraftで扱う。
Computer Useのnative pipeに接続できず、打鍵・IME・Undo/キャレット・DPIは未確認。最終ゲートの結果はPRに記録する。
検索（FR-011）は Issue #40 として継続する。全体の優先順は [計画 #36](../plans/2026-09-12-command-workspace.md)。
設定・Ubuntu 風テーマ・日本語／English／简体中文は #38、行番号は #39、現在 md の正規表現置換は #41、
Google Fonts の同梱は #42。これらは計画であり、まだ「動くもの」には含めない。

Issue #32（FR-018 の改定）は ADR 0015 のとおり実装した（枝 `feat/32-cursor-collapsed-categories`）。
Issue #24（FR-018）は ADR 0013 のとおり実装した（枝 `feat/24-index-focus-vim`）。
Issue #23（FR-017）は ADR 0012 のとおり（枝 `feat/23-note-history`）。
Issue #22（枠の白い線・リサイズ後の再描画・最小サイズとパンくずのはみ出し）は ADR 0011 と ADR 0014 のとおり実装した
（PR #26 と枝 `fix/22-ncactivate`）。白い線が施主の実機で放置後も出ないことの確認だけが残る。

- FR-011 検索: ドロワーの頭に検索窓が要る。索引の絞り込みは core の純関数に置く。行が減れば
  スクロール上限も縮むだけで済む（ADR 0009 の想定）。**フォーカスの区画が 3 つ目（検索窓）になるので、
  ADR 0013 の「区画は Win32 のフォーカスそのもの」をどこまで伸ばすかを先に決める**

FR-018 の残り（IME が ON のときの `j`・96 DPI と高 DPI での寄せ方と印・実際のキーボードでの打鍵・
カテゴリ行のカーソルとドラッグの取り合わせ）と、
FR-006 の残り（IME のかな漢字変換の未確定文字列が残ったままの Ctrl+S）と、
FR-009 の残り（96 DPI と高 DPI での挿入線・実際のマウスでのドラッグ・大文字小文字だけ違う名前への移動）、
FR-012 の残り（96 DPI と高 DPI でのフェード・実際のマウスのホイール）、
FR-010 の残り（96 DPI と高 DPI・実際のマウスの右ボタン・Shift+F10 / Apps 鍵・カスタム色が閉じると消えること）は
施主の実機確認に依頼する。
